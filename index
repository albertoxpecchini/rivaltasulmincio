<!DOCTYPE html>
<html lang="it">
<head>
  <meta charset="UTF-8" />
  <script>!function(){try{var p=JSON.parse(localStorage.getItem('rsm_prefs_v1'))||{},r=document.documentElement,s={sm:'14px',md:'16px',lg:'18px',xl:'20px'},t=p.theme;var ok=t==='chiaro'||t==='scuro'||t==='auto';t=ok?t:'chiaro';r.dataset.theme=t==='auto'?(window.matchMedia&&window.matchMedia('(prefers-color-scheme: dark)').matches?'scuro':'chiaro'):t;r.dataset.themePref=t;r.dataset.fontsize=p.fontsize||'md';r.style.fontSize=s[p.fontsize]||'16px';r.dataset.density=p.uiDensity||'comfortable';if(p.contrast)r.dataset.contrast='high';else delete r.dataset.contrast;}catch(e){}}();</script>
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />

  <title>Pro Loco Rivalta sul Mincio | Valli del Mincio, Festa del Pesce, Ecoturismo</title>
  <meta name="description" content="Pro Loco Rivalta sul Mincio (Mantova): scopri le Valli del Mincio, la Festa del Pesce e del Luccio, l'ecoturismo, le sagre e le tradizioni del borgo nel cuore del Parco del Mincio. Eventi, escursioni, contatti." />
  <meta name="keywords" content="Rivalta sul Mincio, Pro Loco Rivalta, Valli del Mincio, Parco del Mincio, Mantova, festa del pesce, sagra del luccio, ecoturismo Mantova, Rodigo, ciclabile Mincio" />
  <meta name="author" content="Pro Loco Rivalta sul Mincio" />
  <meta name="robots" content="index, follow, max-image-preview:large, max-snippet:-1" />
  <link rel="canonical" href="https://www.rivaltasulmincio.it/" />
  <meta name="theme-color" content="#ffffff" />

  <meta name="geo.region" content="IT-MN" />
  <meta name="geo.placename" content="Rivalta sul Mincio, Rodigo" />
  <meta name="geo.position" content="45.181000;10.665000" />
  <meta name="ICBM" content="45.181000, 10.665000" />

  <meta property="og:type" content="website" />
  <meta property="og:site_name" content="Pro Loco Rivalta sul Mincio" />
  <meta property="og:title" content="Pro Loco Rivalta sul Mincio - Valli del Mincio e tradizioni" />
  <meta property="og:description" content="Ecoturismo, sagre e natura nel cuore del Parco del Mincio. Scopri le Valli, il luccio e i percorsi del borgo." />
  <meta property="og:url" content="https://www.rivaltasulmincio.it/" />
  <meta property="og:locale" content="it_IT" />
  <meta property="og:image" content="https://www.rivaltasulmincio.it/img/Gemini_Generated_Image_9mqfj89mqfj89mqf.jpg" />
  <meta property="og:image:width" content="1200" />
  <meta property="og:image:height" content="630" />
  <meta property="og:image:alt" content="Veduta aerea di Rivalta sul Mincio" />

  <meta name="twitter:card" content="summary_large_image" />
  <meta name="twitter:title" content="Pro Loco Rivalta sul Mincio" />
  <meta name="twitter:description" content="Le Valli del Mincio, il luccio e le tradizioni del borgo. Ecoturismo nel Parco del Mincio." />
  <meta name="twitter:image" content="https://www.rivaltasulmincio.it/img/Gemini_Generated_Image_9mqfj89mqfj89mqf.jpg" />

  <script type="application/ld+json">
  {
    "@context": "https://schema.org",
    "@graph": [
      {
        "@type": "Organization",
        "@id": "https://www.rivaltasulmincio.it/#organization",
        "name": "Pro Loco Rivalta sul Mincio",
        "alternateName": "Pro Loco Rivalta",
        "url": "https://www.rivaltasulmincio.it/",
        "description": "Associazione di promozione turistica, culturale e ambientale del borgo di Rivalta sul Mincio, nel cuore del Parco Regionale del Mincio.",
        "email": "info@prolocorivalta.mn.it",
        "telephone": "+39-339-899-5680",
        "address": {
          "@type": "PostalAddress",
          "streetAddress": "Via Porto, 31",
          "addressLocality": "Rivalta sul Mincio",
          "addressRegion": "MN",
          "postalCode": "46040",
          "addressCountry": "IT"
        },
        "sameAs": [
          "https://www.facebook.com/prolocrivaltasulmincio",
          "https://www.instagram.com/prolocrivalta"
        ]
      },
      {
        "@type": "WebSite",
        "@id": "https://www.rivaltasulmincio.it/#website",
        "url": "https://www.rivaltasulmincio.it/",
        "name": "Pro Loco Rivalta sul Mincio",
        "description": "Sito ufficiale della Pro Loco di Rivalta sul Mincio: Valli del Mincio, Festa del Pesce, ecoturismo, eventi e tradizioni.",
        "publisher": { "@id": "https://www.rivaltasulmincio.it/#organization" },
        "inLanguage": "it-IT"
      },
      {
        "@type": "Festival",
        "@id": "https://www.rivaltasulmincio.it/#festa-pesce",
        "name": "Festa del Pesce e del Luccio alla Rivaltese - 40ª edizione",
        "description": "Quarantesima edizione (1985-2025) della Festa del Pesce e del Luccio alla Rivaltese. Cucina tipica d'acqua dolce, vini di Cantina Ricchi, servizio al tavolo dalle 19:30. In concomitanza la BARACCA dei NOTT-AMBULI con street food e 6 concerti live.",
        "startDate": "2025-07-04T19:30:00+02:00",
        "endDate": "2025-07-13T23:30:00+02:00",
        "eventStatus": "https://schema.org/EventScheduled",
        "eventAttendanceMode": "https://schema.org/OfflineEventAttendanceMode",
        "location": {
          "@type": "Place",
          "name": "Area Feste Platana",
          "address": {
            "@type": "PostalAddress",
            "addressLocality": "Rivalta sul Mincio",
            "addressRegion": "MN",
            "postalCode": "46040",
            "addressCountry": "IT"
          }
        },
        "organizer": { "@id": "https://www.rivaltasulmincio.it/#organization" },
        "url": "https://www.rivaltasulmincio.it/#festa"
      },
      {
        "@type": "ItemList",
        "@id": "https://www.rivaltasulmincio.it/#sections",
        "name": "Esplora il sito",
        "itemListElement": [
          { "@type": "SiteNavigationElement", "position": 1, "name": "Intro", "url": "https://www.rivaltasulmincio.it/#intro" },
          { "@type": "SiteNavigationElement", "position": 2, "name": "Mappa della pagina", "url": "https://www.rivaltasulmincio.it/#mappa" },
          { "@type": "SiteNavigationElement", "position": 3, "name": "Festa del Pesce", "url": "https://www.rivaltasulmincio.it/#festa" },
          { "@type": "SiteNavigationElement", "position": 4, "name": "Eventi", "url": "https://www.rivaltasulmincio.it/#eventi" },
          { "@type": "SiteNavigationElement", "position": 5, "name": "Galleria", "url": "https://www.rivaltasulmincio.it/#galleria" },
          { "@type": "SiteNavigationElement", "position": 6, "name": "Territorio", "url": "https://www.rivaltasulmincio.it/#territorio" },
          { "@type": "SiteNavigationElement", "position": 7, "name": "Contatti", "url": "https://www.rivaltasulmincio.it/#contatti" }
        ]
      }
    ]
  }
  </script>

  <link rel="preconnect" href="https://api.fontshare.com" crossorigin />
  <link rel="preconnect" href="https://fonts.googleapis.com" />
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
  <link href="https://api.fontshare.com/v2/css?f[]=cabinet-grotesk@400,500,700,800,900&display=swap" rel="stylesheet" />
  <link href="https://fonts.googleapis.com/css2?family=Fraunces:ital,opsz,wght@0,9..144,400..900;1,9..144,400..900&family=JetBrains+Mono:wght@400;500;700&display=swap" rel="stylesheet" />

  <link rel="icon" href="/favicon.png" type="image/png" />
  <link rel="shortcut icon" href="/favicon.png" />
  <link rel="apple-touch-icon" href="/favicon.png" />

  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/swiper@11/swiper-bundle.min.css" />
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/tippy.js@6/dist/tippy.css" />
  <link rel="stylesheet" href="/theme.css" />

  <style>

    /* =========================================================
       NAV — nessun override: stile gestito da /partials/topbar.css
       ========================================================= */

    /* =========================================================
       SECTION 1 — INTRO (HERO)
       ========================================================= */
    .rsm-hero {
      position: relative;
      padding: clamp(110px, 14vw, 200px) 0 clamp(64px, 8vw, 120px);
      overflow: hidden;
    }
    .rsm-hero__layout {
      display: grid;
      grid-template-columns: minmax(0, 1.05fr) minmax(360px, 1fr);
      align-items: center;
      gap: clamp(28px, 4vw, 64px);
    }
    .rsm-hero__title {
      margin: 0 0 var(--rsm-s-6);
      color: var(--rsm-ink);
    }
    .rsm-hero__main {
      min-width: 0;
      display: grid;
      gap: var(--rsm-s-5);
      align-content: start;
    }
    .rsm-hero__main .rsm-lead {
      max-width: 58ch;
    }
    .rsm-hero__media {
      position: relative;
      width: 100%;
      aspect-ratio: 4 / 5;
      max-height: clamp(360px, 56vh, 580px);
      min-height: 320px;
      margin-inline: auto;
      max-width: 520px;
      border-radius: var(--rsm-r-3xl);
      overflow: hidden;
      box-shadow: var(--rsm-sh-xl);
      isolation: isolate;
    }
    .rsm-hero__media::after {
      content: "";
      position: absolute;
      inset: 0;
      border-radius: inherit;
      box-shadow: inset 0 0 0 1px rgba(255,255,255,0.45);
      pointer-events: none;
    }
    .rsm-hero__metrics {
      display: grid;
      grid-template-columns: repeat(auto-fit, minmax(140px, 1fr));
      gap: var(--rsm-s-5);
      margin-top: var(--rsm-s-12);
      padding-top: var(--rsm-s-7);
      border-top: 1px solid var(--rsm-line);
    }
    .rsm-metric strong {
      font-family: var(--rsm-font-display);
      font-size: clamp(2rem, 3vw, 2.8rem);
      font-weight: 600;
      line-height: 1;
      letter-spacing: -0.04em;
      display: block;
      color: var(--rsm-ink);
    }
    .rsm-metric span {
      display: block;
      margin-top: 6px;
      font-family: var(--rsm-font-mono);
      font-size: var(--rsm-text-tiny);
      letter-spacing: 0.06em;
      text-transform: uppercase;
      color: var(--rsm-ink-3);
    }

    /* hero swiper (GIF) */
    .rsm-swiper--hero {
      width: 100%;
      height: 100%;
    }
    .rsm-swiper--hero .swiper-slide {
      overflow: hidden;
      border-radius: var(--rsm-r-3xl);
    }
    .rsm-swiper--hero img,
    .rsm-swiper--hero video {
      width: 100%;
      height: 100%;
      object-fit: cover;
      object-position: center;
    }

    /* hero accent ring */
    .rsm-hero__accent {
      position: absolute;
      width: 140%;
      height: 140%;
      top: -20%;
      left: -20%;
      pointer-events: none;
      z-index: -1;
      background:
        radial-gradient(ellipse at 22% 30%, rgba(31,191,125,0.18), transparent 50%),
        radial-gradient(ellipse at 78% 70%, rgba(139,92,246,0.18), transparent 50%);
      animation: rsm-aurora-float 26s ease-in-out infinite;
    }

    /* =========================================================
       SECTION 0 — BANNER VIDEO
       ========================================================= */
    .rsm-banner {
      position: relative;
      width: 100vw;
      margin-left: calc(-50vw + 50%);
      height: 80vh;
      min-height: 520px;
      overflow: hidden;
      display: flex;
      align-items: center;
      justify-content: center;
    }
    .rsm-banner__video-wrap {
      position: absolute;
      inset: 0;
    }
    .rsm-banner__video {
      width: 100%;
      height: 100%;
      object-fit: cover;
      object-position: center;
    }
    .rsm-banner__overlay {
      position: absolute;
      inset: 0;
      background: linear-gradient(
        160deg,
        rgba(7,58,45,0.55) 0%,
        rgba(9,45,94,0.45) 50%,
        rgba(75,28,118,0.55) 100%
      );
    }
    .rsm-banner__content {
      position: relative;
      z-index: 2;
      text-align: center;
      color: #fff;
      padding: 0 clamp(20px, 5vw, 80px);
      max-width: 900px;
    }
    .rsm-banner__kicker {
      color: rgba(255,255,255,0.9);
    }
    .rsm-banner__title {
      color: #fff;
      font-size: clamp(3.5rem, 12vw, 10rem);
      margin: var(--rsm-s-4) 0 var(--rsm-s-5);
      text-shadow: 0 4px 40px rgba(0,0,0,0.3);
      line-height: 0.95;
    }
    .rsm-banner__title em {
      color: var(--rsm-emerald-2);
    }
    .rsm-banner__sub {
      color: rgba(255,255,255,0.8);
      font-size: clamp(1rem, 1.8vw, 1.3rem);
      margin-bottom: var(--rsm-s-8);
      max-width: 72ch;
      margin-left: auto;
      margin-right: auto;
    }
    .rsm-banner__cta {
      display: inline-flex;
    }
    .rsm-banner__scroll-hint {
      position: absolute;
      bottom: 40px;
      left: 50%;
      transform: translateX(-50%);
      z-index: 2;
    }
    .rsm-banner__scroll-line {
      display: block;
      width: 1px;
      height: 60px;
      background: linear-gradient(180deg, transparent, rgba(255,255,255,0.7));
      margin: 0 auto;
      animation: rsm-scroll-hint 2s ease-in-out infinite;
    }
    @keyframes rsm-scroll-hint {
      0%, 100% { opacity: 0; transform: scaleY(0); transform-origin: top; }
      50% { opacity: 1; transform: scaleY(1); }
    }

    /* =========================================================
       SECTION 2 — MAPPA DELLA PAGINA
       ========================================================= */
    .rsm-compass {
      position: relative;
      background: linear-gradient(135deg, rgba(255,255,255,0.96), rgba(255,255,255,0.88));
    }
    .rsm-compass__inner {
      display: grid;
      grid-template-columns: minmax(0, 1fr) minmax(0, 1.2fr);
      gap: clamp(24px, 4vw, 64px);
    }
    .rsm-compass__copy { display: grid; gap: var(--rsm-s-4); align-content: start; }
    .rsm-compass__nav {
      display: grid;
      grid-template-columns: repeat(2, 1fr);
      gap: var(--rsm-s-3);
    }
    .rsm-compass__link {
      display: flex;
      align-items: baseline;
      gap: var(--rsm-s-3);
      padding: 18px 22px;
      border: 1px solid var(--rsm-line);
      border-radius: var(--rsm-r-xl);
      background: #fff;
      color: var(--rsm-ink);
      font-family: var(--rsm-font-sans);
      font-weight: 800;
      font-size: 1.05rem;
      letter-spacing: -0.015em;
      transition: transform var(--rsm-dur-fast), border-color var(--rsm-dur-fast), box-shadow var(--rsm-dur-fast);
      position: relative;
    }
    .rsm-compass__link strong {
      font-family: var(--rsm-font-mono);
      font-weight: 500;
      font-size: 0.78rem;
      letter-spacing: 0.16em;
      color: var(--rsm-ink-4);
    }
    .rsm-compass__link span { flex: 1; }
    .rsm-compass__link::after {
      content: "→";
      font-family: var(--rsm-font-mono);
      color: var(--rsm-ink-4);
      transition: transform var(--rsm-dur-fast), color var(--rsm-dur-fast);
    }
    .rsm-compass__link:hover {
      transform: translateY(-2px);
      border-color: var(--rsm-ink);
      box-shadow: var(--rsm-sh-md);
    }
    .rsm-compass__link:hover::after { transform: translateX(4px); color: var(--rsm-ink); }
    .rsm-compass__link:nth-child(1):hover { box-shadow: var(--rsm-sh-glow-emerald); }
    .rsm-compass__link:nth-child(2):hover { box-shadow: var(--rsm-sh-glow-azure); }
    .rsm-compass__link:nth-child(3):hover { box-shadow: var(--rsm-sh-glow-violet); }
    .rsm-compass__link:nth-child(4):hover { box-shadow: var(--rsm-sh-glow-emerald); }
    .rsm-compass__link:nth-child(5):hover { box-shadow: var(--rsm-sh-glow-azure); }
    .rsm-compass__link:nth-child(6):hover { box-shadow: var(--rsm-sh-glow-violet); }

    .rsm-compass__meta {
      display: flex;
      flex-wrap: wrap;
      gap: var(--rsm-s-2);
      margin-top: var(--rsm-s-3);
    }

    /* =========================================================
       SECTION 3 — FESTA DEL PESCE
       ========================================================= */
    .rsm-festa {
      position: relative;
      padding: clamp(80px, 10vw, 160px) 0;
      background:
        radial-gradient(ellipse at 0% 50%, rgba(31,191,125,0.07) 0%, transparent 60%),
        radial-gradient(ellipse at 100% 50%, rgba(74,155,255,0.06) 0%, transparent 60%),
        linear-gradient(180deg, rgba(31,191,125,0.04) 0%, transparent 40%, rgba(74,155,255,0.04) 100%);
    }
    .rsm-festa__head {
      display: grid;
      grid-template-columns: minmax(0, 1.3fr) minmax(280px, 1fr);
      gap: clamp(32px, 4vw, 64px);
      align-items: end;
      margin-bottom: var(--rsm-s-12);
    }
    .rsm-festa__title {
      font-family: var(--rsm-font-display);
      font-size: clamp(2.2rem, 5vw, 4.6rem);
      font-weight: 600;
      line-height: 0.96;
      letter-spacing: -0.04em;
      margin: var(--rsm-s-4) 0 var(--rsm-s-5);
    }
    .rsm-festa__title em {
    }
    .festa-pulse,
    .rsm-festa__pulse {
      width: 8px;
      height: 8px;
      border-radius: 50%;
      background: var(--rsm-emerald);
      box-shadow: 0 0 0 0 rgba(31,191,125, 0.6);
      animation: rsm-pulse 2s var(--rsm-ease-out) infinite;
      display: inline-block;
      margin-right: 6px;
    }
    @keyframes rsm-pulse {
      0%   { box-shadow: 0 0 0 0 rgba(31,191,125, 0.55); }
      70%  { box-shadow: 0 0 0 14px rgba(31,191,125, 0); }
      100% { box-shadow: 0 0 0 0 rgba(31,191,125, 0); }
    }

    .rsm-festa__poster {
      position: relative;
      width: 100%;
      max-width: 380px;
      aspect-ratio: 3 / 4;
      max-height: clamp(320px, 48vh, 480px);
      margin-left: auto;
      border-radius: var(--rsm-r-2xl);
      overflow: visible;
      box-shadow: var(--rsm-sh-xl);
    }
    .rsm-festa__poster img {
      width: 100%;
      height: 100%;
      object-fit: cover;
      object-position: center;
      border-radius: inherit;
    }
    .rsm-festa__badge {
      position: absolute;
      bottom: -22px;
      right: -16px;
      width: clamp(86px, 12vw, 110px);
      height: clamp(86px, 12vw, 110px);
      border-radius: 50%;
      display: grid;
      place-items: center;
      text-align: center;
      font-family: var(--rsm-font-display);
      background: #fff;
      border: 1px solid var(--rsm-line);
      box-shadow: var(--rsm-sh-glow-violet);
      animation: rsm-spin 32s linear infinite;
    }
    .rsm-festa__badge strong {
      font-size: clamp(1.9rem, 3vw, 2.6rem);
      line-height: 1;
      font-weight: 600;
      background: linear-gradient(120deg, var(--rsm-emerald), var(--rsm-azure), var(--rsm-violet));
      -webkit-background-clip: text;
      background-clip: text;
      color: transparent;
    }
    .rsm-festa__badge span {
      font-family: var(--rsm-font-mono);
      font-size: 0.62rem;
      letter-spacing: 0.12em;
      text-transform: uppercase;
      color: var(--rsm-ink-3);
      margin-top: 4px;
      display: block;
    }
    @keyframes rsm-spin {
      to { transform: rotate(360deg); }
    }

    .rsm-festa__dates {
      display: grid;
      grid-template-columns: repeat(7, minmax(0, 1fr));
      gap: var(--rsm-s-3);
      margin: var(--rsm-s-10) 0 var(--rsm-s-12);
      padding: var(--rsm-s-6);
      border-radius: var(--rsm-r-2xl);
      background: rgba(255,255,255,0.72);
      backdrop-filter: var(--rsm-blur-md);
      border: 1px solid var(--rsm-line);
      box-shadow: var(--rsm-sh-md);
    }
    .rsm-festa__day,
    .festa-day {
      display: grid;
      place-items: center;
      gap: 4px;
      padding: 14px 8px;
      border-radius: var(--rsm-r-md);
      background: rgba(255,255,255,0.6);
      transition: background var(--rsm-dur-fast), transform var(--rsm-dur-fast);
    }
    .rsm-festa__day:hover,
    .festa-day:hover { background: #fff; transform: translateY(-2px); }
    .rsm-festa__day strong,
    .festa-day strong {
      font-family: var(--rsm-font-display);
      font-size: 1.15rem;
      font-weight: 600;
      color: var(--rsm-ink);
    }
    .rsm-festa__day span,
    .festa-day span {
      font-family: var(--rsm-font-mono);
      font-size: 0.68rem;
      letter-spacing: 0.12em;
      text-transform: uppercase;
      color: var(--rsm-ink-3);
    }
    .rsm-festa__day-divider {
      align-self: stretch;
      width: 1px;
      background: var(--rsm-line-2);
      margin: 0 auto;
    }

    .rsm-festa__grid {
      display: grid;
      grid-template-columns: minmax(0, 1.7fr) minmax(0, 1fr);
      gap: clamp(20px, 3vw, 40px);
    }
    .rsm-festa__menu-list {
      list-style: none;
      padding: 0;
      margin: var(--rsm-s-5) 0 0;
      display: grid;
      gap: var(--rsm-s-2);
    }
    .rsm-festa__menu-list li {
      display: grid;
      grid-template-columns: 56px 1fr auto;
      align-items: center;
      gap: var(--rsm-s-4);
      padding: 14px 18px;
      border-radius: var(--rsm-r-lg);
      background: rgba(255,255,255,0.6);
      border: 1px solid var(--rsm-line);
      transition: transform var(--rsm-dur-fast), background var(--rsm-dur-fast), border-color var(--rsm-dur-fast);
    }
    .rsm-festa__menu-list li:hover {
      background: #fff;
      transform: translateX(4px);
      border-color: var(--rsm-line-2);
    }
    .rsm-festa__menu-num {
      font-family: var(--rsm-font-mono);
      font-size: 0.78rem;
      letter-spacing: 0.12em;
      color: var(--rsm-ink-4);
    }
    .rsm-festa__menu-name {
      font-family: var(--rsm-font-sans);
      font-weight: 700;
      color: var(--rsm-ink);
    }
    .rsm-festa__menu-name em {
      font-style: italic;
      color: var(--rsm-violet);
    }
    .rsm-festa__menu-tag {
      font-family: var(--rsm-font-mono);
      font-size: 0.7rem;
      letter-spacing: 0.08em;
      text-transform: uppercase;
      color: var(--rsm-ink-3);
    }

    .rsm-festa__wines ul {
      list-style: none;
      padding: 0;
      margin: var(--rsm-s-5) 0 0;
      display: grid;
      gap: var(--rsm-s-2);
    }
    .rsm-festa__wines li {
      display: flex;
      justify-content: space-between;
      gap: 12px;
      padding: 12px 16px;
      border-bottom: 1px dashed var(--rsm-line-2);
    }
    .rsm-festa__wines li strong { font-weight: 800; }
    .rsm-festa__wines li span { color: var(--rsm-ink-3); }

    .rsm-festa__foot {
      display: grid;
      grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
      gap: var(--rsm-s-5);
      margin-top: var(--rsm-s-12);
    }

    /* =========================================================
       SECTION 4 — EVENTI (NEWS / Supabase)
       ========================================================= */
    .rsm-events {
      position: relative;
      background:
        radial-gradient(ellipse at 100% 0%, rgba(139,92,246,0.07) 0%, transparent 50%),
        linear-gradient(180deg, rgba(74,155,255,0.04) 0%, transparent 100%);
    }

    .news-filter-row,
    .rsm-events__filters {
      display: flex;
      flex-wrap: wrap;
      gap: 8px;
      margin-bottom: var(--rsm-s-8);
    }
    .news-filter-chip {
      display: inline-flex;
      align-items: center;
      padding: 9px 16px;
      border: 1px solid var(--rsm-line-2);
      border-radius: var(--rsm-r-pill);
      background: rgba(255,255,255,0.7);
      backdrop-filter: var(--rsm-blur-sm);
      font-family: var(--rsm-font-sans);
      font-size: var(--rsm-text-small);
      font-weight: 700;
      color: var(--rsm-ink-2);
      cursor: pointer;
      transition: all var(--rsm-dur-fast) var(--rsm-ease-out);
    }
    .news-filter-chip:hover { background: #fff; border-color: var(--rsm-line-3); transform: translateY(-1px); }
    .news-filter-chip.on {
      background: var(--rsm-ink);
      border-color: var(--rsm-ink);
      color: #fff;
    }

    .news-status {
      font-family: var(--rsm-font-mono);
      font-size: var(--rsm-text-small);
      color: var(--rsm-ink-3);
      margin: 0 0 var(--rsm-s-5);
    }

    .post-featured {
      display: grid;
      grid-template-columns: minmax(0, 1.1fr) minmax(0, 1fr);
      gap: var(--rsm-s-8);
      align-items: center;
      padding: var(--rsm-s-6);
      margin-bottom: var(--rsm-s-8);
      border: 1px solid var(--rsm-line);
      border-radius: var(--rsm-r-2xl);
      background: rgba(255,255,255,0.8);
      backdrop-filter: var(--rsm-blur-sm);
      box-shadow: var(--rsm-sh-md);
      transition: transform var(--rsm-dur-base), box-shadow var(--rsm-dur-base);
    }
    .post-featured:hover { transform: translateY(-3px); box-shadow: var(--rsm-sh-lg); }
    .post-featured.skeleton {
      min-height: 260px;
      background: linear-gradient(110deg, var(--rsm-surface) 30%, var(--rsm-mist) 50%, var(--rsm-surface) 70%);
      background-size: 200% 100%;
      animation: rsm-skeleton 1.6s ease infinite;
    }
    @keyframes rsm-skeleton { to { background-position: -200% 0; } }
    .post-featured__media,
    .news-card__media,
    .post-featured__media--photo,
    .news-card__media--photo {
      position: relative;
      aspect-ratio: 16 / 11;
      border-radius: var(--rsm-r-xl);
      overflow: hidden;
      background: var(--rsm-surface);
    }
    .post-featured__media .news-card__img,
    .news-card__media .news-card__img,
    .news-card__img {
      width: 100%;
      height: 100%;
      object-fit: cover;
      transition: transform var(--rsm-dur-slow) var(--rsm-ease-out);
    }
    .post-featured:hover .news-card__img,
    .post-card:hover .news-card__img { transform: scale(1.04); }

    .post-featured__body { display: grid; gap: var(--rsm-s-3); }
    .post-featured__title {
      font-family: var(--rsm-font-display);
      font-size: clamp(1.4rem, 2.4vw, 2rem);
      font-weight: 600;
      line-height: 1.05;
      letter-spacing: -0.03em;
      margin: 0;
    }
    .post-featured__title a { color: var(--rsm-ink); transition: color var(--rsm-dur-fast); }
    .post-featured__title a:hover { color: var(--rsm-violet); }
    .post-featured__excerpt { color: var(--rsm-ink-3); line-height: 1.6; margin: 0; }
    .post-featured__footer { display: flex; align-items: center; justify-content: space-between; gap: 12px; }
    .post-meta {
      display: flex; align-items: center; gap: var(--rsm-s-3);
      font-family: var(--rsm-font-mono);
      font-size: var(--rsm-text-tiny);
      color: var(--rsm-ink-3);
      letter-spacing: 0.04em;
      text-transform: uppercase;
    }
    .post-meta a { color: var(--rsm-azure); transition: color var(--rsm-dur-fast); }
    .post-meta a:hover { color: var(--rsm-violet); }

    .post-card-grid {
      display: grid;
      grid-template-columns: repeat(auto-fill, minmax(280px, 1fr));
      gap: var(--rsm-s-5);
    }
    .post-card {
      display: flex;
      flex-direction: column;
      border: 1px solid var(--rsm-line);
      border-radius: var(--rsm-r-2xl);
      overflow: hidden;
      background: rgba(255,255,255,0.85);
      backdrop-filter: var(--rsm-blur-sm);
      box-shadow: var(--rsm-sh-sm);
      transition: transform var(--rsm-dur-base), box-shadow var(--rsm-dur-base);
    }
    .post-card:hover { transform: translateY(-4px); box-shadow: var(--rsm-sh-lg); }
    .post-card__body { padding: var(--rsm-s-6); display: grid; gap: var(--rsm-s-3); }
    .post-card__title {
      font-family: var(--rsm-font-sans);
      font-size: 1.18rem;
      font-weight: 800;
      line-height: 1.25;
      letter-spacing: -0.02em;
      margin: 0;
    }
    .post-card__title a { color: var(--rsm-ink); transition: color var(--rsm-dur-fast); }
    .post-card__title a:hover { color: var(--rsm-violet); }
    .post-card__excerpt {
      color: var(--rsm-ink-3);
      font-size: var(--rsm-text-small);
      line-height: 1.55;
      margin: 0;
    }
    .post-card__footer {
      display: flex;
      align-items: center;
      justify-content: space-between;
      gap: 12px;
      margin-top: auto;
    }

    .btn-read {
      display: inline-flex;
      align-items: center;
      gap: 6px;
      font-family: var(--rsm-font-sans);
      font-weight: 800;
      color: var(--rsm-ink);
      transition: gap var(--rsm-dur-fast), color var(--rsm-dur-fast);
    }
    .btn-read:hover { gap: 10px; color: var(--rsm-violet); }

    .news-empty {
      padding: var(--rsm-s-10);
      border: 1px dashed var(--rsm-line-2);
      border-radius: var(--rsm-r-2xl);
      background: rgba(255,255,255,0.6);
      text-align: center;
      color: var(--rsm-ink-3);
    }
    .news-empty strong { display: block; color: var(--rsm-ink); font-family: var(--rsm-font-display); font-weight: 600; font-size: 1.4rem; margin-bottom: 8px; }
    .news-actions { display: flex; justify-content: flex-end; margin-top: var(--rsm-s-8); }

    /* =========================================================
       SECTION 5 — GALLERIA
       ========================================================= */
    .rsm-gallery {
      position: relative;
      background:
        radial-gradient(ellipse at 50% 0%, rgba(74,155,255,0.06) 0%, transparent 50%),
        linear-gradient(180deg, rgba(139,92,246,0.03) 0%, transparent 100%);
    }
    .rsm-gallery__stage {
      position: relative;
      border-radius: var(--rsm-r-3xl);
      overflow: hidden;
      box-shadow: var(--rsm-sh-xl);
      isolation: isolate;
    }
    .rsm-swiper {
      width: 100%;
      aspect-ratio: 16 / 9;
      max-height: clamp(280px, 56vh, 560px);
      min-height: 240px;
    }
    .rsm-swiper .swiper-slide {
      background-color: var(--rsm-surface);
      background-size: cover;
      background-position: center;
      overflow: hidden;
      position: relative;
    }
    .rsm-slide .slide-bg-blur {
      position: absolute;
      inset: -8%;
      background-size: cover;
      background-position: center;
      filter: blur(36px) saturate(1.1);
      opacity: 0.65;
      transform: scale(1.1);
    }
    .rsm-slide .slide-img {
      position: absolute;
      inset: 0;
      width: 100%;
      height: 100%;
      object-fit: cover;
    }
    .rsm-swiper .swiper-button-prev,
    .rsm-swiper .swiper-button-next {
      width: 48px;
      height: 48px;
      border-radius: 50%;
      background: rgba(255,255,255,0.92);
      backdrop-filter: var(--rsm-blur-sm);
      box-shadow: var(--rsm-sh-md);
      color: var(--rsm-ink);
      transition: transform var(--rsm-dur-fast), background var(--rsm-dur-fast);
    }
    .rsm-swiper .swiper-button-prev::after,
    .rsm-swiper .swiper-button-next::after {
      font-size: 16px;
      font-weight: 800;
      color: var(--rsm-ink);
    }
    .rsm-swiper .swiper-button-prev:hover,
    .rsm-swiper .swiper-button-next:hover {
      background: #fff;
      transform: scale(1.06);
    }
    .rsm-swiper .swiper-pagination-bullet {
      background: rgba(255,255,255,0.9);
      opacity: 0.6;
    }
    .rsm-swiper .swiper-pagination-bullet-active {
      background: linear-gradient(120deg, var(--rsm-emerald), var(--rsm-azure), var(--rsm-violet));
      opacity: 1;
    }

    /* gallery scroll marquee */
    .rsm-gallery__marquee {
      margin-top: var(--rsm-s-8);
      overflow: hidden;
      mask-image: linear-gradient(90deg, transparent 0, #000 10%, #000 90%, transparent 100%);
    }
    .rsm-gallery__marquee-track {
      display: flex;
      gap: var(--rsm-s-5);
      animation: rsm-marquee 36s linear infinite;
      white-space: nowrap;
    }
    .rsm-gallery__marquee-track > span {
      font-family: var(--rsm-font-display);
      font-size: clamp(1.6rem, 3vw, 2.8rem);
      font-weight: 600;
      letter-spacing: -0.02em;
      color: var(--rsm-ink-4);
    }
    .rsm-gallery__marquee-track > span em {
      background: linear-gradient(120deg, var(--rsm-emerald), var(--rsm-azure), var(--rsm-violet));
      -webkit-background-clip: text;
      background-clip: text;
      color: transparent;
      font-style: italic;
    }
    @keyframes rsm-marquee {
      0% { transform: translateX(0); }
      100% { transform: translateX(-50%); }
    }

    /* =========================================================
       SECTION 6 — TERRITORIO (Atlante + Valli + Esperienze)
       ========================================================= */
    .rsm-territory {
      position: relative;
      background:
        radial-gradient(ellipse at 0% 100%, rgba(31,191,125,0.08) 0%, transparent 55%),
        radial-gradient(ellipse at 100% 0%, rgba(139,92,246,0.05) 0%, transparent 50%),
        linear-gradient(180deg, rgba(31,191,125,0.04) 0%, transparent 100%);
    }

    .rsm-atlas-grid {
      display: grid;
      grid-template-columns: minmax(0, 1.1fr) minmax(0, 1fr);
      gap: clamp(20px, 3vw, 40px);
      margin-bottom: var(--rsm-s-16);
    }
    .rsm-atlas-photo {
      position: relative;
      width: 100%;
      aspect-ratio: 4 / 5;
      max-height: clamp(420px, 60vh, 640px);
      border-radius: var(--rsm-r-3xl);
      overflow: hidden;
      box-shadow: var(--rsm-sh-xl);
    }
    .rsm-swiper--atlas { width: 100%; height: 100%; aspect-ratio: auto; max-height: none; }
    .rsm-swiper--atlas .swiper-slide { aspect-ratio: auto; height: 100%; }
    .rsm-swiper--atlas .slide-img { object-position: center; }

    .rsm-atlas-copy {
      align-self: center;
      display: grid;
      gap: var(--rsm-s-5);
    }
    .rsm-fact-grid {
      display: grid;
      grid-template-columns: repeat(2, 1fr);
      gap: var(--rsm-s-3);
      margin-top: var(--rsm-s-5);
    }
    .fact, .rsm-fact {
      padding: var(--rsm-s-5);
      border: 1px solid var(--rsm-line);
      border-radius: var(--rsm-r-xl);
      background: rgba(255,255,255,0.78);
      backdrop-filter: var(--rsm-blur-sm);
    }
    .fact strong, .rsm-fact strong {
      font-family: var(--rsm-font-display);
      font-size: clamp(1.6rem, 2.4vw, 2.2rem);
      font-weight: 600;
      letter-spacing: -0.04em;
      line-height: 1;
      display: block;
      color: var(--rsm-ink);
    }
    .fact span, .rsm-fact span {
      display: block;
      margin-top: 8px;
      font-size: var(--rsm-text-small);
      color: var(--rsm-ink-3);
      line-height: 1.5;
    }
    .fact:nth-child(1) strong { color: var(--rsm-emerald); }
    .fact:nth-child(2) strong { color: var(--rsm-azure); }
    .fact:nth-child(3) strong { color: var(--rsm-violet); }

    /* Esperienze cards (3) */
    .rsm-xp-grid {
      display: grid;
      grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
      gap: var(--rsm-s-5);
      margin-bottom: var(--rsm-s-16);
    }
    .stack-card, .rsm-xp-card {
      position: relative;
      display: grid;
      grid-template-rows: auto 1fr;
      border: 1px solid var(--rsm-line);
      border-radius: var(--rsm-r-2xl);
      overflow: hidden;
      background: rgba(255,255,255,0.85);
      backdrop-filter: var(--rsm-blur-sm);
      box-shadow: var(--rsm-sh-md);
      transition: transform var(--rsm-dur-base), box-shadow var(--rsm-dur-base);
    }
    .stack-card:hover, .rsm-xp-card:hover {
      transform: translateY(-4px);
      box-shadow: var(--rsm-sh-lg);
    }
    .photo-slot, .rsm-xp-photo {
      position: relative;
      aspect-ratio: 4 / 3;
      background-image: var(--photo);
      background-size: cover;
      background-position: center;
      overflow: hidden;
    }
    .photo-slot::after, .rsm-xp-photo::after {
      content: "";
      position: absolute;
      inset: 0;
      background: linear-gradient(180deg, transparent 60%, rgba(11,13,20,0.18) 100%);
    }
    .stack-card-inner, .rsm-xp-card-inner {
      display: contents;
    }
    .stack-card-copy, .rsm-xp-body {
      padding: var(--rsm-s-6);
      display: grid;
      gap: var(--rsm-s-3);
      align-content: start;
    }
    .stack-card-copy h3, .rsm-xp-body h3 {
      font-family: var(--rsm-font-sans);
      font-size: 1.3rem;
      font-weight: 800;
      letter-spacing: -0.02em;
      margin: 0;
    }
    .stack-card-copy p, .rsm-xp-body p {
      color: var(--rsm-ink-3);
      font-size: var(--rsm-text-small);
      line-height: 1.55;
      margin: 0;
    }
    .stack-meta, .rsm-xp-meta {
      display: flex;
      flex-wrap: wrap;
      gap: 6px;
      margin-top: 4px;
    }
    .rsm-xp-card.is-emerald .photo-slot::before { background: linear-gradient(180deg, rgba(31,191,125,0.20), transparent 50%); content: ""; position:absolute; inset:0; mix-blend-mode: multiply; }
    .rsm-xp-card.is-azure .photo-slot::before   { background: linear-gradient(180deg, rgba(74,155,255,0.20), transparent 50%); content: ""; position:absolute; inset:0; mix-blend-mode: multiply; }
    .rsm-xp-card.is-violet .photo-slot::before  { background: linear-gradient(180deg, rgba(139,92,246,0.20), transparent 50%); content: ""; position:absolute; inset:0; mix-blend-mode: multiply; }

    /* Valli narrative */
    .rsm-valli {
      padding: clamp(40px, 5vw, 72px) 0;
      margin-top: var(--rsm-s-12);
      background:
        linear-gradient(135deg, rgba(255,255,255,0.96), rgba(255,255,255,0.88));
    }
    .rsm-valli__drop {
      font-family: var(--rsm-font-display);
      font-size: clamp(1.8rem, 3.5vw, 3rem);
      font-weight: 600;
      line-height: 1.05;
      letter-spacing: -0.035em;
      margin: 0;
      max-width: 22ch;
    }
    .rsm-valli__drop em {
    }
    .rsm-valli__row {
      display: grid;
      grid-template-columns: repeat(auto-fit, minmax(160px, 1fr));
      gap: var(--rsm-s-5);
      margin: var(--rsm-s-10) 0;
      padding: var(--rsm-s-7) 0;
      border-top: 1px solid var(--rsm-line);
      border-bottom: 1px solid var(--rsm-line);
    }
    .rsm-valli__fact strong {
      font-family: var(--rsm-font-display);
      font-size: 2rem;
      font-weight: 600;
      letter-spacing: -0.04em;
      display: block;
    }
    .rsm-valli__fact span {
      font-family: var(--rsm-font-mono);
      font-size: 0.7rem;
      letter-spacing: 0.12em;
      text-transform: uppercase;
      color: var(--rsm-ink-3);
    }
    .rsm-valli__summary {
      display: grid;
      grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
      gap: var(--rsm-s-6);
      margin: var(--rsm-s-7) 0;
    }
    .rsm-valli__summary article p { color: var(--rsm-ink-3); line-height: 1.66; }
    .rsm-valli__summary article h3 {
      font-family: var(--rsm-font-display);
      font-weight: 600;
      font-size: 1.5rem;
      letter-spacing: -0.02em;
      margin: 0 0 12px;
    }
    .kw {
      position: relative;
      cursor: help;
      background: linear-gradient(180deg, transparent 60%, rgba(139,92,246,0.18) 60%);
      padding: 0 2px;
      border-radius: 2px;
    }

    /* =========================================================
       SECTION 7 — CONTATTI
       ========================================================= */
    .rsm-contact {
      position: relative;
      background:
        radial-gradient(ellipse at 50% 100%, rgba(74,155,255,0.06) 0%, transparent 55%);
    }
    .rsm-contact__grid {
      display: grid;
      grid-template-columns: minmax(0, 1.1fr) minmax(0, 1fr);
      gap: clamp(20px, 3vw, 40px);
      align-items: start;
    }
    .rsm-contact__map {
      border-radius: var(--rsm-r-3xl);
      overflow: hidden;
      box-shadow: var(--rsm-sh-xl);
      aspect-ratio: 4 / 3;
      max-height: clamp(320px, 50vh, 480px);
      background: var(--rsm-surface);
      margin: 0;
    }
    .rsm-contact__map .swiper,
    .rsm-contact__map .rsm-swiper {
      width: 100%;
      height: 100%;
      aspect-ratio: auto;
      max-height: none;
      min-height: 0;
    }
    .rsm-contact__map .swiper-slide { height: 100%; }
    .rsm-contact__map .swiper-slide video,
    .rsm-contact__map .swiper-slide img {
      width: 100%; height: 100%; object-fit: cover;
    }
    .rsm-contact__card {
      padding: var(--rsm-s-10);
      display: grid;
      gap: var(--rsm-s-6);
      align-content: space-between;
      border-radius: var(--rsm-r-3xl);
      background: rgba(255,255,255,0.92);
      border: 1px solid var(--rsm-line);
      box-shadow: var(--rsm-sh-md);
      backdrop-filter: var(--rsm-blur-sm);
    }
    .rsm-contact__lines { display: grid; gap: var(--rsm-s-3); }
    .rsm-contact__line {
      display: grid;
      grid-template-columns: 100px 1fr;
      gap: var(--rsm-s-4);
      padding: 12px 0;
      border-bottom: 1px solid var(--rsm-line);
    }
    .rsm-contact__line:last-child { border-bottom: none; }
    .rsm-contact__line strong {
      font-family: var(--rsm-font-mono);
      font-size: 0.72rem;
      letter-spacing: 0.12em;
      text-transform: uppercase;
      color: var(--rsm-ink-3);
      align-self: center;
    }
    .rsm-contact__line a, .rsm-contact__line span {
      color: var(--rsm-ink);
      font-weight: 700;
    }
    .rsm-contact__line a { transition: color var(--rsm-dur-fast); }
    .rsm-contact__line a:hover { color: var(--rsm-violet); }

    /* Newsletter */
    .rsm-newsletter {
      margin-top: var(--rsm-s-12);
      padding: clamp(36px, 5vw, 72px) 0;
      background:
        radial-gradient(circle at 80% 0, rgba(139,92,246,0.18), transparent 50%),
        radial-gradient(circle at 0 100%, rgba(31,191,125,0.18), transparent 50%),
        linear-gradient(135deg, var(--rsm-ink), #16182a);
      color: #fff;
      overflow: hidden;
      position: relative;
    }
    .rsm-newsletter__grid {
      display: grid;
      grid-template-columns: minmax(0, 1.2fr) minmax(0, 1fr);
      gap: clamp(20px, 3vw, 48px);
      align-items: end;
    }
    .rsm-newsletter h2 {
      font-family: var(--rsm-font-display);
      font-size: clamp(1.8rem, 3.4vw, 2.8rem);
      font-weight: 600;
      letter-spacing: -0.03em;
      line-height: 1.05;
      margin: var(--rsm-s-3) 0 var(--rsm-s-4);
    }
    .rsm-newsletter h2 em {
      font-style: italic;
      background: linear-gradient(120deg, var(--rsm-emerald-2), var(--rsm-azure-2), var(--rsm-violet-2));
      -webkit-background-clip: text;
      background-clip: text;
      color: transparent;
    }
    .rsm-newsletter p { color: rgba(255,255,255,0.7); margin: 0; line-height: 1.6; }
    .rsm-newsletter .rsm-kicker { color: rgba(255,255,255,0.7); }
    .rsm-newsletter .rsm-input {
      background: rgba(255,255,255,0.14);
      border-color: rgba(255,255,255,0.18);
      color: #fff;
    }
    .rsm-newsletter .rsm-input::placeholder { color: rgba(255,255,255,0.5); }
    .rsm-newsletter .rsm-input:focus {
      background: rgba(255,255,255,0.22);
      border-color: var(--rsm-violet-2);
      box-shadow: 0 0 0 4px rgba(139, 92, 246, 0.28);
    }
    .rsm-newsletter .rsm-form-status { color: rgba(255,255,255,0.7); }
    .rsm-newsletter .privacy-note {
      margin-top: 14px;
      color: rgba(255,255,255,0.55);
      font-size: 0.85rem;
    }
    .rsm-newsletter .privacy-note a { text-decoration: underline; }

    /* =========================================================
       BACK TO TOP
       ========================================================= */
    .back-to-top {
      position: fixed;
      bottom: 26px;
      right: 26px;
      z-index: var(--rsm-z-sticky);
      width: 52px;
      height: 52px;
      border-radius: 50%;
      display: grid;
      place-items: center;
      background: var(--rsm-ink);
      color: #fff;
      box-shadow: var(--rsm-sh-glow-violet);
      font-size: 22px;
      transition: transform var(--rsm-dur-base), background var(--rsm-dur-base);
    }
    .back-to-top:hover { transform: translateY(-3px) scale(1.05); }

    /* =========================================================
       REVEAL ANIMATIONS
       ========================================================= */
    [data-reveal] {
      opacity: 0;
      transform: translateY(28px);
      transition: opacity 760ms var(--rsm-ease-out),
                  transform 760ms var(--rsm-ease-out);
      transition-delay: var(--delay, 0ms);
    }
    [data-reveal].is-visible {
      opacity: 1;
      transform: none;
    }

    /* Splitting text */
    .char, .word { display: inline-block; will-change: transform, opacity; }

    /* Tippy custom theme */
    .tippy-box[data-theme~="rsm"] {
      background: var(--rsm-ink);
      color: #fff;
      border-radius: var(--rsm-r-md);
      box-shadow: var(--rsm-sh-lg);
      font-family: var(--rsm-font-sans);
    }
    .tippy-box[data-theme~="rsm"] .tippy-arrow { color: var(--rsm-ink); }
    .tippy-box[data-theme~="rsm"] .kw-pop-title {
      display: block;
      font-family: var(--rsm-font-mono);
      font-size: 0.72rem;
      letter-spacing: 0.12em;
      text-transform: uppercase;
      color: var(--rsm-violet-2);
      margin-bottom: 6px;
    }
    .tippy-box[data-theme~="rsm"] .kw-pop-wiki { font-size: 0.92rem; line-height: 1.5; margin: 0; }
    .tippy-box[data-theme~="rsm"] .kw-pop-claude {
      margin: 8px 0 0;
      padding-top: 8px;
      border-top: 1px solid rgba(255,255,255,0.12);
      font-size: 0.86rem;
      line-height: 1.5;
      color: rgba(255,255,255,0.85);
    }
    .tippy-box[data-theme~="rsm"] .kw-pop-label {
      display: inline-block;
      margin-right: 6px;
      font-family: var(--rsm-font-mono);
      font-size: 0.66rem;
      letter-spacing: 0.12em;
      text-transform: uppercase;
      color: var(--rsm-emerald-2);
    }

    /* =========================================================
       RESPONSIVE
       ========================================================= */
    @media (max-width: 980px) {
      .rsm-hero__layout {
        grid-template-columns: 1fr;
      }
      .rsm-compass__inner {
        grid-template-columns: 1fr;
      }
      .rsm-festa__head {
        grid-template-columns: 1fr;
      }
      .rsm-festa__grid {
        grid-template-columns: 1fr;
      }
      .rsm-atlas-grid {
        grid-template-columns: 1fr;
      }
      .rsm-contact__grid {
        grid-template-columns: 1fr;
      }
      .rsm-newsletter__grid {
        grid-template-columns: 1fr;
      }
      .post-featured {
        grid-template-columns: 1fr;
      }
      .rsm-festa__dates {
        grid-template-columns: repeat(3, 1fr);
      }
      .rsm-festa__day-divider { display: none; }
    }

    @media (max-width: 600px) {
      .rsm-compass__nav { grid-template-columns: 1fr; }
      .rsm-festa__dates { grid-template-columns: repeat(2, 1fr); }
      .rsm-contact__line { grid-template-columns: 1fr; }
      .rsm-contact__line strong { margin-bottom: 4px; }
      .rsm-festa__menu-list li {
        grid-template-columns: 40px 1fr;
        grid-template-rows: auto auto;
      }
      .rsm-festa__menu-tag { grid-column: 2; }
    }

    @media (prefers-reduced-motion: reduce) {
      *, *::before, *::after {
        animation-duration: 0.001ms !important;
        animation-iteration-count: 1 !important;
        transition-duration: 0.001ms !important;
        scroll-behavior: auto !important;
      }
      [data-reveal] { opacity: 1; transform: none; }
      .rsm-aurora__blob { animation: none; }
    }

    /* =========================================================
       DARK MODE compat (theme.css gestisce il resto)
       ========================================================= */
    html[data-theme="scuro"] {
      --rsm-bg: #0a0c14;
      --rsm-paper: #11141f;
      --rsm-surface: #161a27;
      --rsm-surface-2: #1e2230;
      --rsm-mist: #232636;
      --rsm-ink:    #ecedf3;
      --rsm-ink-2:  #c8cbd7;
      --rsm-ink-3:  #8d92a3;
      --rsm-ink-4:  #5d6273;
      --rsm-ink-5:  #3c4055;
      --rsm-line:   rgba(236, 237, 243, 0.08);
      --rsm-line-2: rgba(236, 237, 243, 0.16);
      --rsm-line-3: rgba(236, 237, 243, 0.28);
      --rsm-glass: rgba(20, 22, 32, 0.62);
      --rsm-glass-strong: rgba(20, 22, 32, 0.88);
      color-scheme: dark;
    }
    html[data-theme="scuro"] body { background: var(--rsm-bg); color: var(--rsm-ink); }
    html[data-theme="scuro"] .rsm-aurora::after {
      background:
        radial-gradient(circle at 50% 50%, rgba(0,0,0,0) 0%, rgba(10,12,20,0.65) 90%),
        linear-gradient(180deg, rgba(10,12,20,0.30), rgba(10,12,20,0.10));
    }
    html[data-theme="scuro"] .rsm-btn { color: var(--rsm-ink); background: var(--rsm-surface-2); }
    html[data-theme="scuro"] .rsm-btn-icon { background: var(--rsm-bg); color: var(--rsm-ink); }
    html[data-theme="scuro"] .rsm-btn--ghost { background: rgba(20,22,32,0.6); color: var(--rsm-ink); }
    html[data-theme="scuro"] .rsm-btn--ghost .rsm-btn-icon { background: var(--rsm-ink); color: var(--rsm-bg); }
    html[data-theme="scuro"] .rsm-compass { background: var(--rsm-surface); }
    html[data-theme="scuro"] .rsm-valli { background: var(--rsm-surface); }
    /* nav-island dark mode: gestito da /partials/topbar.css */
  </style>

  <!-- ════════════════════════════════════════════════════════════
       PREMIUM EDITORIAL LAYER v1
       Aurora mouse-tracking · Grain · Stagger · Marquee · Hover micro
       Coerente con la grammatica del footbar.
       ════════════════════════════════════════════════════════════ -->
  <style>
    .rsm-hero,
    .rsm-banner,
    .rsm-compass,
    .rsm-hero__metrics,
    .rsm-hero__main,
    .rsm-banner__content,
    .rsm-compass__inner {
      --rsm-edit-ease: cubic-bezier(0.16, 1, 0.3, 1);
      --rsm-edit-ease-spring: cubic-bezier(0.34, 1.4, 0.64, 1);
      --rsm-edit-emerald: rgba(134, 255, 211, 0.96);
      --rsm-edit-azure: rgba(146, 196, 255, 0.96);
      --rsm-edit-violet: rgba(207, 156, 255, 0.96);
      --rsm-edit-grad: linear-gradient(95deg, var(--rsm-edit-emerald) 0%, var(--rsm-edit-azure) 48%, var(--rsm-edit-violet) 100%);
    }

    /* ═════════════════ GLOBAL GRAIN NOISE OVERLAY ═════════════════ */
    body[data-page="home"]::after {
      content: "";
      position: fixed;
      inset: 0;
      pointer-events: none;
      z-index: 999;
      opacity: 0.045;
      mix-blend-mode: overlay;
      background-image: radial-gradient(circle at 1px 1px, rgba(255,255,255,0.55) 0.6px, transparent 1.2px);
      background-size: 3px 3px;
    }

    /* ═════════════════ BANNER — premium editorial overlay ═════════ */
    .rsm-banner {
      isolation: isolate;
    }

    .rsm-banner__overlay {
      background:
        radial-gradient(ellipse at 14% 6%, rgba(102, 255, 214, 0.34), transparent 38%),
        radial-gradient(ellipse at 84% 10%, rgba(126, 188, 255, 0.30), transparent 42%),
        radial-gradient(ellipse at 50% 100%, rgba(196, 122, 255, 0.34), transparent 48%),
        linear-gradient(160deg, rgba(7, 28, 44, 0.62) 0%, rgba(9, 35, 84, 0.52) 50%, rgba(45, 18, 80, 0.62) 100%) !important;
    }

    .rsm-banner__aurora {
      position: absolute;
      inset: 0;
      z-index: 1;
      pointer-events: none;
      overflow: hidden;
      --mx: 50%;
      --my: 50%;
    }

    .rsm-banner__aurora::after {
      content: "";
      position: absolute;
      inset: 0;
      background: radial-gradient(circle at var(--mx) var(--my), rgba(146, 196, 255, 0.28), transparent 38%);
      opacity: 0;
      transition: opacity 600ms ease;
    }

    .rsm-banner:hover .rsm-banner__aurora::after { opacity: 1; }

    .rsm-banner__blob {
      position: absolute;
      width: 42vmax;
      height: 42vmax;
      border-radius: 50%;
      filter: blur(120px);
      opacity: 0.28;
      will-change: transform;
      animation: rsm-edit-float 32s ease-in-out infinite;
    }

    .rsm-banner__blob--a {
      top: -20vmax;
      left: -14vmax;
      background: radial-gradient(circle, rgba(46, 224, 170, 0.55), transparent 65%);
    }

    .rsm-banner__blob--b {
      top: -16vmax;
      right: -16vmax;
      background: radial-gradient(circle, rgba(86, 156, 255, 0.55), transparent 65%);
      animation-delay: -10s;
      animation-duration: 36s;
    }

    .rsm-banner__blob--c {
      bottom: -22vmax;
      left: 38%;
      background: radial-gradient(circle, rgba(167, 119, 255, 0.55), transparent 65%);
      animation-delay: -18s;
      animation-duration: 40s;
    }

    @keyframes rsm-edit-float {
      0%, 100% { transform: translate3d(0, 0, 0) scale(1); }
      33% { transform: translate3d(8vmax, -4vmax, 0) scale(1.08); }
      66% { transform: translate3d(-6vmax, 6vmax, 0) scale(0.94); }
    }

    .rsm-banner__grain {
      position: absolute;
      inset: 0;
      z-index: 1;
      pointer-events: none;
      opacity: 0.10;
      mix-blend-mode: overlay;
      background-image: radial-gradient(circle at 1px 1px, rgba(255,255,255,0.55) 0.6px, transparent 1.2px);
      background-size: 3px 3px;
    }

    /* Banner content — editorial */
    .rsm-banner__content {
      z-index: 2;
    }

    .rsm-banner__kicker {
      display: inline-flex;
      align-items: center;
      gap: 10px;
      margin: 0 auto;
      font-family: 'Space Grotesk', system-ui, sans-serif !important;
      font-size: 0.74rem !important;
      font-weight: 500 !important;
      letter-spacing: 0.22em !important;
      text-transform: uppercase !important;
      color: rgba(220, 234, 255, 0.78) !important;
    }

    .rsm-banner__kicker .festa-pulse {
      width: 7px;
      height: 7px;
      background: var(--rsm-edit-emerald);
      box-shadow: 0 0 0 4px rgba(134, 255, 211, 0.18);
      animation: rsm-edit-pulse 2.6s ease-in-out infinite;
    }

    @keyframes rsm-edit-pulse {
      0%, 100% { box-shadow: 0 0 0 4px rgba(134, 255, 211, 0.18); }
      50% { box-shadow: 0 0 0 9px rgba(134, 255, 211, 0); }
    }

    .rsm-banner__title {
      font-family: 'Syne', 'Cabinet Grotesk', system-ui, sans-serif !important;
      font-weight: 800 !important;
      letter-spacing: -0.055em !important;
      color: #fff !important;
      text-shadow: 0 8px 60px rgba(0, 0, 0, 0.45) !important;
      line-height: 0.92 !important;
    }

    .rsm-banner__title em,
    .rsm-banner__title .rsm-edit-em {
      font-style: italic;
      font-family: 'Fraunces', Georgia, serif !important;
      font-weight: 500 !important;
      background: var(--rsm-edit-grad);
      background-size: 220% 100%;
      -webkit-background-clip: text;
      background-clip: text;
      -webkit-text-fill-color: transparent;
      color: transparent !important;
      animation: rsm-edit-grad-flow 9s ease-in-out infinite alternate;
    }

    @keyframes rsm-edit-grad-flow {
      0% { background-position: 0% 50%; }
      100% { background-position: 100% 50%; }
    }

    .rsm-edit-line {
      display: block;
      opacity: 0;
      transform: translateY(28px);
      transition:
        opacity 900ms var(--rsm-edit-ease),
        transform 900ms var(--rsm-edit-ease);
    }

    .rsm-edit-line.is-in { opacity: 1; transform: translateY(0); }
    .rsm-edit-line:nth-child(1).is-in { transition-delay: 80ms; }
    .rsm-edit-line:nth-child(2).is-in { transition-delay: 200ms; }
    .rsm-edit-line:nth-child(3).is-in { transition-delay: 320ms; }
    .rsm-edit-line:nth-child(4).is-in { transition-delay: 440ms; }

    .rsm-banner__sub {
      font-family: 'Space Grotesk', system-ui, sans-serif !important;
      font-size: clamp(1rem, 1.6vw, 1.18rem) !important;
      font-weight: 400 !important;
      color: rgba(232, 244, 255, 0.86) !important;
      line-height: 1.55 !important;
      letter-spacing: 0 !important;
    }

    /* Banner CTA — premium gradient pill */
    .rsm-banner__cta.rsm-btn--brand {
      background: var(--rsm-edit-grad) !important;
      background-size: 220% 100% !important;
      background-position: 0% 50% !important;
      color: #061a2a !important;
      font-family: 'Syne', system-ui, sans-serif !important;
      font-weight: 700 !important;
      letter-spacing: -0.005em !important;
      box-shadow: 0 14px 36px rgba(8, 28, 60, 0.42) !important;
      transition:
        background-position 480ms var(--rsm-edit-ease),
        transform 320ms var(--rsm-edit-ease-spring),
        box-shadow 320ms var(--rsm-edit-ease) !important;
    }
    .rsm-banner__cta.rsm-btn--brand:hover {
      background-position: 100% 50% !important;
      transform: translateY(-2px) !important;
      box-shadow: 0 22px 50px rgba(8, 28, 60, 0.5) !important;
    }
    .rsm-banner__cta .rsm-btn-icon {
      background: rgba(6, 26, 42, 0.95) !important;
      color: #e8f2ff !important;
      transition: transform 320ms var(--rsm-edit-ease-spring) !important;
    }
    .rsm-banner__cta:hover .rsm-btn-icon {
      transform: translate(0, 4px) !important;
    }

    /* Scroll hint — solo linea, senza testo */
    .rsm-banner__scroll-line {
      width: 1px !important;
      height: 56px !important;
      background: linear-gradient(180deg, transparent, rgba(255, 255, 255, 0.86)) !important;
    }

    /* CTA banner: niente underline */
    .rsm-banner__cta,
    .rsm-banner__cta:hover,
    .rsm-banner__cta:focus,
    .rsm-banner__cta:active {
      text-decoration: none !important;
    }
    .rsm-banner__cta span {
      text-decoration: none !important;
    }

    /* ═════════════════ HERO (intro) — editorial premium ═════════════════ */
    .rsm-hero {
      position: relative;
      isolation: isolate;
    }

    .rsm-hero__accent {
      width: 130%;
      height: 130%;
      top: -15%;
      left: -15%;
      background:
        radial-gradient(ellipse at 18% 22%, rgba(46, 224, 170, 0.18), transparent 48%),
        radial-gradient(ellipse at 82% 78%, rgba(167, 119, 255, 0.18), transparent 48%),
        radial-gradient(ellipse at 50% 50%, rgba(86, 156, 255, 0.10), transparent 60%) !important;
      animation: rsm-edit-float 32s ease-in-out infinite !important;
    }

    .rsm-hero .rsm-kicker {
      display: inline-flex;
      align-items: center;
      gap: 10px;
      font-family: 'Space Grotesk', system-ui, sans-serif !important;
      font-size: 0.72rem !important;
      font-weight: 500 !important;
      letter-spacing: 0.22em !important;
      text-transform: uppercase !important;
      color: var(--rsm-ink-3) !important;
    }
    .rsm-hero .rsm-kicker::before { display: none !important; }

    .rsm-hero .rsm-kicker .festa-pulse {
      width: 7px;
      height: 7px;
      background: var(--rsm-emerald);
      box-shadow: 0 0 0 4px rgba(31, 191, 125, 0.18);
      animation: rsm-edit-pulse 2.6s ease-in-out infinite;
    }

    .rsm-hero__title {
      font-family: 'Syne', 'Cabinet Grotesk', system-ui, sans-serif !important;
      font-weight: 700 !important;
      letter-spacing: -0.05em !important;
      line-height: 0.94 !important;
    }

    .rsm-hero__title em,
    .rsm-hero__title .rsm-edit-em {
      font-style: italic;
      font-family: 'Fraunces', Georgia, serif !important;
      font-weight: 500 !important;
      background: linear-gradient(95deg, var(--rsm-emerald) 0%, var(--rsm-azure) 50%, var(--rsm-violet) 100%);
      background-size: 220% 100%;
      -webkit-background-clip: text;
      background-clip: text;
      -webkit-text-fill-color: transparent;
      color: transparent !important;
      animation: rsm-edit-grad-flow 9s ease-in-out infinite alternate;
    }

    .rsm-hero__main .rsm-lead {
      font-family: 'Space Grotesk', system-ui, sans-serif;
      font-size: clamp(1.05rem, 1.2vw, 1.18rem);
      line-height: 1.62;
    }

    /* Hero metrics — premium stagger reveal */
    .rsm-hero__metrics {
      gap: clamp(20px, 2.5vw, 32px) !important;
    }
    .rsm-metric {
      position: relative;
      padding: 14px 0 0;
      border-top: 0;
      opacity: 0;
      transform: translateY(20px);
      transition:
        opacity 700ms var(--rsm-edit-ease),
        transform 700ms var(--rsm-edit-ease);
    }
    .rsm-metric::before {
      content: "";
      position: absolute;
      top: 0;
      left: 0;
      width: 36px;
      height: 2px;
      border-radius: 2px;
      background: var(--rsm-edit-grad);
      transform: scaleX(0);
      transform-origin: left;
      transition: transform 720ms var(--rsm-edit-ease) 220ms;
    }
    .rsm-hero__main.is-visible .rsm-metric { opacity: 1; transform: translateY(0); }
    .rsm-hero__main.is-visible .rsm-metric::before { transform: scaleX(1); }
    .rsm-hero__main.is-visible .rsm-metric:nth-child(1) { transition-delay: 360ms; }
    .rsm-hero__main.is-visible .rsm-metric:nth-child(2) { transition-delay: 440ms; }
    .rsm-hero__main.is-visible .rsm-metric:nth-child(3) { transition-delay: 520ms; }
    .rsm-hero__main.is-visible .rsm-metric:nth-child(4) { transition-delay: 600ms; }
    .rsm-hero__main.is-visible .rsm-metric:nth-child(1)::before { transition-delay: 540ms; }
    .rsm-hero__main.is-visible .rsm-metric:nth-child(2)::before { transition-delay: 620ms; }
    .rsm-hero__main.is-visible .rsm-metric:nth-child(3)::before { transition-delay: 700ms; }
    .rsm-hero__main.is-visible .rsm-metric:nth-child(4)::before { transition-delay: 780ms; }

    .rsm-metric strong {
      font-family: 'Syne', system-ui, sans-serif !important;
      font-weight: 800 !important;
      font-size: clamp(2.2rem, 3.4vw, 3.2rem) !important;
      letter-spacing: -0.05em !important;
      line-height: 0.96 !important;
    }
    .rsm-metric span {
      font-family: 'Space Grotesk', system-ui, sans-serif !important;
      font-size: 0.7rem !important;
      font-weight: 500 !important;
      letter-spacing: 0.18em !important;
      color: var(--rsm-ink-4) !important;
    }

    /* Hero media (Swiper GIF) — refined ring */
    .rsm-hero__media {
      box-shadow:
        0 32px 80px rgba(11, 13, 20, 0.16),
        0 12px 24px rgba(11, 13, 20, 0.08),
        inset 0 0 0 1px rgba(255, 255, 255, 0.5);
    }

    /* ═════════════════ MARQUEE BAND ═════════════════ */
    .rsm-edit-band {
      position: relative;
      width: 100vw;
      margin-left: calc(-50vw + 50%);
      padding: clamp(16px, 2vw, 22px) 0;
      border-top: 1px solid rgba(11, 13, 20, 0.08);
      border-bottom: 1px solid rgba(11, 13, 20, 0.08);
      overflow: hidden;
      background: linear-gradient(90deg, rgba(255, 255, 255, 0) 0%, rgba(247, 250, 254, 0.68) 50%, rgba(255, 255, 255, 0) 100%);
      -webkit-mask-image: linear-gradient(90deg, transparent, #000 8%, #000 92%, transparent);
      mask-image: linear-gradient(90deg, transparent, #000 8%, #000 92%, transparent);
    }
    .rsm-edit-band-track {
      display: flex;
      gap: 0;
      width: max-content;
      animation: rsm-edit-marquee 38s linear infinite;
    }
    .rsm-edit-band:hover .rsm-edit-band-track { animation-play-state: paused; }
    .rsm-edit-band-group {
      display: inline-flex;
      align-items: center;
      gap: 28px;
      padding-right: 28px;
      font-family: 'Fraunces', Georgia, serif;
      font-style: italic;
      font-weight: 400;
      font-size: clamp(1.1rem, 1.7vw, 1.6rem);
      letter-spacing: -0.02em;
      white-space: nowrap;
      color: rgba(11, 13, 20, 0.62);
    }
    .rsm-edit-band-item { flex-shrink: 0; }
    .rsm-edit-band-dot {
      display: inline-block;
      width: 6px;
      height: 6px;
      border-radius: 50%;
      background: linear-gradient(95deg, var(--rsm-emerald), var(--rsm-azure), var(--rsm-violet));
      flex-shrink: 0;
      opacity: 0.85;
    }
    @keyframes rsm-edit-marquee {
      from { transform: translate3d(0, 0, 0); }
      to { transform: translate3d(-50%, 0, 0); }
    }

    html[data-theme="scuro"] .rsm-edit-band {
      background: linear-gradient(90deg, transparent 0%, rgba(20, 32, 48, 0.4) 50%, transparent 100%);
      border-color: rgba(150, 190, 175, 0.13);
    }
    html[data-theme="scuro"] .rsm-edit-band-group { color: rgba(214, 232, 255, 0.72); }

    /* ═════════════════ COMPASS — dark editorial glass ═════════════════ */
    .rsm-compass {
      position: relative;
      isolation: isolate;
      background:
        radial-gradient(ellipse at 14% -10%, rgba(102, 255, 214, 0.16), transparent 38%),
        radial-gradient(ellipse at 80% -8%, rgba(126, 188, 255, 0.20), transparent 42%),
        radial-gradient(ellipse at 100% 100%, rgba(196, 122, 255, 0.18), transparent 48%),
        linear-gradient(160deg, #051c2c 0%, #082649 50%, #2a1147 100%) !important;
      color: rgba(220, 234, 255, 0.78);
      width: 100vw;
      margin-left: calc(-50vw + 50%);
      overflow: hidden;
      padding: clamp(60px, 8vw, 100px) 0 !important;
      border-top: 1px solid rgba(160, 208, 255, 0.16);
      border-bottom: 1px solid rgba(160, 208, 255, 0.16);
    }

    .rsm-compass::before {
      content: "";
      position: absolute;
      inset: 0;
      pointer-events: none;
      z-index: 1;
      opacity: 0.10;
      mix-blend-mode: overlay;
      background-image: radial-gradient(circle at 1px 1px, rgba(255,255,255,0.55) 0.6px, transparent 1.2px);
      background-size: 3px 3px;
    }

    .rsm-compass__hairline {
      position: absolute;
      inset: 0 0 auto 0;
      height: 1px;
      background: linear-gradient(90deg, transparent, rgba(180, 220, 255, 0.55) 30%, rgba(220, 200, 255, 0.55) 70%, transparent);
      z-index: 3;
      pointer-events: none;
    }

    .rsm-compass__aurora {
      position: absolute;
      inset: -10%;
      z-index: 0;
      pointer-events: none;
      overflow: hidden;
      --mx: 50%;
      --my: 50%;
    }
    .rsm-compass__aurora::after {
      content: "";
      position: absolute;
      inset: 0;
      background: radial-gradient(circle at var(--mx) var(--my), rgba(146, 196, 255, 0.18), transparent 38%);
      opacity: 0;
      transition: opacity 600ms ease;
    }
    .rsm-compass:hover .rsm-compass__aurora::after { opacity: 1; }
    .rsm-compass__blob {
      position: absolute;
      width: 38vmax;
      height: 38vmax;
      border-radius: 50%;
      filter: blur(110px);
      opacity: 0.30;
      animation: rsm-edit-float 30s ease-in-out infinite;
    }
    .rsm-compass__blob--a {
      top: -22vmax;
      left: -10vmax;
      background: radial-gradient(circle, rgba(46, 224, 170, 0.55), transparent 65%);
    }
    .rsm-compass__blob--b {
      bottom: -22vmax;
      right: -12vmax;
      background: radial-gradient(circle, rgba(167, 119, 255, 0.55), transparent 65%);
      animation-delay: -14s;
      animation-duration: 36s;
    }

    .rsm-compass__inner {
      position: relative;
      z-index: 2;
    }

    .rsm-compass__copy .rsm-kicker {
      font-family: 'Space Grotesk', system-ui, sans-serif !important;
      font-size: 0.72rem !important;
      letter-spacing: 0.22em !important;
      color: rgba(196, 222, 255, 0.6) !important;
    }

    .rsm-compass__copy .rsm-h1 {
      font-family: 'Syne', system-ui, sans-serif !important;
      font-weight: 700 !important;
      letter-spacing: -0.045em !important;
      color: #fff !important;
      line-height: 0.96 !important;
    }

    .rsm-compass__copy .rsm-h1 em {
      font-style: italic;
      font-family: 'Fraunces', Georgia, serif !important;
      font-weight: 500 !important;
      background: var(--rsm-edit-grad);
      background-size: 220% 100%;
      -webkit-background-clip: text;
      background-clip: text;
      -webkit-text-fill-color: transparent;
      color: transparent !important;
      animation: rsm-edit-grad-flow 9s ease-in-out infinite alternate;
    }

    .rsm-compass__copy .rsm-lead {
      color: rgba(220, 234, 255, 0.78) !important;
    }

    .rsm-compass__meta .rsm-tag {
      background: rgba(255, 255, 255, 0.06) !important;
      border-color: rgba(180, 220, 255, 0.22) !important;
      color: rgba(232, 244, 255, 0.92) !important;
      backdrop-filter: blur(12px);
    }

    /* Compass nav — editorial typographic links */
    .rsm-compass__nav {
      gap: 8px !important;
      grid-template-columns: 1fr !important;
    }

    .rsm-compass__link {
      position: relative;
      display: grid !important;
      grid-template-columns: 44px 1fr auto !important;
      align-items: center !important;
      gap: 18px !important;
      padding: 18px 22px !important;
      border: 1px solid rgba(160, 208, 255, 0.14) !important;
      border-radius: 16px !important;
      background: rgba(255, 255, 255, 0.04) !important;
      color: rgba(232, 244, 255, 0.88) !important;
      font-family: 'Syne', system-ui, sans-serif !important;
      font-weight: 600 !important;
      font-size: 1rem !important;
      letter-spacing: -0.01em !important;
      overflow: hidden;
      isolation: isolate;
      transition:
        border-color 320ms var(--rsm-edit-ease),
        background 320ms var(--rsm-edit-ease),
        transform 320ms var(--rsm-edit-ease),
        box-shadow 320ms var(--rsm-edit-ease) !important;
    }

    .rsm-compass__link::before {
      content: "";
      position: absolute;
      inset: 0;
      background: linear-gradient(135deg, rgba(146, 196, 255, 0.16), rgba(207, 156, 255, 0.10));
      opacity: 0;
      z-index: -1;
      transition: opacity 320ms var(--rsm-edit-ease);
    }

    .rsm-compass__link:hover {
      transform: translateY(-2px) !important;
      border-color: rgba(180, 220, 255, 0.4) !important;
      background: rgba(255, 255, 255, 0.07) !important;
      box-shadow: 0 18px 36px rgba(4, 10, 34, 0.32) !important;
      color: #fff !important;
    }
    .rsm-compass__link:hover::before { opacity: 1; }

    .rsm-compass__link strong {
      display: inline-grid !important;
      place-items: center !important;
      width: 36px !important;
      height: 36px !important;
      border-radius: 10px !important;
      background: rgba(146, 196, 255, 0.16) !important;
      border: 1px solid rgba(146, 196, 255, 0.18) !important;
      color: rgba(196, 222, 255, 0.96) !important;
      font-family: 'Space Grotesk', system-ui, sans-serif !important;
      font-size: 0.78rem !important;
      font-weight: 700 !important;
      letter-spacing: 0 !important;
      transition: background 320ms var(--rsm-edit-ease), color 320ms var(--rsm-edit-ease) !important;
    }
    .rsm-compass__link:hover strong {
      background: rgba(146, 196, 255, 0.28) !important;
      color: #fff !important;
    }

    .rsm-compass__link span {
      flex: 1;
    }
    .rsm-compass__link::after {
      content: "↗" !important;
      font-family: 'Space Grotesk', system-ui, sans-serif !important;
      font-size: 1.1rem !important;
      color: rgba(180, 210, 255, 0.4) !important;
      transform: translate(-4px, 4px) !important;
      opacity: 0 !important;
      transition: transform 320ms var(--rsm-edit-ease), opacity 320ms var(--rsm-edit-ease), color 320ms var(--rsm-edit-ease) !important;
    }
    .rsm-compass__link:hover::after {
      transform: translate(0, 0) !important;
      opacity: 1 !important;
      color: var(--rsm-edit-emerald) !important;
    }

    /* Disabilita le glow per-child esistenti (le sostituiamo con la nuova grammatica) */
    .rsm-compass__link:nth-child(1):hover,
    .rsm-compass__link:nth-child(2):hover,
    .rsm-compass__link:nth-child(3):hover,
    .rsm-compass__link:nth-child(4):hover,
    .rsm-compass__link:nth-child(5):hover,
    .rsm-compass__link:nth-child(6):hover {
      box-shadow: 0 18px 36px rgba(4, 10, 34, 0.32) !important;
    }

    /* 2 colonne su desktop wide */
    @media (min-width: 720px) {
      .rsm-compass__nav { grid-template-columns: 1fr 1fr !important; }
    }

    /* ═════════════════ RESPONSIVE ═════════════════ */
    @media (max-width: 600px) {
      .rsm-banner__title { font-size: clamp(2.6rem, 14vw, 4.4rem) !important; }
      .rsm-banner__sub { font-size: 0.96rem !important; }
      .rsm-edit-band-group { gap: 20px; padding-right: 20px; }
    }

    /* ════════════════════════════════════════════════════════════
       FIX TYPOGRAPHY GLOBALE — Syne ovunque + no Times fallback
       ════════════════════════════════════════════════════════════ */
    .rsm-h1,
    .rsm-h2,
    .rsm-h3,
    .rsm-h4,
    .rsm-display-1,
    .rsm-display-2,
    .rsm-festa__title,
    .rsm-newsletter h2,
    .rsm-modal__title,
    .rsm-xp-card h3,
    .rsm-xp-body h3,
    .rsm-atlas-copy h3,
    .post-featured__title,
    .news-empty strong {
      font-family: 'Syne', 'Cabinet Grotesk', system-ui, -apple-system, sans-serif !important;
      font-weight: 700 !important;
      letter-spacing: -0.025em !important;
      line-height: 1.04 !important;
      text-wrap: balance;
      word-spacing: 0.02em;
    }

    /* Em italic accents — Fraunces con gradient, padding anti-clip */
    .rsm-h1 em,
    .rsm-h2 em,
    .rsm-h3 em,
    .rsm-display-1 em,
    .rsm-display-2 em,
    .rsm-festa__title em,
    .rsm-newsletter h2 em,
    .rsm-banner__title em,
    .rsm-hero__title em,
    .rsm-compass__copy .rsm-h1 em {
      font-family: 'Fraunces', Georgia, serif !important;
      font-style: italic !important;
      font-weight: 500 !important;
      padding: 0 0.06em !important;
      margin: 0 -0.02em !important;
      display: inline-block;
    }

    /* Titoli h1 — più aria, no taglio */
    .rsm-h1 {
      font-size: clamp(2rem, 4vw, 3.6rem) !important;
      letter-spacing: -0.02em !important;
    }
    .rsm-h2 {
      font-size: clamp(1.5rem, 2.6vw, 2.4rem) !important;
      letter-spacing: -0.015em !important;
    }
    .rsm-h3 {
      font-size: clamp(1.2rem, 1.7vw, 1.55rem) !important;
      letter-spacing: -0.01em !important;
    }
    .rsm-festa__title {
      font-size: clamp(2rem, 4.4vw, 4rem) !important;
      letter-spacing: -0.025em !important;
    }
    .rsm-banner__title {
      font-size: clamp(3rem, 9vw, 8rem) !important;
      letter-spacing: -0.04em !important;
    }
    .rsm-hero__title {
      font-size: clamp(2.2rem, 4.6vw, 4.4rem) !important;
      letter-spacing: -0.025em !important;
    }
    .rsm-compass__copy .rsm-h1 {
      font-size: clamp(1.8rem, 3.4vw, 3rem) !important;
    }

    /* Body fonts forced sans (no serif fallback ovunque) */
    .rsm-lead,
    .rsm-body,
    p:not(.rsm-kicker),
    .rsm-newsletter p,
    .rsm-newsletter label,
    .rsm-newsletter .privacy-note {
      font-family: 'Space Grotesk', 'Cabinet Grotesk', system-ui, -apple-system, sans-serif !important;
    }

    /* ════════════════════════════════════════════════════════════
       BOTTONI: zero frecce, più in rilievo
       ════════════════════════════════════════════════════════════ */
    .rsm-btn .rsm-btn-icon,
    .rsm-btn-icon {
      display: none !important;
    }
    .rsm-btn {
      min-height: 56px !important;
      padding: 0 26px !important;
      font-family: 'Syne', 'Cabinet Grotesk', system-ui, sans-serif !important;
      font-weight: 700 !important;
      letter-spacing: -0.005em !important;
      box-shadow:
        0 18px 38px rgba(11, 13, 20, 0.20),
        inset 0 1px 0 rgba(255, 255, 255, 0.10) !important;
      transition:
        transform 320ms var(--rsm-edit-ease-spring),
        box-shadow 320ms var(--rsm-edit-ease),
        background 320ms var(--rsm-edit-ease) !important;
    }
    .rsm-btn:hover {
      transform: translateY(-3px) !important;
      box-shadow:
        0 26px 52px rgba(11, 13, 20, 0.28),
        inset 0 1px 0 rgba(255, 255, 255, 0.14) !important;
    }
    .rsm-btn:active {
      transform: translateY(-1px) scale(0.98) !important;
    }

    .rsm-btn--brand {
      background: linear-gradient(120deg, #1fbf7d 0%, #4a9bff 50%, #8b5cf6 100%) !important;
      background-size: 220% 100% !important;
      background-position: 0% 50% !important;
      color: #fff !important;
      box-shadow:
        0 22px 50px rgba(74, 155, 255, 0.34),
        0 8px 18px rgba(139, 92, 246, 0.22),
        inset 0 1px 0 rgba(255, 255, 255, 0.18) !important;
    }
    .rsm-btn--brand:hover {
      background-position: 100% 50% !important;
      box-shadow:
        0 30px 64px rgba(74, 155, 255, 0.42),
        0 12px 24px rgba(139, 92, 246, 0.32),
        inset 0 1px 0 rgba(255, 255, 255, 0.22) !important;
    }

    .rsm-btn--ghost {
      background: rgba(255, 255, 255, 0.85) !important;
      border: 1px solid rgba(11, 13, 20, 0.16) !important;
      color: var(--rsm-ink) !important;
      backdrop-filter: blur(12px);
    }
    .rsm-btn--ghost:hover {
      background: #fff !important;
      border-color: var(--rsm-ink) !important;
    }

    /* Bottoni dentro newsletter scura: ghost variant adattato */
    .rsm-newsletter .rsm-btn--ghost {
      background: rgba(255, 255, 255, 0.10) !important;
      border-color: rgba(255, 255, 255, 0.22) !important;
      color: #fff !important;
    }
    .rsm-newsletter .rsm-btn--ghost:hover {
      background: rgba(255, 255, 255, 0.20) !important;
      border-color: rgba(255, 255, 255, 0.46) !important;
    }

    /* Banner CTA: override colore testo per leggibilità su gradient */
    .rsm-banner__cta.rsm-btn--brand {
      color: #fff !important;
    }

    /* "Leggi →" arrow nei post-card: nascondi span con freccia */
    .btn-read span[aria-hidden="true"] {
      display: none !important;
    }
    .btn-read {
      gap: 0 !important;
      font-family: 'Syne', system-ui, sans-serif !important;
      font-weight: 700 !important;
      padding: 8px 16px !important;
      border-radius: 999px !important;
      background: rgba(11, 13, 20, 0.06) !important;
      transition: background 240ms var(--rsm-edit-ease), color 240ms var(--rsm-edit-ease) !important;
    }
    .btn-read:hover {
      background: var(--rsm-ink) !important;
      color: #fff !important;
    }

    /* Compass link: niente più freccia ↗ */
    .rsm-compass__link::after {
      display: none !important;
    }
    .rsm-compass__link {
      grid-template-columns: 44px 1fr !important;
    }

    /* ════════════════════════════════════════════════════════════
       EVENTI: più cards visibili (grid più compatto)
       ════════════════════════════════════════════════════════════ */
    .post-card-grid {
      grid-template-columns: repeat(auto-fill, minmax(220px, 1fr)) !important;
      gap: 16px !important;
    }
    @media (min-width: 1100px) {
      .post-card-grid {
        grid-template-columns: repeat(4, 1fr) !important;
      }
    }
    @media (min-width: 1400px) {
      .post-card-grid {
        grid-template-columns: repeat(5, 1fr) !important;
      }
    }
    .post-card__body {
      padding: 16px !important;
      gap: 8px !important;
    }
    .post-card__title {
      font-size: 1rem !important;
      line-height: 1.22 !important;
    }
    .post-card__excerpt {
      font-size: 0.85rem !important;
      line-height: 1.45 !important;
    }
    .post-card__media,
    .post-card .news-card__media {
      aspect-ratio: 16 / 11;
    }

    /* ════════════════════════════════════════════════════════════
       NEWSLETTER (Resta in contatto): typography + spacing fix
       ════════════════════════════════════════════════════════════ */
    .rsm-newsletter {
      margin-top: 0 !important;
      padding: clamp(48px, 6vw, 80px) 0 clamp(48px, 6vw, 72px) !important;
    }
    .rsm-newsletter h2 {
      font-family: 'Syne', system-ui, sans-serif !important;
      font-weight: 700 !important;
      letter-spacing: -0.025em !important;
      font-size: clamp(1.8rem, 3.2vw, 2.6rem) !important;
      line-height: 1.04 !important;
    }
    .rsm-newsletter h2 em {
      font-family: 'Fraunces', Georgia, serif !important;
      font-style: italic !important;
      font-weight: 500 !important;
      padding: 0 0.06em !important;
    }
    .rsm-newsletter .rsm-kicker {
      font-family: 'Space Grotesk', system-ui, sans-serif !important;
      font-size: 0.72rem !important;
      letter-spacing: 0.22em !important;
    }
    .rsm-newsletter label {
      font-family: 'Space Grotesk', system-ui, sans-serif !important;
      font-size: 0.7rem !important;
      letter-spacing: 0.2em !important;
      text-transform: uppercase !important;
      color: rgba(255, 255, 255, 0.62) !important;
    }
    .rsm-newsletter .rsm-input {
      font-family: 'Space Grotesk', system-ui, sans-serif !important;
    }
    .rsm-newsletter .privacy-note,
    .rsm-newsletter .privacy-note a {
      font-family: 'Space Grotesk', system-ui, sans-serif !important;
    }

    /* ════════════════════════════════════════════════════════════
       DOVE SIAMO (07): typography fix + contact lines
       ════════════════════════════════════════════════════════════ */
    .rsm-contact h2,
    .rsm-contact .rsm-h1,
    .rsm-contact .rsm-h2 {
      font-family: 'Syne', system-ui, sans-serif !important;
    }
    .rsm-contact .rsm-h2 em,
    .rsm-contact .rsm-h1 em {
      font-family: 'Fraunces', Georgia, serif !important;
      padding: 0 0.06em !important;
    }
    .rsm-contact__line strong {
      font-family: 'Space Grotesk', system-ui, sans-serif !important;
      font-size: 0.7rem !important;
      letter-spacing: 0.2em !important;
      text-transform: uppercase !important;
      color: var(--rsm-ink-3) !important;
    }
    .rsm-contact__line a,
    .rsm-contact__line span {
      font-family: 'Space Grotesk', system-ui, sans-serif !important;
    }

    /* No spazio bianco tra contact e newsletter (è dentro la stessa section) */
    .rsm-contact .rsm-newsletter {
      margin-top: var(--rsm-home-section-space, 30px) !important;
    }

    /* No gap fra newsletter e footer (footer ha già il proprio spacing) */
    .rsm-section.rsm-contact { padding-bottom: var(--rsm-home-section-space, 30px) !important; }
    .rsm-foot { margin-top: 0 !important; }

    /* ════════════════════════════════════════════════════════════
       FIX KICKER GLOBALE — niente serif, niente lineetta lunga
       ════════════════════════════════════════════════════════════ */
    .rsm-kicker {
      font-family: 'Space Grotesk', system-ui, sans-serif !important;
      font-size: 0.72rem !important;
      font-weight: 500 !important;
      letter-spacing: 0.22em !important;
      text-transform: uppercase !important;
    }

    /* ════════════════════════════════════════════════════════════
       FESTA-DAY chips — fix font Times
       ════════════════════════════════════════════════════════════ */
    .rsm-festa__day,
    .festa-day,
    .rsm-festa__day strong,
    .rsm-festa__day span {
      font-family: 'Space Grotesk', 'Cabinet Grotesk', system-ui, sans-serif !important;
    }
    .rsm-festa__day strong {
      font-family: 'Syne', system-ui, sans-serif !important;
      font-weight: 800 !important;
    }

    /* ═════════════════ FIXED SECTION SPACING ═════════════════ */
    body[data-page="home"] {
      --rsm-home-section-space: 30px;
      background:
        radial-gradient(ellipse at 14% -10%, rgba(102, 255, 214, 0.18), transparent 38%),
        radial-gradient(ellipse at 80% -8%, rgba(126, 188, 255, 0.22), transparent 42%),
        radial-gradient(ellipse at 100% 100%, rgba(196, 122, 255, 0.20), transparent 48%),
        linear-gradient(160deg, #051c2c 0%, #082649 50%, #2a1147 100%);
      background-repeat: no-repeat;
      background-attachment: fixed;
    }

    .rsm-hero,
    .rsm-compass,
    .rsm-festa,
    .rsm-gallery,
    .rsm-contact,
    .rsm-valli,
    .rsm-newsletter,
    .rsm-edit-band {
      padding-top: var(--rsm-home-section-space) !important;
      padding-bottom: var(--rsm-home-section-space) !important;
    }

    .rsm-valli,
    .rsm-contact .rsm-newsletter {
      margin-top: var(--rsm-home-section-space) !important;
    }

    /* ═════════════════ FLAT SECTION SURFACES ═════════════════
       Le sezioni restano pronte per un fondo unico dietro il contenuto:
       basta valorizzare --rsm-flat-section-bg sul root interessato.
       Tutte le superfici interne restano neutre e sullo stesso piano. */
    .rsm-hero,
    .rsm-compass,
    .rsm-festa,
    .rsm-events,
    .rsm-gallery,
    .rsm-territory,
    .rsm-contact,
    .rsm-newsletter,
    .rsm-valli {
      --rsm-flat-section-bg: transparent;
      --rsm-flat-section-border: transparent;
      --rsm-flat-panel-bg: transparent;
      --rsm-flat-panel-border: var(--rsm-line);
      --rsm-flat-panel-shadow: none;
      --rsm-flat-panel-filter: none;
      background: var(--rsm-flat-section-bg) !important;
      border-top-color: var(--rsm-flat-section-border) !important;
      border-bottom-color: var(--rsm-flat-section-border) !important;
      box-shadow: none !important;
      backdrop-filter: none !important;
      -webkit-backdrop-filter: none !important;
    }

    .rsm-hero__accent,
    .rsm-compass::before,
    .rsm-compass__hairline,
    .rsm-compass__aurora,
    .rsm-compass__blob,
    .rsm-compass__link::before {
      display: none !important;
    }

    .rsm-compass__copy .rsm-kicker,
    .rsm-compass__copy .rsm-lead,
    .rsm-newsletter .rsm-kicker,
    .rsm-newsletter p,
    .rsm-newsletter label,
    .rsm-newsletter .privacy-note,
    .rsm-newsletter .privacy-note a,
    .rsm-newsletter .rsm-form-status {
      color: var(--rsm-ink-3) !important;
    }

    .rsm-compass,
    .rsm-newsletter,
    .rsm-compass__copy .rsm-h1,
    .rsm-newsletter h2,
    .rsm-contact .rsm-h2,
    .rsm-contact .rsm-h1 {
      color: var(--rsm-ink) !important;
    }

    .rsm-compass__meta .rsm-tag,
    .rsm-compass__link,
    .rsm-gallery__stage,
    .rsm-compass__link strong,
    .rsm-festa__badge,
    .rsm-festa__dates,
    .rsm-festa__day,
    .festa-day,
    .rsm-festa__menu-list li,
    .rsm-festa .rsm-card,
    .news-filter-chip,
    .post-featured,
    .post-card,
    .news-empty,
    .fact,
    .rsm-fact,
    .stack-card,
    .rsm-xp-card,
    .rsm-contact__card,
    .rsm-contact__map,
    .rsm-newsletter .rsm-input,
    .rsm-contact .rsm-btn,
    .rsm-newsletter .rsm-btn,
    .btn-read {
      background: var(--rsm-flat-panel-bg) !important;
      background-image: none !important;
      border-color: var(--rsm-flat-panel-border) !important;
      box-shadow: var(--rsm-flat-panel-shadow) !important;
      backdrop-filter: var(--rsm-flat-panel-filter) !important;
      -webkit-backdrop-filter: var(--rsm-flat-panel-filter) !important;
    }

    .rsm-festa__poster,
    .rsm-gallery__stage,
    .rsm-atlas-photo,
    .rsm-contact__map,
    .rsm-valli,
    .rsm-festa__badge,
    .rsm-contact__card,
    .post-featured,
    .post-card,
    .stack-card,
    .rsm-xp-card {
      box-shadow: none !important;
    }

    .rsm-compass__link,
    .rsm-compass__meta .rsm-tag,
    .news-filter-chip,
    .rsm-contact .rsm-btn,
    .rsm-newsletter .rsm-btn,
    .btn-read {
      color: var(--rsm-ink-2) !important;
    }

    .rsm-compass__link:hover,
    .rsm-festa__day:hover,
    .festa-day:hover,
    .rsm-festa__menu-list li:hover,
    .news-filter-chip:hover,
    .news-filter-chip.on,
    .post-featured:hover,
    .post-card:hover,
    .stack-card:hover,
    .rsm-xp-card:hover,
    .rsm-contact .rsm-btn:hover,
    .rsm-newsletter .rsm-btn:hover,
    .btn-read:hover {
      background: transparent !important;
      box-shadow: none !important;
    }

    .rsm-compass__link strong,
    .news-filter-chip.on,
    .rsm-contact .rsm-btn--brand,
    .rsm-newsletter .rsm-btn--brand {
      color: var(--rsm-ink) !important;
    }

    .rsm-newsletter .rsm-input:focus {
      background: transparent !important;
      box-shadow: none !important;
      border-color: var(--rsm-line-3) !important;
    }

    .rsm-contact__map,
    .rsm-atlas-photo,
    .rsm-festa__poster {
      background-color: transparent !important;
    }

    /* ═════════════════ REDUCED MOTION ═════════════════ */
    @media (prefers-reduced-motion: reduce) {
      .rsm-banner__blob,
      .rsm-compass__blob,
      .rsm-banner__title em,
      .rsm-hero__title em,
      .rsm-compass__copy .rsm-h1 em,
      .rsm-banner__kicker .festa-pulse,
      .rsm-edit-band-track {
        animation: none !important;
      }
      .rsm-edit-line,
      .rsm-metric {
        opacity: 1 !important;
        transform: none !important;
        transition: none !important;
      }
      .rsm-metric::before { transform: scaleX(1) !important; }
    }
  </style>
</head>
<body data-page="home">

<!--PARTIAL:nav-->

  <main id="main">

    <!-- ─────────────────────────────────────────────────────────
         00 · BANNER VIDEO
         ───────────────────────────────────────────────────────── -->
    <section class="rsm-banner" id="banner" aria-label="Benvenuto a Rivalta sul Mincio" data-rsm-edit-banner>
      <div class="rsm-banner__video-wrap">
        <video class="rsm-banner__video" src="/video/banner.mp4"
               muted autoplay loop playsinline preload="metadata"></video>
        <div class="rsm-banner__overlay"></div>
      </div>
      <div class="rsm-banner__aurora" aria-hidden="true">
        <span class="rsm-banner__blob rsm-banner__blob--a"></span>
        <span class="rsm-banner__blob rsm-banner__blob--b"></span>
        <span class="rsm-banner__blob rsm-banner__blob--c"></span>
      </div>
      <div class="rsm-banner__grain" aria-hidden="true"></div>
      <div class="rsm-banner__content" data-rsm-edit-stagger>
        <p class="rsm-kicker rsm-banner__kicker rsm-edit-line">
          <span class="festa-pulse" aria-hidden="true"></span>
          <span>Pro Loco Rivalta sul Mincio</span>
          <span aria-hidden="true">·</span>
          <span>Mantova</span>
        </p>
        <h1 class="rsm-display-1 rsm-banner__title" style="margin-top: var(--rsm-s-5)">
          <span class="rsm-edit-line">Rivalta sul <em>Mincio</em></span>
        </h1>
        <p class="rsm-banner__sub rsm-edit-line">Valli, borghi, tradizioni. Nel cuore del Parco del Mincio.</p>
        <span class="rsm-edit-line" style="display:block;margin-top:var(--rsm-s-7);">
          <a class="rsm-btn rsm-btn--brand magnetic rsm-banner__cta" href="#intro">
            <span>Scopri il borgo</span>
          </a>
        </span>
      </div>
      <div class="rsm-banner__scroll-hint" aria-hidden="true">
        <span class="rsm-banner__scroll-line"></span>
      </div>
    </section>

    <!-- ─────────────────────────────────────────────────────────
         01 · INTRO (HERO)
         ───────────────────────────────────────────────────────── -->
    <section class="rsm-section rsm-hero" id="intro" aria-labelledby="intro-title" data-component="section/hero">
      <span class="rsm-hero__accent" aria-hidden="true"></span>
      <div class="rsm-container">
        <div class="rsm-hero__layout">
          <div class="rsm-hero__main" data-reveal data-rsm-edit-stagger>
            <p class="rsm-kicker rsm-edit-line">
              <span class="festa-pulse" aria-hidden="true"></span>
              <span>Pro Loco Rivalta sul Mincio</span>
              <span aria-hidden="true">·</span>
              <span>Mantova</span>
            </p>
            <h1 class="rsm-display-1 rsm-hero__title" id="intro-title" style="margin-top: var(--rsm-s-5)">
              <span class="rsm-edit-line">Dove l&rsquo;<em>acqua</em> diventa <em>paese</em></span>
            </h1>
            <p class="rsm-lead">Un borgo di pescatori nel cuore del Parco del Mincio: canneti, chiari, aironi, ciclabili lente e una cucina d&rsquo;acqua che tiene insieme memoria e futuro.</p>
            <div class="rsm-button-row">
              <a class="rsm-btn rsm-btn--brand magnetic" href="#festa" data-component="button/primary">
                <span>Festa del Pesce 2025</span>
                <span class="rsm-btn-icon" aria-hidden="true">&rarr;</span>
              </a>
              <a class="rsm-btn rsm-btn--ghost magnetic" href="#mappa" data-component="button/ghost">
                <span>Mappa della pagina</span>
                <span class="rsm-btn-icon" aria-hidden="true">&darr;</span>
              </a>
            </div>

            <div class="rsm-hero__metrics">
              <div class="rsm-metric"><strong data-count="1967">0</strong><span>Pro Loco fondata nel</span></div>
              <div class="rsm-metric"><strong data-count="1081">0</strong><span>ettari di riserva</span></div>
              <div class="rsm-metric"><strong data-count="260">0</strong><span>specie di uccelli</span></div>
              <div class="rsm-metric"><strong data-count="40">0</strong><span>edizioni Festa Pesce</span></div>
            </div>
          </div>

          <div class="rsm-hero__media" data-reveal style="--delay: 180ms" data-component="component/photo-frame">
            <div class="swiper rsm-swiper rsm-swiper--hero" role="region" aria-label="Galleria animata di Rivalta">
              <div class="swiper-wrapper">
                <div class="swiper-slide">
                  <img src="/video/mincio-1.gif" alt="Mantova e i laghi del Mincio" loading="eager" decoding="async">
                </div>
                <div class="swiper-slide">
                  <img src="/video/mincio-2.gif" alt="Rivalta sul Mincio al tramonto" loading="lazy" decoding="async">
                </div>
                <div class="swiper-slide">
                  <img src="/video/mincio-3.gif" alt="Le valli del Mincio tra i canneti" loading="lazy" decoding="async">
                </div>
              </div>
              <div class="swiper-pagination"></div>
              <button class="swiper-button-prev" aria-label="Slide precedente"></button>
              <button class="swiper-button-next" aria-label="Slide successiva"></button>
            </div>
          </div>
        </div>
      </div>
    </section>

    <!-- ─────────────────────────────────────────────────────────
         01.5 · MARQUEE BAND
         ───────────────────────────────────────────────────────── -->
    <div class="rsm-edit-band" aria-hidden="true">
      <div class="rsm-edit-band-track">
        <span class="rsm-edit-band-group">
          <span class="rsm-edit-band-item">Festa del Pesce</span><span class="rsm-edit-band-dot"></span>
          <span class="rsm-edit-band-item">Valli del Mincio</span><span class="rsm-edit-band-dot"></span>
          <span class="rsm-edit-band-item">Sagra del Luccio</span><span class="rsm-edit-band-dot"></span>
          <span class="rsm-edit-band-item">Ecoturismo</span><span class="rsm-edit-band-dot"></span>
          <span class="rsm-edit-band-item">Borgo storico</span><span class="rsm-edit-band-dot"></span>
          <span class="rsm-edit-band-item">Parco Regionale</span><span class="rsm-edit-band-dot"></span>
          <span class="rsm-edit-band-item">Cucina d&rsquo;acqua dolce</span><span class="rsm-edit-band-dot"></span>
        </span>
        <span class="rsm-edit-band-group" aria-hidden="true">
          <span class="rsm-edit-band-item">Festa del Pesce</span><span class="rsm-edit-band-dot"></span>
          <span class="rsm-edit-band-item">Valli del Mincio</span><span class="rsm-edit-band-dot"></span>
          <span class="rsm-edit-band-item">Sagra del Luccio</span><span class="rsm-edit-band-dot"></span>
          <span class="rsm-edit-band-item">Ecoturismo</span><span class="rsm-edit-band-dot"></span>
          <span class="rsm-edit-band-item">Borgo storico</span><span class="rsm-edit-band-dot"></span>
          <span class="rsm-edit-band-item">Parco Regionale</span><span class="rsm-edit-band-dot"></span>
          <span class="rsm-edit-band-item">Cucina d&rsquo;acqua dolce</span><span class="rsm-edit-band-dot"></span>
        </span>
      </div>
    </div>

    <!-- ─────────────────────────────────────────────────────────
         02 · MAPPA DELLA PAGINA
         ───────────────────────────────────────────────────────── -->
    <section class="rsm-section rsm-section--compact rsm-compass" id="mappa" aria-labelledby="mappa-title" data-component="section/compass" data-rsm-edit-compass>
      <div class="rsm-compass__hairline" aria-hidden="true"></div>
      <div class="rsm-compass__aurora" aria-hidden="true">
        <span class="rsm-compass__blob rsm-compass__blob--a"></span>
        <span class="rsm-compass__blob rsm-compass__blob--b"></span>
      </div>
      <div class="rsm-container">
        <div class="rsm-compass__inner" data-reveal>
          <div class="rsm-compass__copy">
            <p class="rsm-kicker">02 · Mappa della pagina</p>
            <h2 class="rsm-h1" id="mappa-title">Sei tappe chiare, nessuno scroll <em>infinito</em></h2>
            <p class="rsm-lead">Se preferisci orientarti prima di leggere, qui sotto c&rsquo;&egrave; la sequenza completa della home: cosa accade ora, cosa vedere, cosa fare e dove arrivare alla fine.</p>
            <div class="rsm-compass__meta">
              <span class="rsm-tag rsm-tag--emerald">7 sezioni</span>
              <span class="rsm-tag rsm-tag--azure">ordine lineare</span>
              <span class="rsm-tag rsm-tag--violet">contatti in chiusura</span>
            </div>
          </div>
          <nav class="rsm-compass__nav" aria-label="Vai a una sezione della homepage">
            <a class="rsm-compass__link" href="#festa"><strong>03</strong><span>Festa del Pesce</span></a>
            <a class="rsm-compass__link" href="#eventi"><strong>04</strong><span>Eventi</span></a>
            <a class="rsm-compass__link" href="#galleria"><strong>05</strong><span>Galleria</span></a>
            <a class="rsm-compass__link" href="#territorio"><strong>06</strong><span>Territorio</span></a>
            <a class="rsm-compass__link" href="#contatti"><strong>07</strong><span>Contatti</span></a>
            <a class="rsm-compass__link" href="#intro"><strong>↑</strong><span>Torna in cima</span></a>
          </nav>
        </div>
      </div>
    </section>

    <!-- ─────────────────────────────────────────────────────────
         03 · FESTA DEL PESCE
         ───────────────────────────────────────────────────────── -->
    <section class="rsm-section rsm-festa" id="festa" aria-labelledby="festa-title" data-component="section/festa">
      <div class="rsm-container">
        <header class="rsm-festa__head">
          <div data-reveal>
            <p class="rsm-kicker"><span class="festa-pulse" aria-hidden="true"></span>03 · 40&deg; edizione · 1985 — 2025</p>
            <h2 class="rsm-festa__title" id="festa-title">Festa del Pesce e del <em>Luccio alla Rivaltese</em></h2>
            <p class="rsm-lead">Quarant&rsquo;anni di cucina d&rsquo;acqua dolce all&rsquo;Area Feste Platana, ai margini del canneto delle Valli del Mincio. Cucine aperte dalle 19:30, servizio al tavolo a cura dei volontari della Pro Loco.</p>
            <div class="rsm-button-row">
              <a class="rsm-btn rsm-btn--brand magnetic" href="#eventi"><span>Tutto il programma</span><span class="rsm-btn-icon" aria-hidden="true">&rarr;</span></a>
              <button class="rsm-btn rsm-btn--ghost magnetic" type="button" data-modal-open="newsletter-modal"><span>Ricorda l&rsquo;evento</span><span class="rsm-btn-icon" aria-hidden="true">&rarr;</span></button>
            </div>
          </div>

          <aside class="rsm-festa__poster festa-poster-frame" data-reveal style="--delay: 160ms" aria-label="Manifesto della 40° Festa del Pesce">
            <img src="/img/festa-pesce-2025.jpg" alt="Manifesto Festa del Pesce e del Luccio alla Rivaltese 2025" loading="lazy" />
            <span class="rsm-festa__badge" aria-hidden="true">
              <strong>40</strong>
              <span>1985 — 2025</span>
            </span>
          </aside>
        </header>

        <div class="rsm-festa__dates" data-reveal style="--delay: 80ms" aria-label="Date Festa del Pesce 2025">
          <div class="rsm-festa__day festa-day"><strong>Ven 4</strong><span>Luglio</span></div>
          <div class="rsm-festa__day festa-day"><strong>Sab 5</strong><span>Luglio</span></div>
          <div class="rsm-festa__day festa-day"><strong>Dom 6</strong><span>Luglio</span></div>
          <div class="rsm-festa__day-divider" aria-hidden="true"></div>
          <div class="rsm-festa__day festa-day"><strong>Ven 11</strong><span>Luglio</span></div>
          <div class="rsm-festa__day festa-day"><strong>Sab 12</strong><span>Luglio</span></div>
          <div class="rsm-festa__day festa-day"><strong>Dom 13</strong><span>Luglio</span></div>
        </div>

        <div class="rsm-festa__grid">
          <article class="rsm-card" data-reveal data-component="card/menu">
            <header>
              <p class="rsm-kicker">Il men&ugrave; 2025</p>
              <h3 class="rsm-h2" style="margin-top:12px">Quarant&rsquo;anni di ricette d&rsquo;acqua dolce</h3>
              <p class="rsm-body" style="color:var(--rsm-ink-3);margin-top:10px">Le specialit&agrave; storiche della Pro Loco. <strong>Luccio in salsa</strong> e <strong>Luccio in bianco</strong> sono Prodotti Agroalimentari Tradizionali (PAT) e De.Co. comunale.</p>
            </header>
            <ol class="rsm-festa__menu-list" aria-label="Piatti della 40° edizione">
              <li><span class="rsm-festa__menu-num">01</span><span class="rsm-festa__menu-name">Risotto con pesce gatto, pesina o saltarei</span><span class="rsm-festa__menu-tag">primo</span></li>
              <li><span class="rsm-festa__menu-num">02</span><span class="rsm-festa__menu-name">Tortelloni di branzino</span><span class="rsm-festa__menu-tag">primo</span></li>
              <li><span class="rsm-festa__menu-num">03</span><span class="rsm-festa__menu-name">Bigoli al torchio con sardelle</span><span class="rsm-festa__menu-tag">primo</span></li>
              <li><span class="rsm-festa__menu-num">04</span><span class="rsm-festa__menu-name"><em>Luccio in salsa</em> alla Rivaltese</span><span class="rsm-festa__menu-tag">PAT · De.Co.</span></li>
              <li><span class="rsm-festa__menu-num">05</span><span class="rsm-festa__menu-name"><em>Luccio in bianco</em> alla Rivaltese</span><span class="rsm-festa__menu-tag">PAT · De.Co.</span></li>
              <li><span class="rsm-festa__menu-num">06</span><span class="rsm-festa__menu-name">Anguilla ai ferri</span><span class="rsm-festa__menu-tag">secondo</span></li>
              <li><span class="rsm-festa__menu-num">07</span><span class="rsm-festa__menu-name">Pesina e saltarei fritti</span><span class="rsm-festa__menu-tag">fritto</span></li>
              <li><span class="rsm-festa__menu-num">08</span><span class="rsm-festa__menu-name">Frittata con saltarei</span><span class="rsm-festa__menu-tag">tradizione</span></li>
              <li><span class="rsm-festa__menu-num">09</span><span class="rsm-festa__menu-name">Angolo dolci</span><span class="rsm-festa__menu-tag">dessert</span></li>
            </ol>
          </article>

          <aside class="rsm-card rsm-festa__wines" data-reveal style="--delay: 140ms" data-component="card/wines">
            <p class="rsm-kicker">In abbinamento</p>
            <h3 class="rsm-h2" style="margin-top:12px">Cantina Ricchi · Monzambano</h3>
            <p class="rsm-body" style="color:var(--rsm-ink-3);margin-top:10px">Etichette dell&rsquo;Alto Mantovano selezionate per armonizzarsi con i piatti a base di pesce di fiume.</p>
            <ul>
              <li><strong>Chiaretto</strong><span>&ldquo;La Casina&rdquo;</span></li>
              <li><strong>Bianco</strong><span>&ldquo;La Casina&rdquo;</span></li>
              <li><strong>Bianco</strong><span>&ldquo;Brioso&rdquo;</span></li>
              <li><strong>Spumante brut</strong><span>&ldquo;Espressioni 8&rdquo;</span></li>
              <li><strong>Spumante dolce</strong><span>&ldquo;Aroma 85&rdquo;</span></li>
            </ul>
          </aside>
        </div>

        <div class="rsm-festa__foot">
          <article class="rsm-card" data-reveal>
            <p class="rsm-kicker">In ricordo</p>
            <h4 class="rsm-h3" style="margin-top:10px">Luciano Soave</h4>
            <p style="color:var(--rsm-ink-3);margin-top:10px;line-height:1.6">La 40&deg; edizione &egrave; dedicata a Luciano, volontario storico della Pro Loco. Per anni ha curato i risotti della festa e, d&rsquo;inverno, il vin brul&eacute; della comunit&agrave;.</p>
          </article>
          <article class="rsm-card" data-reveal style="--delay: 120ms">
            <p class="rsm-kicker">In concomitanza</p>
            <h4 class="rsm-h3" style="margin-top:10px">BARACCA dei NOTT-AMBULI 2025</h4>
            <p style="color:var(--rsm-ink-3);margin-top:10px;line-height:1.6">Quarta edizione: panini con porchetta, hot dog, cipperone, aperitivi e birre artigianali, con <strong>6 concerti live</strong> nell&rsquo;area eventi adiacente.</p>
            <a class="rsm-btn rsm-btn--ghost rsm-btn--sm magnetic" href="#eventi" style="margin-top:18px"><span>Programma concerti</span><span class="rsm-btn-icon" aria-hidden="true">&rarr;</span></a>
          </article>
        </div>
      </div>
    </section>

    <!-- ─────────────────────────────────────────────────────────
         04 · EVENTI (NEWS dal Supabase)
         ───────────────────────────────────────────────────────── -->
    <section class="rsm-section rsm-events" id="eventi" aria-labelledby="eventi-title" data-component="section/events">
      <div class="rsm-container">
        <div class="rsm-section-head">
          <div data-reveal>
            <p class="rsm-kicker">04 · Eventi e iniziative</p>
            <h2 class="rsm-h1" id="eventi-title">Tutto quello che <em>succede</em> a Rivalta</h2>
          </div>
          <p class="rsm-lead" data-reveal style="--delay: 120ms">Eventi, escursioni, avvisi e racconti caricati dalla redazione tramite Supabase. Filtra per categoria o sfoglia gli ultimi articoli pubblicati.</p>
        </div>

        <div data-reveal>
          <div class="news-filter-row" id="news-filter-row" aria-label="Filtra news per categoria"></div>
          <div class="news-status" id="news-status">Caricamento articoli in corso.</div>
          <article class="post-featured skeleton" id="post-featured" style="display:none"></article>
          <div class="post-card-grid" id="posts-grid"></div>
          <div class="news-empty" id="news-empty" style="display:none">
            <strong>Nessun articolo pubblicato</strong>
            <span>I contenuti verranno mostrati qui appena la redazione li pubblica.</span>
          </div>
          <div class="news-actions">
            <a class="rsm-btn rsm-btn--ghost magnetic" href="/category">
              <span>Vedi tutti i post</span>
              <span class="rsm-btn-icon" aria-hidden="true">&rarr;</span>
            </a>
          </div>
        </div>
      </div>
    </section>

    <!-- ─────────────────────────────────────────────────────────
         05 · GALLERIA
         ───────────────────────────────────────────────────────── -->
    <section class="rsm-section rsm-gallery" id="galleria" aria-labelledby="galleria-title" data-component="section/gallery">
      <div class="rsm-container">
        <div class="rsm-section-head">
          <div data-reveal>
            <p class="rsm-kicker">05 · Galleria</p>
            <h2 class="rsm-h1" id="galleria-title">Foto del borgo, delle <em>valli</em> e degli eventi</h2>
          </div>
          <p class="rsm-lead" data-reveal style="--delay: 120ms">Un carosello pulito, automatico e consultabile. Sostituibile in pochi click con immagini fornite dalla redazione o da Figma.</p>
        </div>

        <div class="rsm-gallery__stage" data-reveal data-parallax="0.06">
          <div class="swiper rsm-swiper" role="region" aria-label="Galleria fotografica di Rivalta sul Mincio">
            <div class="swiper-wrapper">
              <div class="swiper-slide rsm-slide">
                <div class="slide-bg-blur" style="background-image:url('/img/mantova-laghi-aerea.jpg')"></div>
                <img class="slide-img" src="/img/mantova-laghi-aerea.jpg" alt="Mantova e i laghi del Mincio, vista aerea" loading="lazy">
              </div>
              <div class="swiper-slide rsm-slide">
                <div class="slide-bg-blur" style="background-image:url('/img/festa-pesce.jpg')"></div>
                <img class="slide-img" src="/img/festa-pesce.jpg" alt="Festa del Pesce e del Luccio all&rsquo;Area Feste Platana" loading="lazy">
              </div>
              <div class="swiper-slide rsm-slide">
                <div class="slide-bg-blur" style="background-image:url('/img/luccio-in-salsa.jpg')"></div>
                <img class="slide-img" src="/img/luccio-in-salsa.jpg" alt="Luccio in salsa alla rivaltese, prodotto PAT" loading="lazy">
              </div>
              <div class="swiper-slide rsm-slide">
                <div class="slide-bg-blur" style="background-image:url('/img/gong-bath-parco.jpg')"></div>
                <img class="slide-img" src="/img/gong-bath-parco.jpg" alt="Gong Bath al Parco della Corte di Rivalta" loading="lazy">
              </div>
              <div class="swiper-slide rsm-slide">
                <div class="slide-bg-blur" style="background-image:url('/img/waterbike-mincio.jpg')"></div>
                <img class="slide-img" src="/img/waterbike-mincio.jpg" alt="Waterbike sul fiume Mincio tra i canneti" loading="lazy">
              </div>
              <div class="swiper-slide rsm-slide">
                <div class="slide-bg-blur" style="background-image:url('/img/museo-etnografico-tramonto.jpg')"></div>
                <img class="slide-img" src="/img/museo-etnografico-tramonto.jpg" alt="Porto storico di Rivalta al tramonto" loading="lazy">
              </div>
              <div class="swiper-slide rsm-slide">
                <div class="slide-bg-blur" style="background-image:url('/img/atlante/sunset-ga646351d3_1920.jpg')"></div>
                <img class="slide-img" src="/img/atlante/sunset-ga646351d3_1920.jpg" alt="Tramonto sul Mincio" loading="lazy">
              </div>
              <div class="swiper-slide rsm-slide">
                <div class="slide-bg-blur" style="background-image:url('/img/atlante/ste2447-natura-parco-del-mincio-isola-fiori-di-loto-04-07-14-b.jpg')"></div>
                <img class="slide-img" src="/img/atlante/ste2447-natura-parco-del-mincio-isola-fiori-di-loto-04-07-14-b.jpg" alt="Fiori di loto nel Parco del Mincio" loading="lazy">
              </div>
              <div class="swiper-slide rsm-slide">
                <div class="slide-bg-blur" style="background-image:url('/img/Garzetta - Mssimo Brambilla.jpg')"></div>
                <img class="slide-img" src="/img/Garzetta - Mssimo Brambilla.jpg" alt="Garzetta nel Parco del Mincio" loading="lazy">
              </div>
              <div class="swiper-slide rsm-slide">
                <div class="slide-bg-blur" style="background-image:url('/img/corte-mincio-storica.jpg')"></div>
                <img class="slide-img" src="/img/corte-mincio-storica.jpg" alt="Corte Mincio storica" loading="lazy">
              </div>
              <div class="swiper-slide rsm-slide">
                <div class="slide-bg-blur" style="background-image:url('/img/museo-etnografico.jpg')"></div>
                <img class="slide-img" src="/img/museo-etnografico.jpg" alt="Museo etnografico di Rivalta" loading="lazy">
              </div>
              <div class="swiper-slide rsm-slide">
                <div class="slide-bg-blur" style="background-image:url('/img/atlante/mantua-g7c7ae163b_1920.jpg')"></div>
                <img class="slide-img" src="/img/atlante/mantua-g7c7ae163b_1920.jpg" alt="Mantova e il Mincio" loading="lazy">
              </div>
              <div class="swiper-slide rsm-slide">
                <div class="slide-bg-blur" style="background-image:url('/img/atlante/CRED-PARCO-DEL-MINCIO.webp')"></div>
                <img class="slide-img" src="/img/atlante/CRED-PARCO-DEL-MINCIO.webp" alt="Parco del Mincio" loading="lazy">
              </div>
              <div class="swiper-slide rsm-slide">
                <div class="slide-bg-blur" style="background-image:url('/img/atlante/Parco-Naturale-di-Albufera-scaled.jpg')"></div>
                <img class="slide-img" src="/img/atlante/Parco-Naturale-di-Albufera-scaled.jpg" alt="Parco Naturale di Albufera" loading="lazy">
              </div>
              <div class="swiper-slide rsm-slide">
                <div class="slide-bg-blur" style="background-image:url('/img/atlante/paulis-veduta1.webp')"></div>
                <img class="slide-img" src="/img/atlante/paulis-veduta1.webp" alt="Veduta del territorio" loading="lazy">
              </div>
              <div class="swiper-slide rsm-slide">
                <div class="slide-bg-blur" style="background-image:url('/img/atlante/ciclabile-borghetto-peschiera.webp')"></div>
                <img class="slide-img" src="/img/atlante/ciclabile-borghetto-peschiera.webp" alt="Ciclabile Borghetto–Peschiera" loading="lazy">
              </div>
              <div class="swiper-slide rsm-slide">
                <div class="slide-bg-blur" style="background-image:url('/img/atlante/ciclovia-del-mincio-1.webp')"></div>
                <img class="slide-img" src="/img/atlante/ciclovia-del-mincio-1.webp" alt="Ciclovia del Mincio" loading="lazy">
              </div>
              <div class="swiper-slide rsm-slide">
                <div class="slide-bg-blur" style="background-image:url('/img/atlante/Fal%C3%B2%20delle%20befana.jpg')"></div>
                <img class="slide-img" src="/img/atlante/Fal%C3%B2%20delle%20befana.jpg" alt="Falò delle Befana" loading="lazy">
              </div>
              <div class="swiper-slide rsm-slide">
                <div class="slide-bg-blur" style="background-image:url('/img/atlante/taniamuserfloraldesignerverona-taniamuserfloraldesigner-taniamuserflowerdesigneritalia-fioridiloto-fiorituralotomincio-fiorituralotomantova-fioridilotomincio-viaggiispirazione-natura-ibarcaiolidelm.webp')"></div>
                <img class="slide-img" src="/img/atlante/taniamuserfloraldesignerverona-taniamuserfloraldesigner-taniamuserflowerdesigneritalia-fioridiloto-fiorituralotomincio-fiorituralotomantova-fioridilotomincio-viaggiispirazione-natura-ibarcaiolidelm.webp" alt="Fiori di loto sul Mincio" loading="lazy">
              </div>
              <div class="swiper-slide rsm-slide">
                <div class="slide-bg-blur" style="background-image:url('/img/atlante/20190822_104621.jpg')"></div>
                <img class="slide-img" src="/img/atlante/20190822_104621.jpg" alt="Veduta del Mincio" loading="lazy">
              </div>
              <div class="swiper-slide rsm-slide">
                <div class="slide-bg-blur" style="background-image:url('/img/atlante/3eb85a6da2459a72bb33081648900153.webp')"></div>
                <img class="slide-img" src="/img/atlante/3eb85a6da2459a72bb33081648900153.webp" alt="Natura nel Parco del Mincio" loading="lazy">
              </div>
              <div class="swiper-slide rsm-slide">
                <div class="slide-bg-blur" style="background-image:url('/img/atlante/OIP.webp')"></div>
                <img class="slide-img" src="/img/atlante/OIP.webp" alt="Scena del Parco del Mincio" loading="lazy">
              </div>
              <div class="swiper-slide rsm-slide">
                <div class="slide-bg-blur" style="background-image:url('/img/atlante/R.jpg')"></div>
                <img class="slide-img" src="/img/atlante/R.jpg" alt="Veduta del territorio" loading="lazy">
              </div>
            </div>
            <div class="swiper-pagination"></div>
            <button class="swiper-button-prev" aria-label="Foto precedente"></button>
            <button class="swiper-button-next" aria-label="Foto successiva"></button>
          </div>
        </div>

        <div class="rsm-gallery__marquee" aria-hidden="true">
          <div class="rsm-gallery__marquee-track">
            <span>Borgo · <em>Valli</em> · Festa · Luccio · Mincio · <em>Aironi</em> · Canneti · Tramonti · Borgo · <em>Valli</em> · Festa · Luccio · Mincio · <em>Aironi</em> · Canneti · Tramonti ·&nbsp;</span>
            <span>Borgo · <em>Valli</em> · Festa · Luccio · Mincio · <em>Aironi</em> · Canneti · Tramonti · Borgo · <em>Valli</em> · Festa · Luccio · Mincio · <em>Aironi</em> · Canneti · Tramonti ·&nbsp;</span>
          </div>
        </div>
      </div>
    </section>

    <!-- ─────────────────────────────────────────────────────────
         06 · TERRITORIO
         ───────────────────────────────────────────────────────── -->
    <section class="rsm-section rsm-territory" id="territorio" aria-labelledby="territorio-title" data-component="section/territory">
      <div class="rsm-container">
        <div class="rsm-section-head">
          <div data-reveal>
            <p class="rsm-kicker">06 · Atlante vivo · Sito Ramsar</p>
            <h2 class="rsm-h1" id="territorio-title">Le <em>Valli del Mincio</em>, oasi d&rsquo;acqua e silenzio</h2>
          </div>
          <p class="rsm-lead" data-reveal style="--delay: 120ms">Tra Rivalta e Borgo Angeli il fiume rallenta e diventa un mosaico di canneti, chiari e praterie a carice. Una zona umida riconosciuta dalla Convenzione di Ramsar e dalla rete Natura 2000.</p>
        </div>

        <!-- Atlante: foto + dati -->
        <div class="rsm-atlas-grid">
          <article class="rsm-atlas-photo" data-reveal data-parallax="0.05">
            <div class="swiper rsm-swiper rsm-swiper--atlas" role="region" aria-label="Galleria fotografica del territorio">
              <div class="swiper-wrapper">
                <div class="swiper-slide rsm-slide">
                  <div class="slide-bg-blur" style="background-image:url('/img/atlante/mantua-g7c7ae163b_1920.jpg')"></div>
                  <img class="slide-img" src="/img/atlante/mantua-g7c7ae163b_1920.jpg" alt="Mantova e il Mincio" loading="lazy">
                </div>
                <div class="swiper-slide rsm-slide">
                  <div class="slide-bg-blur" style="background-image:url('/img/atlante/CRED-PARCO-DEL-MINCIO.webp')"></div>
                  <img class="slide-img" src="/img/atlante/CRED-PARCO-DEL-MINCIO.webp" alt="Parco del Mincio" loading="lazy">
                </div>
                <div class="swiper-slide rsm-slide">
                  <div class="slide-bg-blur" style="background-image:url('/img/atlante/Parco-Naturale-di-Albufera-scaled.jpg')"></div>
                  <img class="slide-img" src="/img/atlante/Parco-Naturale-di-Albufera-scaled.jpg" alt="Parco Naturale" loading="lazy">
                </div>
                <div class="swiper-slide rsm-slide">
                  <div class="slide-bg-blur" style="background-image:url('/img/atlante/paulis-veduta1.webp')"></div>
                  <img class="slide-img" src="/img/atlante/paulis-veduta1.webp" alt="Veduta del territorio" loading="lazy">
                </div>
                <div class="swiper-slide rsm-slide">
                  <div class="slide-bg-blur" style="background-image:url('/img/atlante/ciclabile-borghetto-peschiera.webp')"></div>
                  <img class="slide-img" src="/img/atlante/ciclabile-borghetto-peschiera.webp" alt="Ciclabile Borghetto–Peschiera" loading="lazy">
                </div>
                <div class="swiper-slide rsm-slide">
                  <div class="slide-bg-blur" style="background-image:url('/img/atlante/ciclovia-del-mincio-1.webp')"></div>
                  <img class="slide-img" src="/img/atlante/ciclovia-del-mincio-1.webp" alt="Ciclovia del Mincio" loading="lazy">
                </div>
                <div class="swiper-slide rsm-slide">
                  <div class="slide-bg-blur" style="background-image:url('/img/atlante/Fal%C3%B2%20delle%20befana.jpg')"></div>
                  <img class="slide-img" src="/img/atlante/Fal%C3%B2%20delle%20befana.jpg" alt="Falò delle Befana" loading="lazy">
                </div>
                <div class="swiper-slide rsm-slide">
                  <div class="slide-bg-blur" style="background-image:url('/img/atlante/taniamuserfloraldesignerverona-taniamuserfloraldesigner-taniamuserflowerdesigneritalia-fioridiloto-fiorituralotomincio-fiorituralotomantova-fioridilotomincio-viaggiispirazione-natura-ibarcaiolidelm.webp')"></div>
                  <img class="slide-img" src="/img/atlante/taniamuserfloraldesignerverona-taniamuserfloraldesigner-taniamuserflowerdesigneritalia-fioridiloto-fiorituralotomincio-fiorituralotomantova-fioridilotomincio-viaggiispirazione-natura-ibarcaiolidelm.webp" alt="Fiori di loto sul Mincio" loading="lazy">
                </div>
                <div class="swiper-slide rsm-slide">
                  <div class="slide-bg-blur" style="background-image:url('/img/Garzetta - Mssimo Brambilla.jpg')"></div>
                  <img class="slide-img" src="/img/Garzetta - Mssimo Brambilla.jpg" alt="Garzetta nel Parco del Mincio" loading="lazy">
                </div>
                <div class="swiper-slide rsm-slide">
                  <div class="slide-bg-blur" style="background-image:url('/img/corte-mincio-storica.jpg')"></div>
                  <img class="slide-img" src="/img/corte-mincio-storica.jpg" alt="Corte Mincio storica" loading="lazy">
                </div>
                <div class="swiper-slide rsm-slide">
                  <div class="slide-bg-blur" style="background-image:url('/img/museo-etnografico.jpg')"></div>
                  <img class="slide-img" src="/img/museo-etnografico.jpg" alt="Museo etnografico di Rivalta" loading="lazy">
                </div>
                <div class="swiper-slide rsm-slide">
                  <div class="slide-bg-blur" style="background-image:url('/img/atlante/20190822_104621.jpg')"></div>
                  <img class="slide-img" src="/img/atlante/20190822_104621.jpg" alt="Veduta del Mincio" loading="lazy">
                </div>
                <div class="swiper-slide rsm-slide">
                  <div class="slide-bg-blur" style="background-image:url('/img/atlante/3eb85a6da2459a72bb33081648900153.webp')"></div>
                  <img class="slide-img" src="/img/atlante/3eb85a6da2459a72bb33081648900153.webp" alt="Natura nel Parco del Mincio" loading="lazy">
                </div>
                <div class="swiper-slide rsm-slide">
                  <div class="slide-bg-blur" style="background-image:url('/img/atlante/OIP.webp')"></div>
                  <img class="slide-img" src="/img/atlante/OIP.webp" alt="Scena del Parco del Mincio" loading="lazy">
                </div>
                <div class="swiper-slide rsm-slide">
                  <div class="slide-bg-blur" style="background-image:url('/img/atlante/R.jpg')"></div>
                  <img class="slide-img" src="/img/atlante/R.jpg" alt="Veduta del territorio" loading="lazy">
                </div>
              </div>
              <div class="swiper-pagination"></div>
              <button class="swiper-button-prev" aria-label="Foto precedente"></button>
              <button class="swiper-button-next" aria-label="Foto successiva"></button>
            </div>
          </article>

          <div class="rsm-atlas-copy" data-reveal style="--delay: 140ms">
            <h3 class="rsm-h2">Acqua, canneti, paese</h3>
            <p class="rsm-lead">Il Mincio rallenta in <strong>oltre 1.000 ettari di acqua, canneti e ninfee</strong>: una riserva tra Rivalta e Mantova riconosciuta da <span class="kw" data-kw="ramsar">Ramsar</span> e dalla rete Natura 2000.</p>
            <div class="rsm-fact-grid">
              <div class="fact"><strong data-count="260">0</strong><span>specie di uccelli censite</span></div>
              <div class="fact"><strong data-count="1100">0</strong><span>ettari di riserva</span></div>
              <div class="fact"><strong data-count="43">0</strong><span>chilometri di Mincio dal Garda al Po</span></div>
              <div class="fact"><strong data-count="172">0</strong><span>paesi firmatari Ramsar</span></div>
            </div>
            <div class="rsm-button-row">
              <a class="rsm-btn rsm-btn--ghost magnetic" href="/storia"><span>Storia di Rivalta</span><span class="rsm-btn-icon" aria-hidden="true">&rarr;</span></a>
            </div>
          </div>
        </div>

        <!-- Esperienze -->
        <div class="rsm-section-head" data-reveal>
          <p class="rsm-kicker">Tre modi per entrare</p>
          <h3 class="rsm-h2">Acqua, bici, osservazione lenta</h3>
        </div>
        <div class="rsm-xp-grid">
          <article class="stack-card rsm-xp-card is-emerald" data-reveal data-component="card/experience">
            <div class="photo-slot" data-photo-slot="experience-boat" style="--photo:url('/img/waterbike-mincio.jpg')" role="img" aria-label="Escursione in barca nelle Valli del Mincio"></div>
            <div class="rsm-xp-body stack-card-copy">
              <h3>Dal livello dell&rsquo;acqua</h3>
              <p>Partenze dagli imbarcaderi, canali stretti, silenzio dei canneti e un punto di vista che cambia il modo di leggere la valle.</p>
              <div class="stack-meta rsm-xp-meta">
                <span class="rsm-tag rsm-tag--emerald">barca</span>
                <span class="rsm-tag rsm-tag--emerald">canoa</span>
                <span class="rsm-tag rsm-tag--emerald">guide locali</span>
              </div>
            </div>
          </article>
          <article class="stack-card rsm-xp-card is-azure" data-reveal style="--delay: 120ms" data-component="card/experience">
            <div class="photo-slot" data-photo-slot="experience-bike" style="--photo:url('/img/mantova-laghi-aerea.jpg')" role="img" aria-label="Ciclabile nei pressi di Rivalta"></div>
            <div class="rsm-xp-body stack-card-copy">
              <h3>La valle a pedali</h3>
              <p>Rivalta &egrave; tappa ideale tra Mantova e Peschiera del Garda, con deviazioni lente verso argini, pontili e scorci d&rsquo;acqua.</p>
              <div class="stack-meta rsm-xp-meta">
                <span class="rsm-tag rsm-tag--azure">cicloturismo</span>
                <span class="rsm-tag rsm-tag--azure">argini</span>
                <span class="rsm-tag rsm-tag--azure">soste</span>
              </div>
            </div>
          </article>
          <article class="stack-card rsm-xp-card is-violet" data-reveal style="--delay: 240ms" data-component="card/experience">
            <div class="photo-slot" data-photo-slot="experience-birdwatching" style="--photo:url('/img/museo-etnografico-tramonto.jpg')" role="img" aria-label="Birdwatching nelle Valli del Mincio"></div>
            <div class="rsm-xp-body stack-card-copy">
              <h3>Aironi, chiari, canneti</h3>
              <p>La zona umida &egrave; un archivio naturale aperto: specie migratrici, nidificazioni, fioriture estive e tracce da osservare con rispetto.</p>
              <div class="stack-meta rsm-xp-meta">
                <span class="rsm-tag rsm-tag--violet">fauna</span>
                <span class="rsm-tag rsm-tag--violet">Ramsar</span>
                <span class="rsm-tag rsm-tag--violet">stagioni</span>
              </div>
            </div>
          </article>
        </div>

      </div>

      <!-- Valli narrative -->
      <article class="rsm-valli" data-reveal>
        <div class="rsm-container">
          <p class="rsm-kicker">Riserva naturale · 1984</p>
          <p class="rsm-valli__drop">C&rsquo;&egrave; un posto in Lombardia dove il tempo si misura in <em>voli di aironi</em>.</p>
          <div class="rsm-valli__row">
            <div class="rsm-valli__fact"><strong data-count="1081">0</strong><span>ettari protetti</span></div>
            <div class="rsm-valli__fact"><strong data-count="260">0</strong><span>specie di uccelli</span></div>
            <div class="rsm-valli__fact"><strong data-count="172">0</strong><span>paesi firmatari Ramsar</span></div>
            <div class="rsm-valli__fact"><strong data-count="2009">0</strong><span>Eden Project — EU Excellence</span></div>
          </div>
          <div class="rsm-valli__summary">
            <article>
              <h3>Paesaggio</h3>
              <p>Quattro comuni si dividono il territorio e otto chilometri di fiume diventano palude, chiari e <span class="kw" data-kw="canneti">canneti</span>. Il cielo occupa tutto, l&rsquo;acqua lo ripete, e il paesaggio resta leggibile anche in poche soste.</p>
            </article>
            <article>
              <h3>Gente d&rsquo;acqua</h3>
              <p>Per secoli qui si viveva di pesca, canne palustri, reti e <span class="kw" data-kw="arelle"><em>arelle</em></span>. Oggi quella memoria sopravvive a <strong>Corte Mincio</strong>, dove il Museo Etnografico conserva mestieri e oggetti reali del borgo.</p>
            </article>
          </div>
        </div>
      </article>
    </section>

    <!-- ─────────────────────────────────────────────────────────
         07 · CONTATTI
         ───────────────────────────────────────────────────────── -->
    <section class="rsm-section rsm-contact" id="contatti" aria-labelledby="contatti-title" data-component="section/contact">
      <div class="rsm-container">
        <div class="rsm-section-head">
          <div data-reveal>
            <p class="rsm-kicker">07 · Dove siamo</p>
            <h2 class="rsm-h1" id="contatti-title">Arrivare a Rivalta &egrave; gi&agrave; <em>parte del viaggio</em></h2>
          </div>
          <p class="rsm-lead" data-reveal style="--delay: 120ms">Rivalta sul Mincio &egrave; frazione del Comune di Rodigo, a circa 10 km da Mantova. L&rsquo;ufficio turistico Pro Loco si trova in Via Porto, 31.</p>
        </div>

        <div class="rsm-contact__grid">
          <figure class="rsm-contact__map" data-reveal data-component="component/map-plate">
            <div class="swiper rsm-swiper rsm-swiper--map" role="region" aria-label="Carosello del territorio">
              <div class="swiper-wrapper">
                <div class="swiper-slide rsm-slide">
                  <video src="/video/dove-siamo-1.mp4" muted loop autoplay playsinline preload="metadata" aria-label="Veduta animata di Rivalta"></video>
                </div>
              </div>
              <div class="swiper-pagination"></div>
              <button class="swiper-button-prev" aria-label="Slide precedente"></button>
              <button class="swiper-button-next" aria-label="Slide successiva"></button>
            </div>
          </figure>
          <article class="rsm-contact__card" data-reveal style="--delay: 120ms" data-component="card/contact">
            <div>
              <p class="rsm-kicker">Pro Loco · Ufficio turistico</p>
              <h3 class="rsm-h2" style="margin-top:10px">Via Porto, 31</h3>
              <p class="rsm-lead" style="margin-top:10px">46040 Rivalta sul Mincio, frazione di Rodigo, provincia di Mantova.</p>
            </div>
            <div class="rsm-contact__lines">
              <div class="rsm-contact__line"><strong>Email</strong><a href="mailto:info@prolocorivalta.mn.it">info@prolocorivalta.mn.it</a></div>
              <div class="rsm-contact__line"><strong>Telefono</strong><a href="tel:+393398995680">+39 339 899 5680</a></div>
              <div class="rsm-contact__line"><strong>Accessi</strong><span>Auto dalla SP 18, bici lungo la ciclabile del Mincio, barca dagli imbarcaderi della valle.</span></div>
              <div class="rsm-contact__line"><strong>Social</strong><span><a href="https://www.facebook.com/prolocoamicidirivalta/" target="_blank" rel="noopener">Facebook</a> &middot; <a href="https://www.instagram.com/prolocrivalta" target="_blank" rel="noopener">Instagram</a></span></div>
            </div>
            <div class="rsm-button-row">
              <a class="rsm-btn rsm-btn--brand magnetic" href="https://www.google.com/maps/dir/?api=1&destination=Via+Porto+31,+46040+Rivalta+sul+Mincio+MN" target="_blank" rel="noopener">
                <span>Apri percorso su Maps</span><span class="rsm-btn-icon" aria-hidden="true">&rarr;</span>
              </a>
              <button class="rsm-btn rsm-btn--ghost magnetic" type="button" data-modal-open="newsletter-modal">
                <span>Iscriviti alla newsletter</span><span class="rsm-btn-icon" aria-hidden="true">&rarr;</span>
              </button>
            </div>
          </article>
        </div>

      </div>

      <!-- Newsletter inline -->
      <div class="rsm-newsletter" data-reveal style="--delay: 120ms">
        <div class="rsm-container">
          <div class="rsm-newsletter__grid">
            <div>
              <p class="rsm-kicker">Resta in contatto</p>
              <h2>Il <em>Mincio</em> nella tua casella di posta</h2>
              <p>Una mail al mese con eventi, sagre, escursioni e racconti dalle Valli del Mincio. Niente rumore, solo cose utili.</p>
            </div>
            <form class="rsm-field" id="newsletter-form" novalidate>
              <label for="newsletter-email">Email</label>
              <input class="rsm-input rsm-input--lg" id="newsletter-email" type="email" name="email" autocomplete="email" placeholder="nome@email.it" required />
              <p class="rsm-form-status" id="newsletter-status" role="status"></p>
              <button class="rsm-btn rsm-btn--brand magnetic" type="submit" style="margin-top:6px"><span>Iscrivimi</span><span class="rsm-btn-icon" aria-hidden="true">&rarr;</span></button>
              <p class="privacy-note">Iscrivendoti accetti la <a href="/privacy">privacy policy</a>. Puoi disiscriverti in qualsiasi momento.</p>
            </form>
          </div>
        </div>
      </div>
    </section>
  </main>

  <!-- ─────────────────────────────────────────────────────────
       MODAL · Newsletter / Promemoria evento
       ───────────────────────────────────────────────────────── -->
  <div class="rsm-modal" id="newsletter-modal" role="dialog" aria-modal="true" aria-labelledby="newsletter-modal-title" hidden>
    <div class="rsm-modal__panel">
      <button class="rsm-modal__close" type="button" data-modal-close aria-label="Chiudi">×</button>
      <p class="rsm-kicker">Pro Loco · Newsletter</p>
      <h3 class="rsm-modal__title" id="newsletter-modal-title">Ti avvisiamo noi quando inizia</h3>
      <p class="rsm-lead" style="margin-bottom:20px">Lasciaci la tua email: ti scriviamo solo per la Festa del Pesce e per gli eventi importanti della Pro Loco.</p>
      <form class="rsm-field" id="newsletter-modal-form" novalidate>
        <label for="newsletter-modal-email">Email</label>
        <input class="rsm-input" id="newsletter-modal-email" type="email" placeholder="nome@email.it" required />
        <p class="rsm-form-status" id="newsletter-modal-status" role="status"></p>
        <button class="rsm-btn rsm-btn--brand magnetic" type="submit" style="margin-top:10px"><span>Avvisami</span><span class="rsm-btn-icon" aria-hidden="true">&rarr;</span></button>
      </form>
    </div>
  </div>

<!--PARTIAL:footer-->

  <a class="back-to-top" href="#intro" aria-label="Torna su">↑</a>

  <script src="https://cdn.jsdelivr.net/npm/@popperjs/core@2/dist/umd/popper.min.js"></script>
  <script src="https://cdn.jsdelivr.net/npm/tippy.js@6/dist/tippy.umd.min.js"></script>
  <script src="https://cdn.jsdelivr.net/npm/splitting@1.0.6/dist/splitting.min.js"></script>
  <script src="https://cdn.jsdelivr.net/npm/swiper@11/swiper-bundle.min.js"></script>
  <script src="/node_modules/animejs/dist/bundles/anime.umd.min.js"></script>
  <script src="https://cdn.jsdelivr.net/npm/@supabase/supabase-js@2/dist/umd/supabase.min.js"></script>
  <script src="/supabase.config.js"></script>
  <script src="https://cdn.jsdelivr.net/npm/gsap@3.12.5/dist/gsap.min.js"></script>
  <script src="https://cdn.jsdelivr.net/npm/gsap@3.12.5/dist/ScrollTrigger.min.js"></script>
  <script src="https://cdn.jsdelivr.net/npm/@studio-freight/lenis@1.0.29/dist/lenis.min.js"></script>
  <script>
  (function () {
    'use strict';

    var reduceMotion = window.matchMedia && window.matchMedia('(prefers-reduced-motion: reduce)').matches;
    var CATEGORY_ORDER = [
      'Ambiente', 'Anniversari', 'Assemblea', 'Canoa', 'Ciclismo',
      'Festa del Pesce', 'Iniziative', 'Love-luccio', 'Mincio-art',
      'Natale', 'Rassegna Stampa', 'Tesseramento', 'Video'
    ];
    var LEGACY_TO_CANONICAL = {
      'ambiente': 'Ambiente', 'anniversari': 'Anniversari', 'assemblea': 'Assemblea',
      'canoa': 'Canoa', 'ciclismo': 'Ciclismo', 'festa del pesce': 'Festa del Pesce',
      'festa-del-pesce': 'Festa del Pesce', 'iniziative': 'Iniziative',
      'love-luccio': 'Love-luccio', 'mincio-art': 'Mincio-art', 'natale': 'Natale',
      'rassegna stampa': 'Rassegna Stampa', 'rassegna-stampa': 'Rassegna Stampa',
      'tesseramento': 'Tesseramento', 'video': 'Video',
      'eventi': 'Iniziative', 'territorio': 'Ambiente', 'vita locale': 'Iniziative',
      'avvisi': 'Iniziative', 'richieste': 'Iniziative', 'storie': 'Iniziative',
      'il luccio': 'Iniziative', 'fauna': 'Ambiente', 'ecoturismo': 'Ambiente',
      'storia': 'Iniziative'
    };
    var BADGE_COLOR_BY_CATEGORY = {
      'Ambiente': 'default', 'Anniversari': 'green', 'Assemblea': 'default',
      'Canoa': 'green', 'Ciclismo': 'default', 'Festa del Pesce': 'green',
      'Iniziative': 'green', 'Love-luccio': 'green', 'Mincio-art': 'default',
      'Natale': 'green', 'Rassegna Stampa': 'default', 'Tesseramento': 'default',
      'Video': 'default'
    };

    function $(selector, root) { return (root || document).querySelector(selector); }
    function $all(selector, root) { return Array.prototype.slice.call((root || document).querySelectorAll(selector)); }

    function escapeHtml(value) {
      return String(value || '')
        .replace(/&/g, '&amp;').replace(/</g, '&lt;').replace(/>/g, '&gt;')
        .replace(/"/g, '&quot;').replace(/'/g, '&#39;');
    }

    function canonicalCategory(input) {
      var raw = String(input || '').trim();
      if (!raw) return '';
      var normalized = raw.toLowerCase();
      if (LEGACY_TO_CANONICAL[normalized]) return LEGACY_TO_CANONICAL[normalized];
      for (var i = 0; i < CATEGORY_ORDER.length; i += 1) {
        if (CATEGORY_ORDER[i].toLowerCase() === normalized) return CATEGORY_ORDER[i];
      }
      return raw;
    }

    function badgeColor(input) { return BADGE_COLOR_BY_CATEGORY[canonicalCategory(input)] || 'default'; }
    function categoryHref(input) { return 'category?name=' + encodeURIComponent(canonicalCategory(input) || ''); }

    window.RSM_CATEGORIES = {
      all: function () { return CATEGORY_ORDER.slice(); },
      canonical: canonicalCategory, badgeColor: badgeColor, categoryHref: categoryHref
    };

    /* ─── Reveal on scroll ─── */
    function initReveal() {
      var items = $all('[data-reveal]');
      if (!items.length) return;
      if (reduceMotion || !('IntersectionObserver' in window)) {
        items.forEach(function (item) { item.classList.add('is-visible'); });
        return;
      }
      var observer = new IntersectionObserver(function (entries) {
        entries.forEach(function (entry) {
          if (entry.isIntersecting) {
            entry.target.classList.add('is-visible');
            observer.unobserve(entry.target);
          }
        });
      }, { threshold: 0.14 });
      items.forEach(function (item) { observer.observe(item); });
    }

    /* ─── Counters ─── */
    function initCounters() {
      var counters = $all('[data-count]');
      if (!counters.length) return;
      if (reduceMotion || !('IntersectionObserver' in window)) {
        counters.forEach(function (el) { el.textContent = el.dataset.count || '0'; });
        return;
      }
      var observer = new IntersectionObserver(function (entries) {
        entries.forEach(function (entry) {
          if (!entry.isIntersecting) return;
          var el = entry.target;
          var target = parseInt(el.dataset.count, 10) || 0;
          var start = performance.now();
          var duration = 1300;
          function frame(now) {
            var t = Math.min((now - start) / duration, 1);
            var eased = 1 - Math.pow(1 - t, 3);
            el.textContent = String(Math.round(target * eased));
            if (t < 1) requestAnimationFrame(frame);
          }
          requestAnimationFrame(frame);
          observer.unobserve(el);
        });
      }, { threshold: 0.45 });
      counters.forEach(function (el) { observer.observe(el); });
    }

    /* ─── Magnetic buttons ─── */
    function initMagneticButtons() {
      $all('.magnetic').forEach(function (button) {
        button.addEventListener('pointermove', function (event) {
          var rect = button.getBoundingClientRect();
          var x = ((event.clientX - rect.left) / rect.width) * 100;
          var y = ((event.clientY - rect.top) / rect.height) * 100;
          button.style.setProperty('--mx', x + '%');
          button.style.setProperty('--my', y + '%');
        });
      });
    }

    /* ─── Newsletter form (inline + modal) ─── */
    function bindNewsletter(formId, inputId, statusId) {
      var form = $('#' + formId);
      var input = $('#' + inputId);
      var status = $('#' + statusId);
      if (!form || !input || !status) return;
      form.addEventListener('submit', function (event) {
        event.preventDefault();
        var value = input.value.trim();
        if (!value || !/^[^\s@]+@[^\s@]+\.[^\s@]+$/.test(value)) {
          status.textContent = 'Inserisci un indirizzo email valido.';
          status.style.color = 'var(--rsm-danger)';
          input.focus();
          return;
        }
        status.textContent = 'Grazie, iscrizione registrata in questa sessione.';
        status.style.color = 'var(--rsm-success)';
        form.reset();
      });
    }
    function initNewsletter() {
      bindNewsletter('newsletter-form', 'newsletter-email', 'newsletter-status');
      bindNewsletter('newsletter-modal-form', 'newsletter-modal-email', 'newsletter-modal-status');
    }

    /* ─── Modal handling ─── */
    function initModals() {
      $all('[data-modal-open]').forEach(function (trigger) {
        trigger.addEventListener('click', function () {
          var id = trigger.getAttribute('data-modal-open');
          var modal = document.getElementById(id);
          if (!modal) return;
          modal.removeAttribute('hidden');
          requestAnimationFrame(function () { modal.classList.add('is-open'); });
          var firstInput = modal.querySelector('input, button:not([data-modal-close])');
          if (firstInput) setTimeout(function(){ firstInput.focus(); }, 200);
        });
      });
      function close(modal) {
        modal.classList.remove('is-open');
        setTimeout(function () { modal.setAttribute('hidden', ''); }, 360);
      }
      $all('.rsm-modal').forEach(function (modal) {
        modal.addEventListener('click', function (e) {
          if (e.target === modal || (e.target.closest && e.target.closest('[data-modal-close]'))) {
            close(modal);
          }
        });
      });
      document.addEventListener('keydown', function (e) {
        if (e.key !== 'Escape') return;
        $all('.rsm-modal.is-open').forEach(close);
      });
    }

    /* ─── Carousels (Swiper) ─── */
    function initCarousel() {
      if (typeof Swiper === 'undefined') return;
      document.querySelectorAll('.rsm-swiper:not(.rsm-swiper--hero)').forEach(function (el) {
        var slideCount = el.querySelectorAll('.swiper-slide').length;
        var hasMultipleSlides = slideCount > 1;
        new Swiper(el, {
          loop: hasMultipleSlides, grabCursor: true, spaceBetween: 20,
          effect: 'fade', fadeEffect: { crossFade: true },
          autoplay: hasMultipleSlides ? { delay: 4500, disableOnInteraction: false } : false,
          pagination: { el: el.querySelector('.swiper-pagination'), clickable: true, dynamicBullets: true },
          navigation: { nextEl: el.querySelector('.swiper-button-next'), prevEl: el.querySelector('.swiper-button-prev') },
          keyboard: { enabled: true }
        });
      });
    }

    function initHeroVideoSwiper() {
      var heroEl = document.querySelector('.rsm-swiper--hero');
      if (!heroEl || typeof Swiper === 'undefined') return;
      new Swiper(heroEl, {
        loop: true, grabCursor: true,
        effect: 'fade', fadeEffect: { crossFade: true },
        autoplay: { delay: 4200, disableOnInteraction: false },
        pagination: { el: heroEl.querySelector('.swiper-pagination'), clickable: true, dynamicBullets: true },
        navigation: { nextEl: heroEl.querySelector('.swiper-button-next'), prevEl: heroEl.querySelector('.swiper-button-prev') },
        keyboard: { enabled: true }
      });
    }

    /* ─── Helpers per news ─── */
    function isValidHttpsUrl(url) {
      if (!url || typeof url !== 'string') return false;
      try { var u = new URL(url); return u.protocol === 'https:'; } catch (_) { return false; }
    }
    function newsSeed(post) {
      var type = String(post.image_type || post.category || 'territorio').toLowerCase().replace(/[^a-z0-9]+/g, '-');
      return type || 'territorio';
    }
    function newsPhoto(mediaClass, slot, seed, imageUrl) {
      var url = (isValidHttpsUrl(imageUrl) ? imageUrl : null) ||
                ('https://picsum.photos/seed/rivalta-news-' + encodeURIComponent(seed) + '/900/680');
      return '<div class="' + escapeHtml(mediaClass) + ' news-card__media--photo photo-slot" data-photo-slot="' + escapeHtml(slot) + '" style="--photo:none" aria-hidden="true"><img class="news-card__img" src="' + escapeHtml(url) + '" alt="" loading="lazy" decoding="async" /></div>';
    }

    /* ─── Posts (Supabase) ─── */
    function initPosts() {
      var status = $('#news-status');
      var filterRow = $('#news-filter-row');
      var featured = $('#post-featured');
      var grid = $('#posts-grid');
      var empty = $('#news-empty');
      if (!status || !filterRow || !featured || !grid || !empty) return;

      var categories = CATEGORY_ORDER.slice();
      var activeCategory = 'all';
      var posts = [];

      function setStatus(message) {
        status.textContent = message || '';
        status.style.display = message ? '' : 'none';
      }
      function fmt(iso) {
        if (!iso) return '';
        return new Date(iso).toLocaleDateString('it-IT', { day: 'numeric', month: 'short', year: 'numeric' });
      }
      function badgeClass(color) { return color === 'green' ? 'badge green' : 'badge'; }
      function normalize(post) {
        var category = canonicalCategory(post.category) || 'Territorio';
        return Object.assign({}, post, { category: category, badge_color: post.badge_color || badgeColor(category) });
      }
      function filteredPosts() {
        if (activeCategory === 'all') return posts;
        return posts.filter(function (post) { return post.category === activeCategory; });
      }
      function renderFilters() {
        var chips = ['all'].concat(categories);
        filterRow.innerHTML = chips.map(function (cat) {
          var label = cat === 'all' ? 'Tutte' : cat;
          var on = activeCategory === cat ? ' on' : '';
          return '<button type="button" class="news-filter-chip' + on + '" data-cat="' + escapeHtml(cat) + '">' + escapeHtml(label) + '</button>';
        }).join('');
        $all('[data-cat]', filterRow).forEach(function (button) {
          button.addEventListener('click', function () {
            activeCategory = button.getAttribute('data-cat') || 'all';
            renderFilters(); renderPosts();
          });
        });
      }
      function cardMarkup(post) {
        var seed = newsSeed(post);
        return '<article class="post-card">' +
          newsPhoto('news-card__media', 'news-card-' + seed, seed, post.image_url) +
          '<div class="post-card__body">' +
          '<div class="post-meta"><time>' + escapeHtml(fmt(post.published_at)) + '</time><a href="' + categoryHref(post.category) + '">' + escapeHtml(post.category) + '</a></div>' +
          '<h3 class="post-card__title"><a href="post?id=' + encodeURIComponent(post.id) + '">' + escapeHtml(post.title || 'Articolo') + '</a></h3>' +
          '<p class="post-card__excerpt">' + escapeHtml(post.excerpt || '') + '</p>' +
          '<div class="post-card__footer"><a class="btn-read" href="post?id=' + encodeURIComponent(post.id) + '">Leggi <span aria-hidden="true">&rarr;</span></a><span class="' + badgeClass(post.badge_color) + '">' + escapeHtml(post.badge || post.category) + '</span></div>' +
          '</div></article>';
      }
      function renderPosts() {
        var list = filteredPosts();
        featured.style.display = 'none';
        grid.innerHTML = '';
        if (!list.length) {
          empty.style.display = '';
          setStatus(activeCategory === 'all' ? '' : 'Nessun articolo per la categoria selezionata.');
          return;
        }
        empty.style.display = 'none'; setStatus('');
        var first = list[0]; var seed = newsSeed(first);
        featured.classList.remove('skeleton');
        featured.innerHTML =
          newsPhoto('post-featured__media', 'news-featured-' + seed, seed, first.image_url) +
          '<div class="post-featured__body">' +
          '<div class="post-meta"><time>' + escapeHtml(fmt(first.published_at)) + '</time><a href="' + categoryHref(first.category) + '">' + escapeHtml(first.category) + '</a></div>' +
          '<h2 class="post-featured__title"><a href="post?id=' + encodeURIComponent(first.id) + '">' + escapeHtml(first.title || 'Articolo') + '</a></h2>' +
          '<p class="post-featured__excerpt">' + escapeHtml(first.excerpt || '') + '</p>' +
          '<div class="post-featured__footer"><a class="btn-read" href="post?id=' + encodeURIComponent(first.id) + '">Leggi <span aria-hidden="true">&rarr;</span></a><span class="' + badgeClass(first.badge_color) + '">' + escapeHtml(first.badge || first.category) + '</span></div>' +
          '</div>';
        featured.style.display = '';
        grid.innerHTML = list.slice(1, 7).map(cardMarkup).join('');
      }
      renderFilters();

      if (typeof supabase === 'undefined' || typeof SUPABASE_URL === 'undefined' || SUPABASE_URL.indexOf('your-project') >= 0) {
        posts = []; renderPosts();
        setStatus('Archivio non collegato. Quando Supabase risponde, le news appariranno qui.');
        return;
      }
      var client = supabase.createClient(SUPABASE_URL, SUPABASE_ANON_KEY);
      client.from('posts')
        .select('id, title, excerpt, category, badge, badge_color, image_type, image_url, published_at')
        .eq('published', true).order('published_at', { ascending: false }).limit(60)
        .then(function (result) {
          if (result.error) { posts = []; renderPosts(); setStatus('Non riesco a caricare le news in questo momento.'); return; }
          posts = (result.data || []).map(normalize); renderPosts();
        });
    }

    /* ─── GSAP scroll triggers + Lenis smooth scroll ─── */
    function initGsap() {
      if (reduceMotion || typeof gsap === 'undefined' || typeof ScrollTrigger === 'undefined') return;
      gsap.registerPlugin(ScrollTrigger);

      /* Lenis smooth scroll */
      if (typeof Lenis !== 'undefined') {
        var lenis = new Lenis({ lerp: 0.1, smoothWheel: true });
        lenis.on('scroll', ScrollTrigger.update);
        gsap.ticker.add(function (time) { lenis.raf(time * 1000); });
        gsap.ticker.lagSmoothing(0);
      }

      /* Banner video parallax + overlay fade */
      var bannerVideo = $('.rsm-banner__video');
      if (bannerVideo) {
        gsap.fromTo(bannerVideo, { scale: 1.1 }, {
          scale: 0.95, ease: 'none',
          scrollTrigger: { trigger: '.rsm-banner', start: 'top top', end: 'bottom top', scrub: 1 }
        });
      }
      var bannerOverlay = $('.rsm-banner__overlay');
      if (bannerOverlay) {
        gsap.fromTo(bannerOverlay, { opacity: 0.55 }, {
          opacity: 0.78, ease: 'none',
          scrollTrigger: { trigger: '.rsm-banner', start: 'top top', end: 'bottom top', scrub: 1.2 }
        });
      }

      /* Section backgrounds: color wash al enter/leave */
      var sections = [
        { el: '.rsm-festa', color1: 'rgba(31,191,125,0.05)', color2: 'rgba(31,191,125,0.12)' },
        { el: '.rsm-events', color1: 'rgba(139,92,246,0.04)', color2: 'rgba(139,92,246,0.10)' },
        { el: '.rsm-gallery', color1: 'rgba(74,155,255,0.03)', color2: 'rgba(74,155,255,0.09)' },
        { el: '.rsm-territory', color1: 'rgba(31,191,125,0.04)', color2: 'rgba(31,191,125,0.12)' },
        { el: '.rsm-contact', color1: 'rgba(74,155,255,0.04)', color2: 'rgba(74,155,255,0.10)' }
      ];
      sections.forEach(function (sec) {
        var el = $(sec.el);
        if (el) {
          gsap.fromTo(el, { filter: 'brightness(1)' }, {
            filter: 'brightness(1.02)', ease: 'none',
            scrollTrigger: { trigger: el, start: 'top 60%', end: 'top -60%', scrub: 1 }
          });
        }
      });

      /* Parallax profondo su elementi dati */
      gsap.utils.toArray('[data-parallax]').forEach(function (el) {
        var amount = parseFloat(el.getAttribute('data-parallax') || '0.1') * 240;
        gsap.to(el, { y: amount, ease: 'none',
          scrollTrigger: { trigger: el, start: 'top bottom', end: 'bottom top', scrub: 1.2 }
        });
      });

      /* Photo slots: zoom-pan su scroll */
      gsap.utils.toArray('.photo-slot').forEach(function (slot) {
        gsap.fromTo(slot, { scale: 0.96 }, {
          scale: 1.08, ease: 'none',
          scrollTrigger: { trigger: slot, start: 'top bottom', end: 'bottom top', scrub: 1.4 }
        });
      });

      /* Gallery stage: scale + Y parallax cinematico */
      var galleryStage = $('.rsm-gallery__stage');
      if (galleryStage) {
        gsap.fromTo(galleryStage, { scale: 0.94, opacity: 0.8 }, {
          scale: 1.02, opacity: 1, ease: 'none',
          scrollTrigger: { trigger: galleryStage, start: 'top 70%', end: 'top 30%', scrub: 1.5 }
        });
      }

      /* Territory swiper: slow horizontal pan effect */
      var atlasPhoto = $('.rsm-atlas-photo');
      if (atlasPhoto) {
        gsap.to(atlasPhoto, { x: 40, ease: 'none',
          scrollTrigger: { trigger: atlasPhoto, start: 'top bottom', end: 'bottom top', scrub: 1.8 }
        });
      }

      /* Festa menu items: stagger entrance on scroll */
      var menuItems = $all('.rsm-festa__menu-list li');
      if (menuItems.length) {
        gsap.fromTo(menuItems, { opacity: 0, x: -40 }, {
          opacity: 1, x: 0, ease: 'power2.out',
          scrollTrigger: { trigger: '.rsm-festa__grid', start: 'top 65%', end: 'top 35%' },
          stagger: 0.08
        });
      }

      /* Festa dates: bottom entrance */
      var dateItems = $all('.rsm-festa__day');
      if (dateItems.length) {
        gsap.fromTo(dateItems, { opacity: 0, y: 20 }, {
          opacity: 1, y: 0, ease: 'back.out(1.4)',
          scrollTrigger: { trigger: '.rsm-festa__dates', start: 'top 65%', end: 'top 35%' },
          stagger: 0.05
        });
      }

      /* Experience cards: 3D tilt on scroll */
      var xpCards = $all('.rsm-xp-card');
      if (xpCards.length) {
        gsap.fromTo(xpCards, { rotateY: -5, opacity: 0.8 }, {
          rotateY: 0, opacity: 1, ease: 'none',
          scrollTrigger: { trigger: '.rsm-xp-grid', start: 'top 70%', end: 'top 30%', scrub: 1 },
          stagger: 0.1
        });
      }

      /* Back-to-top button: fade on scroll position */
      var backToTop = $('.back-to-top');
      if (backToTop) {
        gsap.to(backToTop, {
          opacity: 0, pointerEvents: 'none',
          scrollTrigger: { trigger: 'body', start: 'top bottom', onUpdate: function (self) {
            backToTop.style.opacity = self.getVelocity() > 0 && window.scrollY > window.innerHeight ? 1 : 0;
            backToTop.style.pointerEvents = self.getVelocity() > 0 && window.scrollY > window.innerHeight ? 'auto' : 'none';
          }}
        });
      }

      ScrollTrigger.refresh();
    }

    /* ─── Anime.js entrance + hover + banner effects ─── */
    function initAnimeJs() {
      if (reduceMotion || typeof anime === 'undefined' || typeof anime.animate !== 'function') return;

      /* Nav entrance */
      anime.animate('.nav-island', { translateY: [-28, 0], opacity: [0, 1], duration: 880, ease: 'out(4)' });

      /* Banner entrance: all elements cascade */
      anime.animate('.rsm-banner__kicker', {
        translateY: [20, 0], opacity: [0, 1],
        duration: 700, delay: 200, ease: 'out(3)'
      });
      anime.animate('.rsm-banner__title', {
        translateY: [40, 0], scale: [0.92, 1], opacity: [0, 1],
        duration: 1100, delay: 300, ease: 'out(4)'
      });
      anime.animate('.rsm-banner__sub', {
        translateY: [20, 0], opacity: [0, 0.8],
        duration: 800, delay: 600, ease: 'out(3)'
      });
      anime.animate('.rsm-banner__cta', {
        scale: [0.85, 1], opacity: [0, 1],
        duration: 600, delay: 800, ease: 'out(4)'
      });
      anime.animate('.rsm-banner__scroll-hint', {
        opacity: [0, 1],
        duration: 1200, delay: 1200, ease: 'out(2)'
      });

      /* Hero section entrance */
      anime.animate('.rsm-hero__main > *', {
        translateY: [30, 0], opacity: [0, 1],
        delay: anime.stagger(95, { start: 140 }),
        duration: 900, ease: 'out(3)'
      });

      anime.animate('.rsm-hero__media', {
        translateY: [22, 0], scale: [0.97, 1], opacity: [0, 1],
        duration: 1000, delay: 220, ease: 'out(4)'
      });

      anime.animate('.rsm-hero__metrics .rsm-metric', {
        translateY: [16, 0], opacity: [0, 1],
        delay: anime.stagger(110, { start: 480 }),
        duration: 760, ease: 'out(3)'
      });

      /* Gallery marquee: breathing pulsing loop */
      anime.animate('.rsm-gallery__marquee-track', {
        opacity: [0.4, 1], duration: 1700,
        direction: 'alternate', loop: true, ease: 'inOut(2)'
      });

      /* Festa poster: spin + scale entrance */
      anime.animate('.rsm-festa__poster', {
        rotate: [-8, 0], scale: [0.9, 1], opacity: [0, 1],
        duration: 1100, ease: 'out(4)'
      });

      /* Section headings: stagger words */
      var sectionHeadings = $all('.rsm-section-head h2');
      sectionHeadings.forEach(function (heading) {
        if (heading.closest('[data-reveal]')) return;
        var words = heading.querySelectorAll('.word');
        if (words.length) {
          anime.fromTo(words, { opacity: 0, translateY: 12 }, {
            opacity: 1, translateY: 0, duration: 600,
            delay: anime.stagger(40, { start: 200 }), ease: 'out(3)'
          });
        }
      });

      /* Interactive cards: rich hover effects */
      var interactiveCards = $all('.stack-card, .post-card, .rsm-card, .festa-day, .rsm-festa__poster, .rsm-valli');
      interactiveCards.forEach(function (card) {
        card.addEventListener('pointerenter', function () {
          anime.animate(card, { scale: 1.015, translateY: -6, duration: 420, ease: 'out(3)' });
        });
        card.addEventListener('pointerleave', function () {
          anime.animate(card, { scale: 1, translateY: 0, duration: 480, ease: 'out(3)' });
        });
      });

      /* Newsletter form: reveal on view */
      var newsletterForm = $('.rsm-newsletter');
      if (newsletterForm) {
        anime.animate(newsletterForm, {
          scale: [0.92, 1], opacity: [0, 1],
          duration: 900, ease: 'out(4)', delay: 200
        });
      }
    }

    /* ─── Splitting.js: typewriter sui titoli ─── */
    function initSplitting() {
      if (typeof Splitting !== 'function' || !window.gsap) return;
      Splitting({ target: '.rsm-display-1', by: 'words' });
      Splitting({ target: '.rsm-festa__title', by: 'chars' });
      Splitting({ target: '.rsm-valli__drop', by: 'words' });

      var io = new IntersectionObserver(function (entries) {
        entries.forEach(function (entry) {
          if (!entry.isIntersecting) return;
          var el = entry.target;
          var chars = el.querySelectorAll('.char');
          var words = el.querySelectorAll('.word');
          if (chars.length) {
            gsap.fromTo(chars,
              { opacity: 0, y: 14, rotateX: -40 },
              { opacity: 1, y: 0, rotateX: 0, duration: 0.45, stagger: 0.026, ease: 'power3.out', clearProps: 'transform' });
          } else if (words.length) {
            gsap.fromTo(words,
              { opacity: 0, y: 8 },
              { opacity: 1, y: 0, duration: 0.55, stagger: 0.07, ease: 'power2.out', clearProps: 'transform' });
          }
          io.unobserve(el);
        });
      }, { threshold: 0.25 });

      document.querySelectorAll('.rsm-display-1, .rsm-festa__title, .rsm-valli__drop').forEach(function (el) {
        io.observe(el);
      });
    }

    /* ─── Tippy popups (.kw) ─── */
    var KW_FALLBACK = {
      'lago-di-garda':    { title: 'Lago di Garda',                   body: 'Il Mincio nasce dal suo angolo meridionale e alimenta le Valli.' },
      'anno-1984':        { title: 'Riserva dal 1984',                body: 'Istituita con D.C.R. 1739 dell\'11 ottobre 1984, superficie 1.081 ha.' },
      'ramsar':           { title: 'Convenzione di Ramsar',           body: 'Trattato del 1971 che protegge le zone umide di importanza internazionale.' },
      'uccelli-acquatici':{ title: 'Uccelli acquatici',                body: 'Oltre 260 specie censite nelle Valli: aironi, garzette, nitticore, svassi.' },
      'pianura-padana':   { title: 'Pianura Padana',                   body: 'Le Valli del Mincio sono una delle ultime oasi umide intatte della pianura.' },
      'canneti':          { title: 'Canneto (Phragmites australis)',   body: 'Specie dominante: crea habitat per nidificazione di aironi e tarabusi.' },
      'canne-palustri':   { title: 'Raccolta delle canne',             body: 'Fino agli anni 60 le canne venivano tagliate e vendute per coperture.' },
      'arelle':           { title: 'Arelle',                            body: 'Stuoie intrecciate con giunchi, documentate al Museo Etnografico di Corte Mincio.' },
      'martin-pescatore': { title: 'Martin pescatore (Alcedo atthis)',  body: 'Specie protetta, indicatore di qualità dell\'acqua.' }
    };
    var _kwClientCache = Object.create(null);
    function buildKwPopupHtml(key, data) {
      var fb = KW_FALLBACK[key] || {};
      var title = fb.title || key;
      var html = '<strong class="kw-pop-title">' + title + '</strong>';
      if (data && data.wiki) html += '<p class="kw-pop-wiki">' + data.wiki + '</p>';
      else if (fb.body)     html += '<p class="kw-pop-wiki">' + fb.body + '</p>';
      if (data && data.claude) html += '<p class="kw-pop-claude"><span class="kw-pop-label">Sul Mincio</span>' + data.claude + '</p>';
      return html;
    }
    function initKwPopups() {
      if (typeof tippy !== 'function') return;
      document.querySelectorAll('.kw[data-kw]').forEach(function (el) {
        var key = el.dataset.kw;
        var label = el.textContent.trim();
        tippy(el, {
          content: '<span class="kw-loading">· · ·</span>',
          allowHTML: true, theme: 'rsm', placement: 'top', maxWidth: 320,
          delay: [200, 80], touch: ['hold', 500], interactive: false,
          onShow: function (i) {
            if (_kwClientCache[key]) { i.setContent(_kwClientCache[key]); return; }
            var url = '/api/kw-context?kw=' + encodeURIComponent(key) + '&title=' + encodeURIComponent(label);
            fetch(url)
              .then(function (r) { return r.ok ? r.json() : null; })
              .then(function (data) { var html = buildKwPopupHtml(key, data); _kwClientCache[key] = html; i.setContent(html); })
              .catch(function () { var html = buildKwPopupHtml(key, null); _kwClientCache[key] = html; i.setContent(html); });
          }
        });
      });
    }

    /* ─── DOMContentLoaded ─── */
    document.addEventListener('DOMContentLoaded', function () {
      var year = $('#year');
      if (year) year.textContent = String(new Date().getFullYear());
      initReveal();
      initCounters();
      initMagneticButtons();
      initNewsletter();
      initModals();
      initCarousel();
      initHeroVideoSwiper();
      initPosts();
      initGsap();
      initAnimeJs();
      initKwPopups();
      initSplitting();
    });
  })();
  </script>

  <!-- ════════════════════════════════════════════════════════════
       PREMIUM EDITORIAL LAYER — JS (stagger + mouse-tracking)
       ════════════════════════════════════════════════════════════ -->
  <script>
  (function(){
    var reduced = window.matchMedia && window.matchMedia('(prefers-reduced-motion: reduce)').matches;

    /* ── Stagger reveal ── */
    if ('IntersectionObserver' in window) {
      var io = new IntersectionObserver(function(entries){
        entries.forEach(function(e){
          if (e.isIntersecting) {
            var lines = e.target.querySelectorAll('.rsm-edit-line');
            lines.forEach(function(line, i){
              setTimeout(function(){ line.classList.add('is-in'); }, reduced ? 0 : i * 90);
            });
            io.unobserve(e.target);
          }
        });
      }, { threshold: 0.18, rootMargin: '0px 0px -6% 0px' });
      document.querySelectorAll('[data-rsm-edit-stagger]').forEach(function(n){ io.observe(n); });
    } else {
      document.querySelectorAll('.rsm-edit-line').forEach(function(n){ n.classList.add('is-in'); });
    }

    /* Banner: trigger immediato (sopra il fold) */
    var banner = document.querySelector('[data-rsm-edit-banner]');
    if (banner) {
      requestAnimationFrame(function(){
        banner.querySelectorAll('.rsm-edit-line').forEach(function(line, i){
          setTimeout(function(){ line.classList.add('is-in'); }, reduced ? 0 : 80 + i * 110);
        });
      });
    }

    if (reduced) return;

    /* ── Mouse-tracking aurora (banner + compass) ── */
    function bindAurora(scope, auroraEl){
      if (!auroraEl) return;
      var rect = null, raf = 0, x = 0.5, y = 0.5;
      var onMove = function(ev){
        if (!rect) rect = scope.getBoundingClientRect();
        x = (ev.clientX - rect.left) / rect.width;
        y = (ev.clientY - rect.top) / rect.height;
        if (!raf) raf = requestAnimationFrame(apply);
      };
      var apply = function(){
        raf = 0;
        auroraEl.style.setProperty('--mx', (x * 100).toFixed(2) + '%');
        auroraEl.style.setProperty('--my', (y * 100).toFixed(2) + '%');
      };
      var onLeave = function(){ rect = null; auroraEl.style.removeProperty('--mx'); auroraEl.style.removeProperty('--my'); };
      var onResize = function(){ rect = null; };
      scope.addEventListener('mousemove', onMove, { passive: true });
      scope.addEventListener('mouseleave', onLeave);
      window.addEventListener('resize', onResize);
    }

    var bannerScope = document.querySelector('[data-rsm-edit-banner]');
    if (bannerScope) bindAurora(bannerScope, bannerScope.querySelector('.rsm-banner__aurora'));

    var compassScope = document.querySelector('[data-rsm-edit-compass]');
    if (compassScope) bindAurora(compassScope, compassScope.querySelector('.rsm-compass__aurora'));
  })();
  </script>
</body>
</html>
