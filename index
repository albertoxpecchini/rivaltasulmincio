<!DOCTYPE html>
<html lang="it">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />

  <title>Pro Loco Rivalta sul Mincio | Valli del Mincio, Luccio, Ecoturismo</title>
  <meta name="description" content="Pro Loco Rivalta sul Mincio (Mantova): scopri le Valli del Mincio, il luccio, l'ecoturismo, le sagre e le tradizioni del borgo nel cuore del Parco del Mincio. Eventi, escursioni e contatti." />
  <meta name="keywords" content="Rivalta sul Mincio, Pro Loco Rivalta, Valli del Mincio, Parco del Mincio, Mantova, sagra del luccio, ecoturismo Mantova, Rodigo, ninfei Mincio, birdwatching Mincio, ciclabile Mincio" />
  <meta name="author" content="Pro Loco Rivalta sul Mincio" />
  <meta name="robots" content="index, follow, max-image-preview:large, max-snippet:-1" />
  <link rel="canonical" href="https://www.rivaltasulmincio.it/" />
  <meta name="theme-color" content="#253f35" />

  <meta name="geo.region" content="IT-MN" />
  <meta name="geo.placename" content="Rivalta sul Mincio, Rodigo" />
  <meta name="geo.position" content="45.181000;10.665000" />
  <meta name="ICBM" content="45.181000, 10.665000" />

  <meta property="og:type" content="website" />
  <meta property="og:site_name" content="Pro Loco Rivalta sul Mincio" />
  <meta property="og:title" content="Pro Loco Rivalta sul Mincio - Valli del Mincio e tradizioni" />
  <meta property="og:description" content="Ecoturismo, sagre e natura nel cuore del Parco del Mincio. Scopri le Valli, il luccio e i percorsi del borgo." />
  <meta property="og:url" content="https://www.rivaltasulmincio.it/" />
  <meta property="og:locale" content="it_IT" />
  <meta property="og:image" content="https://picsum.photos/seed/rivalta-og-wetlands/1200/630" />
  <meta property="og:image:width" content="1200" />
  <meta property="og:image:height" content="630" />
  <meta property="og:image:alt" content="Valli del Mincio a Rivalta sul Mincio" />

  <meta name="twitter:card" content="summary_large_image" />
  <meta name="twitter:title" content="Pro Loco Rivalta sul Mincio" />
  <meta name="twitter:description" content="Le Valli del Mincio, il luccio e le tradizioni del borgo. Ecoturismo nel Parco del Mincio." />
  <meta name="twitter:image" content="https://picsum.photos/seed/rivalta-og-wetlands/1200/630" />

  <script type="application/ld+json">
  {
    "@context": "https://schema.org",
    "@graph": [
      {
        "@type": "Organization",
        "@id": "https://www.rivaltasulmincio.it/#organization",
        "name": "Pro Loco Rivalta sul Mincio",
        "alternateName": "Pro Loco Rivalta",
        "url": "https://www.rivaltasulmincio.it/",
        "description": "Associazione di promozione turistica, culturale e ambientale del borgo di Rivalta sul Mincio, nel cuore del Parco Regionale del Mincio.",
        "email": "info@prolocorivalta.mn.it",
        "telephone": "+39-339-899-5680",
        "address": {
          "@type": "PostalAddress",
          "streetAddress": "Via Porto, 31",
          "addressLocality": "Rivalta sul Mincio",
          "addressRegion": "MN",
          "postalCode": "46040",
          "addressCountry": "IT"
        },
        "areaServed": {
          "@type": "Place",
          "name": "Rivalta sul Mincio, Parco Regionale del Mincio"
        },
        "sameAs": [
          "https://www.facebook.com/prolocrivaltasulmincio",
          "https://www.instagram.com/prolocrivalta"
        ]
      },
      {
        "@type": "WebSite",
        "@id": "https://www.rivaltasulmincio.it/#website",
        "url": "https://www.rivaltasulmincio.it/",
        "name": "Pro Loco Rivalta sul Mincio",
        "description": "Sito ufficiale della Pro Loco di Rivalta sul Mincio: Valli del Mincio, il luccio, ecoturismo, eventi, sagre e tradizioni.",
        "publisher": { "@id": "https://www.rivaltasulmincio.it/#organization" },
        "inLanguage": "it-IT"
      },
      {
        "@type": "TouristDestination",
        "@id": "https://www.rivaltasulmincio.it/#destination",
        "name": "Rivalta sul Mincio",
        "description": "Borgo di pescatori nel Parco Regionale del Mincio, frazione di Rodigo (Mantova). Riserva Naturale, Valli del Mincio, fioritura dei ninfei, birdwatching e tradizione del luccio.",
        "url": "https://www.rivaltasulmincio.it/",
        "geo": {
          "@type": "GeoCoordinates",
          "latitude": 45.181,
          "longitude": 10.665
        },
        "address": {
          "@type": "PostalAddress",
          "addressLocality": "Rivalta sul Mincio",
          "addressRegion": "MN",
          "postalCode": "46040",
          "addressCountry": "IT"
        },
        "touristType": ["Birdwatching", "Ecoturismo", "Cicloturismo", "Pesca sportiva", "Turismo culturale"],
        "includesAttraction": [
          {
            "@type": "TouristAttraction",
            "name": "Le Valli del Mincio",
            "description": "Riserva Naturale Regionale di oltre 1.500 ettari: zona umida con canneti, ninfei e oltre 260 specie di uccelli."
          },
          {
            "@type": "TouristAttraction",
            "name": "Sagra del Luccio",
            "description": "Sagra estiva tradizionale dedicata al luccio, simbolo gastronomico di Rivalta."
          },
          {
            "@type": "TouristAttraction",
            "name": "Ciclabile del Mincio",
            "description": "Tappa intermedia tra Mantova e Peschiera del Garda."
          }
        ]
      },
      {
        "@type": "ItemList",
        "@id": "https://www.rivaltasulmincio.it/#sections",
        "name": "Esplora il sito",
        "itemListElement": [
          { "@type": "SiteNavigationElement", "position": 1, "name": "Introduzione", "url": "https://www.rivaltasulmincio.it/#introduzione" },
          { "@type": "SiteNavigationElement", "position": 2, "name": "Esperienze", "url": "https://www.rivaltasulmincio.it/#esperienze" },
          { "@type": "SiteNavigationElement", "position": 3, "name": "Galleria", "url": "https://www.rivaltasulmincio.it/#galleria" },
          { "@type": "SiteNavigationElement", "position": 4, "name": "Le Valli del Mincio", "url": "https://www.rivaltasulmincio.it/#valli" },
          { "@type": "SiteNavigationElement", "position": 5, "name": "Il Luccio", "url": "https://www.rivaltasulmincio.it/#luccio" },
          { "@type": "SiteNavigationElement", "position": 6, "name": "News", "url": "https://www.rivaltasulmincio.it/#news" },
          { "@type": "SiteNavigationElement", "position": 7, "name": "Contatti", "url": "https://www.rivaltasulmincio.it/#contatti" }
        ]
      }
    ]
  }
  </script>

  <link rel="preconnect" href="https://api.fontshare.com" crossorigin />
  <link href="https://api.fontshare.com/v2/css?f[]=cabinet-grotesk@400,500,700,800,900&display=swap" rel="stylesheet" />
  <link rel="preconnect" href="https://fonts.googleapis.com" />
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
  <link href="https://fonts.googleapis.com/css2?family=Fraunces:ital,opsz,wght@0,9..144,500;0,9..144,650;1,9..144,550&display=swap" rel="stylesheet" />
  <link rel="icon" href="data:image/svg+xml,%3Csvg xmlns='http://www.w3.org/2000/svg' viewBox='0 0 64 64'%3E%3Crect width='64' height='64' rx='16' fill='%23253f35'/%3E%3Cpath d='M9 39 C18 30 46 30 55 39' stroke='%23f7f0e4' stroke-width='4' fill='none' stroke-linecap='round'/%3E%3Cpath d='M9 49 C19 43 45 43 55 49' stroke='%23c58b5d' stroke-width='3' fill='none' stroke-linecap='round'/%3E%3Ccircle cx='43' cy='27' r='3' fill='%23f7f0e4'/%3E%3C/svg%3E" />

  <style>
    :root {
      --ink: #17221d;
      --ink-soft: #31453b;
      --cream: #f7f0e4;
      --paper: #fffaf0;
      --mist: #e4ddcf;
      --marsh: #253f35;
      --marsh-2: #3d6353;
      --water: #2f6b67;
      --clay: #b8663f;
      --reed: #c6a462;
      --fog: rgba(247, 240, 228, 0.72);
      --white: #fffdf8;
      --line: rgba(37, 63, 53, 0.16);
      --line-dark: rgba(255, 250, 240, 0.16);
      --shadow: 0 26px 80px rgba(37, 63, 53, 0.16);
      --shadow-strong: 0 42px 110px rgba(23, 34, 29, 0.28);
      --ease: cubic-bezier(0.32, 0.72, 0, 1);
      --ease-soft: cubic-bezier(0.16, 1, 0.3, 1);
      --sans: "Cabinet Grotesk", system-ui, sans-serif;
      --serif: "Fraunces", Georgia, serif;
      --container: min(100% - 44px, 1400px);
      color-scheme: light;
    }

    *, *::before, *::after { box-sizing: border-box; }

    html {
      scroll-behavior: smooth;
      background: var(--cream);
    }

    body {
      margin: 0;
      min-width: 320px;
      font-family: var(--sans);
      color: var(--ink);
      background:
        radial-gradient(circle at 14% 8%, rgba(198, 164, 98, 0.32), transparent 28rem),
        radial-gradient(circle at 88% 2%, rgba(47, 107, 103, 0.20), transparent 34rem),
        linear-gradient(135deg, var(--cream), #efe5d2 54%, #f8f2e8);
      line-height: 1.5;
      overflow-x: hidden;
      text-rendering: optimizeLegibility;
      -webkit-font-smoothing: antialiased;
    }

    body.menu-open { overflow: hidden; }

    body::before {
      content: "";
      position: fixed;
      inset: 0;
      z-index: 80;
      pointer-events: none;
      opacity: 0.26;
      background-image:
        radial-gradient(circle at 12% 18%, rgba(23, 34, 29, 0.11) 0 1px, transparent 1px),
        radial-gradient(circle at 77% 54%, rgba(23, 34, 29, 0.08) 0 1px, transparent 1px);
      background-size: 23px 23px, 31px 31px;
      mix-blend-mode: multiply;
    }

    a {
      color: inherit;
      text-decoration: none;
    }

    button,
    input {
      font: inherit;
    }

    button {
      cursor: pointer;
    }

    img,
    svg {
      display: block;
      max-width: 100%;
    }

    ::selection {
      background: rgba(184, 102, 63, 0.28);
    }

    .skip-link {
      position: fixed;
      left: 16px;
      top: 12px;
      z-index: 90;
      padding: 10px 14px;
      border-radius: 999px;
      background: var(--ink);
      color: var(--paper);
      transform: translateY(-160%);
      transition: transform 360ms var(--ease);
    }

    .skip-link:focus { transform: translateY(0); }

    .container {
      width: var(--container);
      margin: 0 auto;
    }

    .page-shell {
      width: 100%;
      max-width: 100%;
      overflow-x: clip;
    }

    .site-header {
      position: fixed;
      top: 20px;
      left: 0;
      right: 0;
      z-index: 70;
      pointer-events: none;
    }

    .nav-island {
      position: relative;
      width: min(100% - 28px, 1240px);
      margin: 0 auto;
      display: grid;
      grid-template-columns: auto 1fr auto;
      align-items: center;
      gap: 18px;
      padding: 8px 10px 8px 12px;
      border: 1px solid rgba(255, 250, 240, 0.68);
      border-radius: 999px;
      background: rgba(247, 240, 228, 0.78);
      box-shadow: 0 18px 48px rgba(23, 34, 29, 0.12), inset 0 1px 0 rgba(255, 255, 255, 0.7);
      backdrop-filter: blur(24px);
      pointer-events: auto;
    }

    .brand {
      display: inline-flex;
      align-items: center;
      gap: 11px;
      min-width: 0;
    }

    .brand-mark {
      position: relative;
      width: 42px;
      height: 42px;
      flex: 0 0 42px;
      border-radius: 15px;
      background: linear-gradient(145deg, var(--marsh), var(--water));
      box-shadow: inset 0 1px 0 rgba(255, 255, 255, 0.20);
      overflow: hidden;
    }

    .brand-mark::before,
    .brand-mark::after {
      content: "";
      position: absolute;
      left: 8px;
      right: 8px;
      border: 2px solid var(--paper);
      border-top: 0;
      border-left-color: transparent;
      border-right-color: transparent;
      border-radius: 50%;
      opacity: 0.92;
    }

    .brand-mark::before { height: 18px; bottom: 10px; }
    .brand-mark::after { height: 12px; bottom: 4px; opacity: 0.48; }

    .brand-name {
      display: grid;
      line-height: 1.02;
      letter-spacing: -0.02em;
    }

    .brand-name strong {
      font-size: 0.98rem;
      font-weight: 800;
    }

    .brand-name span {
      font-size: 0.72rem;
      color: rgba(23, 34, 29, 0.62);
      font-weight: 600;
    }

    .nav-links {
      display: flex;
      align-items: center;
      justify-content: center;
      gap: 4px;
      min-width: 0;
    }

    .nav-links a {
      display: inline-flex;
      align-items: center;
      min-height: 38px;
      padding: 0 13px;
      border-radius: 999px;
      font-size: 0.86rem;
      font-weight: 700;
      color: rgba(23, 34, 29, 0.68);
      transition: color 420ms var(--ease), background 420ms var(--ease), transform 420ms var(--ease);
    }

    .nav-mobile-login {
      display: none !important;
    }

    .nav-links a:hover,
    .nav-links a:focus-visible,
    .nav-links a.active {
      color: var(--ink);
      background: rgba(255, 250, 240, 0.84);
      transform: translateY(-1px);
    }

    .nav-actions {
      display: inline-flex;
      align-items: center;
      gap: 8px;
    }

    .nav-login,
    .profile-btn,
    .nav-toggle {
      border: 0;
      border-radius: 999px;
      background: var(--marsh);
      color: var(--paper);
      min-height: 42px;
      padding: 0 16px;
      display: inline-flex;
      align-items: center;
      justify-content: center;
      gap: 9px;
      font-weight: 800;
      box-shadow: inset 0 1px 0 rgba(255, 255, 255, 0.16);
      transition: transform 420ms var(--ease), background 420ms var(--ease), box-shadow 420ms var(--ease);
    }

    .nav-login:hover,
    .profile-btn:hover,
    .nav-toggle:hover {
      transform: translateY(-1px);
      background: #1f362e;
      box-shadow: 0 14px 30px rgba(23, 34, 29, 0.18), inset 0 1px 0 rgba(255, 255, 255, 0.20);
    }

    .nav-login:active,
    .profile-btn:active,
    .nav-toggle:active,
    .button:active {
      transform: scale(0.98);
    }

    .nav-toggle {
      display: none;
      width: 44px;
      padding: 0;
      position: relative;
    }

    .nav-toggle span {
      position: absolute;
      width: 17px;
      height: 2px;
      border-radius: 999px;
      background: currentColor;
      transition: transform 500ms var(--ease), opacity 500ms var(--ease);
    }

    .nav-toggle span:first-child { transform: translateY(-5px); }
    .nav-toggle span:last-child { transform: translateY(5px); }
    .nav-toggle.open span:first-child { transform: rotate(45deg); }
    .nav-toggle.open span:nth-child(2) { opacity: 0; transform: scaleX(0); }
    .nav-toggle.open span:last-child { transform: rotate(-45deg); }

    .profile-menu {
      position: relative;
    }

    .profile-avatar {
      width: 26px;
      height: 26px;
      display: inline-flex;
      align-items: center;
      justify-content: center;
      border-radius: 10px;
      background: var(--water);
      color: var(--paper);
      font-size: 0.75rem;
      font-weight: 900;
      text-transform: uppercase;
    }

    .profile-dropdown {
      position: absolute;
      right: 0;
      top: calc(100% + 12px);
      width: 230px;
      padding: 10px;
      border: 1px solid rgba(255, 250, 240, 0.58);
      border-radius: 24px;
      background: rgba(255, 250, 240, 0.94);
      box-shadow: var(--shadow);
      backdrop-filter: blur(20px);
      opacity: 0;
      visibility: hidden;
      transform: translateY(10px) scale(0.98);
      transition: opacity 360ms var(--ease), transform 360ms var(--ease), visibility 360ms var(--ease);
    }

    .profile-dropdown.open {
      opacity: 1;
      visibility: visible;
      transform: translateY(0) scale(1);
    }

    .profile-dropdown__section-title {
      padding: 8px 10px;
      font-size: 0.66rem;
      letter-spacing: 0.16em;
      text-transform: uppercase;
      color: rgba(23, 34, 29, 0.5);
      font-weight: 900;
    }

    .profile-dropdown a,
    .profile-dropdown button {
      width: 100%;
      border: 0;
      background: transparent;
      color: var(--ink);
      display: flex;
      align-items: center;
      justify-content: space-between;
      min-height: 38px;
      padding: 0 10px;
      border-radius: 14px;
      font-weight: 750;
      text-align: left;
      transition: background 300ms var(--ease), transform 300ms var(--ease);
    }

    .profile-dropdown a:hover,
    .profile-dropdown button:hover,
    .profile-dropdown .active {
      background: rgba(37, 63, 53, 0.08);
      transform: translateX(2px);
    }

    .profile-dropdown .danger { color: #8f3b28; }
    .profile-dropdown__sep { height: 1px; background: var(--line); margin: 8px 4px; }

    .hero {
      position: relative;
      min-height: auto;
      padding: clamp(112px, 10vw, 138px) 0 clamp(64px, 8vw, 86px);
      isolation: isolate;
    }

    .hero::before {
      content: "";
      position: absolute;
      inset: 0;
      z-index: -2;
      background:
        radial-gradient(circle at 79% 16%, rgba(47, 107, 103, 0.28), transparent 24rem),
        linear-gradient(180deg, rgba(255, 250, 240, 0.35), rgba(239, 229, 210, 0.92));
    }

    .hero::after {
      content: "";
      position: absolute;
      left: 0;
      right: 0;
      bottom: -2px;
      z-index: -1;
      height: 180px;
      background: linear-gradient(180deg, transparent, var(--cream));
    }

    .hero-layout {
      display: grid;
      grid-template-columns: minmax(0, 0.96fr) minmax(360px, 0.74fr);
      align-items: center;
      gap: clamp(28px, 5vw, 86px);
    }

    .eyebrow {
      display: inline-flex;
      align-items: center;
      gap: 10px;
      margin-bottom: 18px;
      color: var(--marsh);
      font-size: 0.76rem;
      font-weight: 900;
      letter-spacing: 0.16em;
      text-transform: uppercase;
    }

    .eyebrow::before {
      content: "";
      width: 34px;
      height: 1px;
      background: currentColor;
      opacity: 0.5;
    }

    .hero-title {
      max-width: 940px;
      margin: 0;
      font-family: var(--serif);
      font-size: clamp(3.55rem, 8vw, 8.7rem);
      line-height: 0.88;
      letter-spacing: -0.034em;
      text-wrap: balance;
    }

    .hero-title em {
      font-style: italic;
      color: var(--clay);
      letter-spacing: -0.07em;
    }

    .hero-copy {
      max-width: 680px;
      margin: 28px 0 0;
      color: rgba(23, 34, 29, 0.72);
      font-size: clamp(1.08rem, 1.8vw, 1.36rem);
      line-height: 1.52;
    }

    .button-row {
      display: flex;
      flex-wrap: wrap;
      gap: 12px;
      margin-top: 34px;
    }

    .button {
      position: relative;
      min-height: 54px;
      display: inline-flex;
      align-items: center;
      justify-content: center;
      gap: 12px;
      padding: 8px 9px 8px 22px;
      border: 1px solid transparent;
      border-radius: 999px;
      font-weight: 900;
      color: var(--paper);
      background: var(--marsh);
      box-shadow: inset 0 1px 0 rgba(255, 255, 255, 0.16), 0 18px 38px rgba(23, 34, 29, 0.18);
      transition: transform 520ms var(--ease), background 520ms var(--ease), box-shadow 520ms var(--ease);
      overflow: hidden;
      will-change: transform;
    }

    .button::before {
      content: "";
      position: absolute;
      inset: -45%;
      background: radial-gradient(circle at var(--mx, 50%) var(--my, 50%), rgba(255, 250, 240, 0.22), transparent 34%);
      opacity: 0;
      transition: opacity 420ms var(--ease);
    }

    .button span {
      position: relative;
      z-index: 1;
    }

    .button .button-icon {
      position: relative;
      z-index: 1;
      width: 38px;
      height: 38px;
      border-radius: 50%;
      display: inline-flex;
      align-items: center;
      justify-content: center;
      color: var(--marsh);
      background: var(--paper);
      transition: transform 520ms var(--ease);
    }

    .button:hover {
      transform: translateY(-2px);
      background: #20392f;
      box-shadow: 0 24px 52px rgba(23, 34, 29, 0.24), inset 0 1px 0 rgba(255, 255, 255, 0.2);
    }

    .button:hover::before { opacity: 1; }
    .button:hover .button-icon { transform: translate(3px, -1px) scale(1.04); }

    .button.secondary {
      color: var(--ink);
      background: rgba(255, 250, 240, 0.66);
      border-color: rgba(37, 63, 53, 0.16);
      box-shadow: inset 0 1px 0 rgba(255, 255, 255, 0.7);
    }

    .button.secondary .button-icon {
      background: var(--marsh);
      color: var(--paper);
    }

    .hero-media {
      position: relative;
      min-height: clamp(460px, 50vw, 620px);
    }

    .photo-frame {
      padding: 8px;
      border-radius: 42px;
      background: rgba(255, 250, 240, 0.36);
      border: 1px solid rgba(255, 250, 240, 0.58);
      box-shadow: var(--shadow-strong);
    }

    .photo-slot {
      position: relative;
      overflow: hidden;
      isolation: isolate;
      border-radius: 34px;
      background-image:
        linear-gradient(180deg, rgba(23, 34, 29, 0.02), rgba(23, 34, 29, 0.34)),
        var(--photo);
      background-size: cover;
      background-position: center;
      min-height: 260px;
      box-shadow: inset 0 1px 0 rgba(255, 255, 255, 0.28);
      transform: translateZ(0);
    }

    .photo-slot::after {
      content: "";
      position: absolute;
      inset: 0;
      z-index: 1;
      background:
        linear-gradient(135deg, rgba(255, 250, 240, 0.18), transparent 38%),
        radial-gradient(circle at 80% 16%, rgba(255, 250, 240, 0.24), transparent 16rem);
      mix-blend-mode: screen;
      pointer-events: none;
    }

    .photo-slot[data-photo-label]::before {
      content: attr(data-photo-label);
      position: absolute;
      left: 18px;
      bottom: 18px;
      z-index: 2;
      max-width: calc(100% - 36px);
      padding: 8px 12px;
      border-radius: 999px;
      color: var(--paper);
      background: rgba(23, 34, 29, 0.50);
      border: 1px solid rgba(255, 250, 240, 0.20);
      font-size: 0.72rem;
      letter-spacing: 0.13em;
      text-transform: uppercase;
      font-weight: 900;
      backdrop-filter: blur(12px);
    }

    .hero-photo-main {
      position: absolute;
      inset: 0 0 54px 88px;
      transform: rotate(2deg);
    }

    .hero-photo-main .photo-slot { height: 100%; }

    .hero-photo-main .photo-slot[data-photo-label]::before {
      left: auto;
      right: 18px;
      top: 18px;
      bottom: auto;
    }

    .hero-photo-side {
      position: absolute;
      left: 0;
      bottom: 86px;
      width: min(52%, 330px);
      transform: rotate(-6deg);
    }

    .hero-photo-side .photo-slot { min-height: 260px; }

    .hero-photo-tiny {
      position: absolute;
      right: 20px;
      bottom: 0;
      width: min(46%, 280px);
      transform: rotate(5deg);
    }

    .hero-photo-tiny .photo-slot { min-height: 210px; }

    .hero-note {
      position: absolute;
      left: 48px;
      top: 22px;
      z-index: 4;
      width: 176px;
      padding: 16px;
      border: 1px solid rgba(255, 250, 240, 0.58);
      border-radius: 26px;
      background: rgba(255, 250, 240, 0.80);
      box-shadow: 0 20px 48px rgba(23, 34, 29, 0.16);
      backdrop-filter: blur(18px);
      font-weight: 800;
      line-height: 1.12;
    }

    .hero-note span {
      display: block;
      margin-top: 10px;
      color: rgba(23, 34, 29, 0.58);
      font-size: 0.74rem;
      font-weight: 700;
      letter-spacing: 0.02em;
    }

    .marquee-band {
      position: relative;
      overflow: hidden;
      border-top: 1px solid var(--line);
      border-bottom: 1px solid var(--line);
      background: rgba(255, 250, 240, 0.34);
    }

    .marquee-track {
      display: flex;
      width: max-content;
      gap: 46px;
      padding: 18px 0;
      animation: drift 36s infinite linear;
      color: rgba(37, 63, 53, 0.72);
      font-weight: 900;
      letter-spacing: 0.02em;
      white-space: nowrap;
    }

    .marquee-track span {
      display: inline-flex;
      align-items: center;
      gap: 46px;
    }

    .marquee-track span::after {
      content: "";
      width: 8px;
      height: 8px;
      border-radius: 50%;
      background: var(--clay);
      opacity: 0.72;
    }

    @keyframes drift {
      from { transform: translate3d(0, 0, 0); }
      to { transform: translate3d(-50%, 0, 0); }
    }

    .section {
      padding: clamp(92px, 11vw, 168px) 0;
      position: relative;
    }

    .section.compact {
      padding-top: clamp(70px, 8vw, 120px);
    }

    .section.dark {
      color: var(--paper);
      background:
        radial-gradient(circle at 86% 14%, rgba(184, 102, 63, 0.22), transparent 28rem),
        linear-gradient(145deg, #14231e, #253f35 55%, #17221d);
    }

    .section.dark .eyebrow,
    .section.dark .section-kicker,
    .section.dark .section-lead {
      color: rgba(255, 250, 240, 0.72);
    }

    .section-head {
      display: grid;
      grid-template-columns: minmax(0, 0.84fr) minmax(260px, 0.5fr);
      gap: clamp(26px, 6vw, 90px);
      align-items: end;
      margin-bottom: clamp(38px, 6vw, 84px);
    }

    .section-title {
      margin: 0;
      max-width: 920px;
      font-family: var(--serif);
      font-size: clamp(2.8rem, 6vw, 6.8rem);
      line-height: 0.94;
      letter-spacing: -0.056em;
      text-wrap: balance;
    }

    .section-title em {
      color: var(--clay);
      font-style: italic;
    }

    .section-lead {
      max-width: 620px;
      margin: 0;
      color: rgba(23, 34, 29, 0.70);
      font-size: clamp(1rem, 1.5vw, 1.18rem);
      line-height: 1.7;
    }

    .section-kicker {
      margin: 0 0 18px;
      color: var(--water);
      font-size: 0.82rem;
      letter-spacing: 0.17em;
      text-transform: uppercase;
      font-weight: 900;
    }

    .atlas-grid {
      display: grid;
      grid-template-columns: 1.22fr 0.78fr;
      gap: 18px;
      align-items: stretch;
    }

    .atlas-panel {
      position: relative;
      padding: 8px;
      border-radius: 42px;
      border: 1px solid rgba(37, 63, 53, 0.12);
      background: rgba(255, 250, 240, 0.42);
      box-shadow: var(--shadow);
      overflow: hidden;
    }

    .atlas-panel-inner {
      min-height: 100%;
      border-radius: 34px;
      background: rgba(255, 250, 240, 0.74);
      box-shadow: inset 0 1px 0 rgba(255, 255, 255, 0.75);
      overflow: hidden;
    }

    .atlas-photo .photo-slot {
      min-height: 610px;
      border-radius: 34px;
    }

    .atlas-copy {
      padding: clamp(24px, 4vw, 52px);
      display: flex;
      flex-direction: column;
      justify-content: space-between;
      gap: 36px;
    }

    .atlas-copy h2 {
      margin: 0;
      font-family: var(--serif);
      font-size: clamp(2rem, 3.9vw, 4.3rem);
      line-height: 0.98;
      letter-spacing: -0.045em;
    }

    .atlas-copy p {
      margin: 20px 0 0;
      color: rgba(23, 34, 29, 0.68);
      line-height: 1.72;
      font-size: 1.04rem;
    }

    .fact-grid {
      display: grid;
      grid-template-columns: repeat(2, minmax(0, 1fr));
      gap: 10px;
    }

    .fact {
      min-height: 138px;
      padding: 18px;
      border-radius: 26px;
      background: rgba(37, 63, 53, 0.06);
      display: flex;
      flex-direction: column;
      justify-content: space-between;
    }

    .fact strong {
      font-size: clamp(1.9rem, 3.5vw, 3.3rem);
      line-height: 0.9;
      letter-spacing: -0.055em;
      font-variant-numeric: tabular-nums;
    }

    .fact span {
      color: rgba(23, 34, 29, 0.62);
      font-weight: 800;
      line-height: 1.2;
    }

    .experience-grid {
      display: grid;
      grid-template-columns: minmax(290px, 0.72fr) minmax(0, 1fr);
      gap: clamp(24px, 6vw, 96px);
      align-items: start;
    }

    .sticky-copy {
      position: sticky;
      top: 128px;
    }

    .stack-list {
      display: grid;
      gap: 18px;
    }

    .stack-card {
      position: relative;
      min-height: 420px;
      padding: 8px;
      border-radius: 42px;
      background: rgba(255, 250, 240, 0.42);
      border: 1px solid rgba(37, 63, 53, 0.12);
      box-shadow: var(--shadow);
      overflow: hidden;
      transform-origin: 50% 0%;
    }

    .stack-card-inner {
      min-height: 404px;
      border-radius: 34px;
      overflow: hidden;
      display: grid;
      grid-template-columns: minmax(0, 0.58fr) minmax(260px, 0.42fr);
      background: var(--paper);
    }

    .stack-card .photo-slot {
      height: 100%;
      min-height: 404px;
      border-radius: 0;
    }

    .stack-card-copy {
      padding: clamp(24px, 4vw, 44px);
      display: flex;
      flex-direction: column;
      justify-content: space-between;
      gap: 32px;
    }

    .stack-card-copy h3 {
      margin: 0;
      font-family: var(--serif);
      font-size: clamp(1.8rem, 3vw, 3rem);
      line-height: 1;
      letter-spacing: -0.04em;
    }

    .stack-card-copy p {
      margin: 14px 0 0;
      color: rgba(23, 34, 29, 0.66);
      line-height: 1.64;
    }

    .stack-meta {
      display: flex;
      flex-wrap: wrap;
      gap: 8px;
    }

    .tag {
      display: inline-flex;
      align-items: center;
      min-height: 30px;
      padding: 0 10px;
      border-radius: 999px;
      background: rgba(47, 107, 103, 0.10);
      color: var(--water);
      font-size: 0.76rem;
      font-weight: 900;
      letter-spacing: 0.04em;
    }

    .gallery-stage {
      position: relative;
      padding: 8px;
      border-radius: 46px;
      background: linear-gradient(145deg, rgba(211, 237, 243, 0.62), rgba(235, 248, 252, 0.42));
      border: 1px solid rgba(37, 63, 53, 0.12);
      box-shadow: var(--shadow-strong);
      overflow: hidden;
    }

    .carousel {
      position: relative;
      border-radius: 38px;
      background:
        radial-gradient(circle at 18% 16%, rgba(198, 164, 98, 0.24), transparent 25rem),
        linear-gradient(145deg, rgba(37, 63, 53, 0.96), rgba(23, 34, 29, 0.98));
      min-height: 660px;
      overflow: hidden;
      color: var(--paper);
    }

    .carousel-viewport {
      position: relative;
      min-height: 560px;
      perspective: 1200px;
      padding: 48px 0 84px;
      touch-action: pan-y;
    }

    .carousel-slide {
      position: absolute;
      left: 50%;
      top: 48px;
      width: min(72vw, 760px);
      height: min(64vw, 500px);
      min-height: 360px;
      opacity: 0;
      pointer-events: none;
      transform: translate3d(-50%, 26px, 0) scale(0.76) rotateY(-12deg);
      transition: opacity 760ms var(--ease), transform 760ms var(--ease), filter 760ms var(--ease);
      filter: saturate(0.82) contrast(0.98) brightness(0.7);
      animation: floatPhoto 6.2s ease-in-out infinite;
    }

    .carousel-slide[data-state="active"] {
      z-index: 4;
      opacity: 1;
      pointer-events: auto;
      transform: translate3d(-50%, 0, 0) scale(1) rotateY(0deg);
      filter: saturate(1) contrast(1.04) brightness(1);
    }

    .carousel-slide[data-state="prev"] {
      z-index: 3;
      opacity: 0.44;
      transform: translate3d(-108%, 34px, -140px) scale(0.78) rotateY(16deg);
    }

    .carousel-slide[data-state="next"] {
      z-index: 3;
      opacity: 0.44;
      transform: translate3d(8%, 34px, -140px) scale(0.78) rotateY(-16deg);
    }

    .carousel-card {
      height: 100%;
      padding: 8px;
      border-radius: 42px;
      border: 1px solid rgba(255, 250, 240, 0.20);
      background: rgba(255, 250, 240, 0.08);
      box-shadow: 0 36px 90px rgba(0, 0, 0, 0.20);
    }

    .carousel-card .photo-slot {
      height: 100%;
      min-height: 100%;
      border-radius: 34px;
      transition: transform 440ms var(--ease-soft), filter 440ms var(--ease-soft);
      will-change: transform;
    }

    .carousel-card .photo-slot:hover {
      transform: scale(1.01);
      filter: saturate(1.1);
    }
    @keyframes floatPhoto {
      0%, 100% { transform: translate3d(-50%, 0, 0) scale(1) rotateY(0deg); }
      50% { transform: translate3d(-50%, -8px, 0) scale(1.015) rotateY(0deg); }
    }

    .carousel-caption {
      position: absolute;
      left: 26px;
      right: 26px;
      bottom: 24px;
      z-index: 5;
      display: flex;
      justify-content: space-between;
      align-items: end;
      gap: 18px;
      color: var(--paper);
    }

    .carousel-caption h3 {
      margin: 0;
      font-family: var(--serif);
      font-size: clamp(1.9rem, 3vw, 3rem);
      line-height: 0.95;
      letter-spacing: -0.045em;
      text-shadow: 0 12px 30px rgba(0, 0, 0, 0.26);
    }

    .carousel-caption p {
      max-width: 290px;
      margin: 0;
      color: rgba(255, 250, 240, 0.74);
      font-weight: 700;
      line-height: 1.34;
    }

    .carousel-controls {
      position: absolute;
      left: 0;
      right: 0;
      bottom: 28px;
      z-index: 8;
      display: flex;
      align-items: center;
      justify-content: center;
      gap: 16px;
    }

    .carousel-btn,
    .carousel-dot {
      border: 1px solid rgba(255, 250, 240, 0.18);
      color: var(--paper);
      background: rgba(255, 250, 240, 0.10);
      backdrop-filter: blur(14px);
      transition: transform 420ms var(--ease), background 420ms var(--ease);
    }

    .carousel-btn {
      width: 48px;
      height: 48px;
      border-radius: 50%;
      display: inline-flex;
      align-items: center;
      justify-content: center;
      font-size: 1.25rem;
    }

    .carousel-btn:hover,
    .carousel-dot:hover,
    .carousel-dot.active {
      background: rgba(255, 250, 240, 0.22);
      transform: translateY(-2px);
    }

    .carousel-dots {
      display: inline-flex;
      align-items: center;
      gap: 8px;
    }

    .carousel-dot {
      width: 11px;
      height: 11px;
      padding: 0;
      border-radius: 50%;
    }

    .photo-rail {
      display: grid;
      grid-auto-flow: column;
      grid-auto-columns: minmax(260px, 36vw);
      gap: 18px;
      overflow-x: auto;
      padding: 4px var(--side-pad, 0) 14px;
      scroll-snap-type: x mandatory;
      scrollbar-width: thin;
    }

    .rail-item {
      scroll-snap-align: start;
      padding: 8px;
      border-radius: 36px;
      border: 1px solid rgba(37, 63, 53, 0.12);
      background: rgba(255, 250, 240, 0.44);
      box-shadow: var(--shadow);
    }

    .rail-item .photo-slot {
      min-height: clamp(300px, 42vw, 510px);
      border-radius: 28px;
    }

    .split-story {
      display: grid;
      grid-template-columns: minmax(0, 0.9fr) minmax(340px, 0.62fr);
      gap: clamp(24px, 6vw, 90px);
      align-items: center;
    }

    .story-card {
      padding: clamp(24px, 4vw, 54px);
      border-radius: 42px;
      border: 1px solid rgba(37, 63, 53, 0.12);
      background: rgba(255, 250, 240, 0.62);
      box-shadow: var(--shadow);
    }

    .story-card h3 {
      margin: 0 0 20px;
      font-family: var(--serif);
      font-size: clamp(2rem, 4vw, 4rem);
      line-height: 0.98;
      letter-spacing: -0.045em;
    }

    .story-card p {
      margin: 0 0 16px;
      color: rgba(23, 34, 29, 0.70);
      line-height: 1.72;
    }

    .story-list {
      list-style: none;
      padding: 0;
      margin: 28px 0 0;
      display: grid;
      gap: 12px;
    }

    .story-list li {
      display: grid;
      grid-template-columns: 38px 1fr;
      gap: 12px;
      align-items: start;
      color: rgba(23, 34, 29, 0.70);
    }

    .story-list li::before {
      content: "";
      width: 38px;
      height: 2px;
      margin-top: 12px;
      border-radius: 999px;
      background: var(--clay);
    }

    .luccio-layout {
      display: grid;
      grid-template-columns: minmax(0, 0.58fr) minmax(0, 0.42fr);
      gap: 18px;
    }

    .recipe-panel {
      padding: clamp(24px, 4vw, 46px);
      border-radius: 38px;
      background: linear-gradient(160deg, rgba(227, 245, 252, 0.9), rgba(244, 252, 255, 0.86));
      border: 1px solid rgba(76, 135, 162, 0.2);
      box-shadow: var(--shadow);
    }

    .recipe-panel h3 {
      margin: 0 0 12px;
      font-family: var(--serif);
      font-size: clamp(1.8rem, 3.1vw, 3rem);
      line-height: 1;
      letter-spacing: -0.04em;
    }

    .recipe-panel p {
      color: rgba(23, 34, 29, 0.68);
      line-height: 1.68;
    }

    .recipe-list {
      display: flex;
      flex-wrap: wrap;
      gap: 8px;
      margin-top: 24px;
    }
    #luccio .story-card {
      background: linear-gradient(155deg, rgba(208, 239, 249, 0.85), rgba(236, 250, 255, 0.94));
      border-color: rgba(76, 135, 162, 0.26);
    }

    .news-shell {
      display: grid;
      grid-template-columns: minmax(0, 1fr);
      gap: 22px;
      align-items: start;
    }

    .news-main {
      padding: 8px;
      border-radius: 42px;
      background: rgba(255, 250, 240, 0.46);
      border: 1px solid rgba(37, 63, 53, 0.12);
      box-shadow: var(--shadow);
    }

    .news-inner {
      border-radius: 34px;
      background: rgba(255, 250, 240, 0.76);
      box-shadow: inset 0 1px 0 rgba(255, 255, 255, 0.7);
    }

    .news-inner { padding: clamp(18px, 3vw, 34px); }

    .news-filter-row {
      display: flex;
      flex-wrap: wrap;
      gap: 8px;
      margin-bottom: 20px;
    }

    .news-filter-chip {
      border: 1px solid rgba(37, 63, 53, 0.12);
      border-radius: 999px;
      min-height: 36px;
      padding: 0 14px;
      color: rgba(23, 34, 29, 0.68);
      background: rgba(255, 250, 240, 0.62);
      font-weight: 850;
      transition: transform 340ms var(--ease), background 340ms var(--ease), color 340ms var(--ease);
    }

    .news-filter-chip:hover,
    .news-filter-chip.on {
      color: var(--paper);
      background: var(--marsh);
      transform: translateY(-1px);
    }

    .news-status {
      color: rgba(23, 34, 29, 0.62);
      font-weight: 800;
      margin-bottom: 18px;
    }

    .post-featured,
    .post-card {
      overflow: hidden;
      border-radius: 30px;
      background: var(--white);
      border: 1px solid rgba(37, 63, 53, 0.10);
      transition: transform 520ms var(--ease), box-shadow 520ms var(--ease);
    }

    .post-featured:hover,
    .post-card:hover {
      transform: translateY(-5px);
      box-shadow: 0 28px 70px rgba(23, 34, 29, 0.16);
    }

    .post-featured {
      display: grid;
      grid-template-columns: minmax(0, 0.55fr) minmax(280px, 0.45fr);
      margin-bottom: 18px;
    }

    .news-card__media,
    .post-featured__media {
      min-height: 300px;
      border-radius: 0;
    }

    .post-featured__body,
    .post-card__body {
      padding: 24px;
    }

    .post-meta {
      display: flex;
      flex-wrap: wrap;
      gap: 8px;
      align-items: center;
      color: rgba(23, 34, 29, 0.50);
      font-size: 0.78rem;
      font-weight: 900;
      letter-spacing: 0.08em;
      text-transform: uppercase;
    }

    .post-featured__title,
    .post-card__title {
      margin: 14px 0 12px;
      font-family: var(--serif);
      line-height: 1.02;
      letter-spacing: -0.035em;
    }

    .post-featured__title { font-size: clamp(2rem, 3.3vw, 3.35rem); }
    .post-card__title { font-size: 1.65rem; }

    .post-featured__excerpt,
    .post-card__excerpt {
      color: rgba(23, 34, 29, 0.66);
      line-height: 1.62;
    }

    .post-card-grid {
      display: grid;
      grid-template-columns: repeat(2, minmax(0, 1fr));
      gap: 18px;
    }

    .post-card .news-card__media {
      min-height: 220px;
    }

    .post-card__footer,
    .post-featured__footer {
      display: flex;
      align-items: center;
      justify-content: space-between;
      gap: 12px;
      margin-top: 20px;
    }

    .btn-read {
      display: inline-flex;
      align-items: center;
      gap: 9px;
      color: var(--marsh);
      font-weight: 900;
    }

    .badge {
      display: inline-flex;
      align-items: center;
      min-height: 28px;
      padding: 0 10px;
      border-radius: 999px;
      background: rgba(184, 102, 63, 0.12);
      color: #8f492f;
      font-size: 0.72rem;
      font-weight: 900;
      letter-spacing: 0.06em;
      text-transform: uppercase;
    }

    .badge.green {
      background: rgba(47, 107, 103, 0.12);
      color: var(--water);
    }

    .news-empty {
      min-height: 280px;
      display: grid;
      place-items: center;
      text-align: center;
      border: 1px dashed rgba(37, 63, 53, 0.20);
      border-radius: 30px;
      padding: 34px;
      color: rgba(23, 34, 29, 0.62);
    }

    .news-empty strong {
      display: block;
      margin-bottom: 8px;
      font-family: var(--serif);
      font-size: 2rem;
      color: var(--ink);
      letter-spacing: -0.035em;
    }

    .side-list {
      display: grid;
      gap: 14px;
      margin-top: 18px;
    }

    .side-list a {
      padding: 14px 0;
      border-top: 1px solid rgba(37, 63, 53, 0.12);
      display: flex;
      justify-content: space-between;
      gap: 16px;
      color: rgba(23, 34, 29, 0.70);
      font-weight: 850;
    }

    .community-grid {
      display: grid;
      grid-template-columns: 1.1fr 0.9fr;
      gap: 18px;
      align-items: stretch;
    }

    .community-panel {
      padding: clamp(24px, 5vw, 58px);
      border-radius: 42px;
      background: rgba(255, 250, 240, 0.08);
      border: 1px solid var(--line-dark);
      box-shadow: inset 0 1px 0 rgba(255, 255, 255, 0.08);
    }

    .community-panel h2 {
      margin: 0;
      font-family: var(--serif);
      font-size: clamp(2.6rem, 5.6vw, 6rem);
      line-height: 0.94;
      letter-spacing: -0.055em;
    }

    .community-panel p {
      max-width: 720px;
      color: rgba(255, 250, 240, 0.74);
      line-height: 1.72;
      font-size: 1.06rem;
    }

    .community-list {
      display: grid;
      gap: 10px;
    }

    .community-item {
      padding: 24px;
      border-radius: 30px;
      background: rgba(255, 250, 240, 0.08);
      border: 1px solid var(--line-dark);
    }

    .community-item strong {
      display: block;
      margin-bottom: 8px;
      font-family: var(--serif);
      font-size: 1.72rem;
      letter-spacing: -0.035em;
    }

    .community-item span {
      color: rgba(255, 250, 240, 0.68);
      line-height: 1.58;
    }

    .contact-grid {
      display: grid;
      grid-template-columns: minmax(0, 0.72fr) minmax(310px, 0.38fr);
      gap: 18px;
    }

    .map-plate {
      min-height: 590px;
      padding: 8px;
      border-radius: 42px;
      background: rgba(255, 250, 240, 0.44);
      border: 1px solid rgba(37, 63, 53, 0.12);
      box-shadow: var(--shadow);
    }

    .map-plate .photo-slot {
      min-height: 574px;
      border-radius: 34px;
    }

    .contact-card {
      padding: clamp(24px, 4vw, 42px);
      border-radius: 42px;
      background: rgba(255, 250, 240, 0.76);
      border: 1px solid rgba(37, 63, 53, 0.12);
      box-shadow: var(--shadow);
      display: flex;
      flex-direction: column;
      justify-content: space-between;
      gap: 28px;
    }

    .contact-card h3 {
      margin: 0;
      font-family: var(--serif);
      font-size: clamp(2rem, 4vw, 4rem);
      line-height: 0.98;
      letter-spacing: -0.045em;
    }

    .contact-lines {
      display: grid;
      gap: 14px;
    }

    .contact-line {
      padding-top: 14px;
      border-top: 1px solid rgba(37, 63, 53, 0.12);
    }

    .contact-line strong {
      display: block;
      margin-bottom: 4px;
      color: var(--water);
      font-size: 0.78rem;
      letter-spacing: 0.14em;
      text-transform: uppercase;
    }

    .newsletter {
      position: relative;
      color: var(--paper);
      padding: clamp(74px, 9vw, 132px) 0;
      background: var(--marsh);
      overflow: hidden;
    }

    .newsletter::before {
      content: "";
      position: absolute;
      inset: -20% 40% -20% -10%;
      background: radial-gradient(circle, rgba(198, 164, 98, 0.30), transparent 38rem);
      pointer-events: none;
    }

    .newsletter-grid {
      position: relative;
      display: grid;
      grid-template-columns: minmax(0, 0.9fr) minmax(320px, 0.48fr);
      gap: clamp(24px, 6vw, 90px);
      align-items: center;
    }

    .newsletter h2 {
      margin: 0;
      max-width: 900px;
      font-family: var(--serif);
      font-size: clamp(3rem, 7vw, 7.8rem);
      line-height: 0.9;
      letter-spacing: -0.06em;
    }

    .newsletter p {
      max-width: 620px;
      color: rgba(255, 250, 240, 0.72);
      font-size: 1.08rem;
      line-height: 1.66;
    }

    .newsletter-form {
      padding: 8px;
      border-radius: 34px;
      background: rgba(255, 250, 240, 0.08);
      border: 1px solid var(--line-dark);
      box-shadow: inset 0 1px 0 rgba(255, 255, 255, 0.10);
    }

    .newsletter-form-inner {
      padding: 22px;
      border-radius: 26px;
      background: rgba(255, 250, 240, 0.92);
      color: var(--ink);
    }

    .field {
      display: grid;
      gap: 8px;
    }

    .field label {
      font-size: 0.78rem;
      letter-spacing: 0.14em;
      text-transform: uppercase;
      font-weight: 900;
      color: rgba(23, 34, 29, 0.62);
    }

    .field input {
      width: 100%;
      border: 1px solid rgba(37, 63, 53, 0.15);
      border-radius: 18px;
      background: var(--paper);
      min-height: 54px;
      padding: 0 16px;
      color: var(--ink);
      outline: none;
      transition: border 320ms var(--ease), box-shadow 320ms var(--ease);
    }

    .field input:focus {
      border-color: rgba(47, 107, 103, 0.50);
      box-shadow: 0 0 0 4px rgba(47, 107, 103, 0.10);
    }

    .form-status {
      min-height: 22px;
      margin: 12px 0 0;
      color: var(--water);
      font-weight: 800;
    }

    .footer {
      color: rgba(255, 250, 240, 0.72);
      background: #101c18;
      padding: 72px 0 34px;
    }

    .footer-grid {
      display: grid;
      grid-template-columns: minmax(0, 0.72fr) repeat(3, minmax(140px, 0.22fr));
      gap: clamp(24px, 5vw, 76px);
      align-items: start;
    }

    .footer h3,
    .footer h4 {
      margin: 0 0 16px;
      color: var(--paper);
      letter-spacing: -0.02em;
    }

    .footer h3 {
      max-width: 520px;
      font-family: var(--serif);
      font-size: clamp(2rem, 4vw, 4.8rem);
      line-height: 0.96;
      letter-spacing: -0.055em;
    }

    .footer p {
      max-width: 540px;
      line-height: 1.66;
    }

    .footer-links {
      list-style: none;
      padding: 0;
      margin: 0;
      display: grid;
      gap: 10px;
    }

    .footer-links a {
      color: rgba(255, 250, 240, 0.68);
      font-weight: 750;
    }

    .footer-links a:hover { color: var(--paper); }

    .footer-bottom {
      display: flex;
      justify-content: space-between;
      flex-wrap: wrap;
      gap: 14px;
      margin-top: 64px;
      padding-top: 24px;
      border-top: 1px solid rgba(255, 250, 240, 0.12);
      font-size: 0.86rem;
    }

    .back-to-top {
      position: fixed;
      right: 18px;
      bottom: 18px;
      z-index: 60;
      width: 46px;
      height: 46px;
      border-radius: 50%;
      display: inline-flex;
      align-items: center;
      justify-content: center;
      background: var(--marsh);
      color: var(--paper);
      box-shadow: var(--shadow);
      border: 1px solid rgba(255, 250, 240, 0.18);
      transition: transform 420ms var(--ease), background 420ms var(--ease);
    }

    .back-to-top:hover {
      transform: translateY(-3px);
      background: #1f362e;
    }

    [data-reveal] {
      opacity: 0;
      transform: translate3d(0, 36px, 0);
      transition: opacity 900ms var(--ease), transform 900ms var(--ease);
      transition-delay: var(--delay, 0ms);
    }

    [data-reveal].is-visible {
      opacity: 1;
      transform: translate3d(0, 0, 0);
    }

    .skeleton {
      position: relative;
      overflow: hidden;
      background: rgba(37, 63, 53, 0.08);
    }

    .skeleton::after {
      content: "";
      position: absolute;
      inset: 0;
      transform: translateX(-100%);
      background: linear-gradient(90deg, transparent, rgba(255, 250, 240, 0.42), transparent);
      animation: shimmer 1.6s infinite;
    }

    @keyframes shimmer {
      to { transform: translateX(100%); }
    }

    @media (max-width: 1120px) {
      .nav-island {
        display: flex;
        justify-content: space-between;
      }

      .brand {
        grid-column: 1;
        grid-row: 1;
      }

      .nav-actions {
        margin-left: auto;
        justify-self: end;
      }

      .nav-links {
        grid-column: 1 / -1;
        grid-row: 2;
        position: fixed;
        inset: 78px 14px auto 14px;
        display: grid;
        grid-template-columns: 1fr;
        gap: 8px;
        padding: 16px;
        border-radius: 30px;
        background: rgba(247, 240, 228, 0.94);
        border: 1px solid rgba(255, 250, 240, 0.7);
        box-shadow: var(--shadow-strong);
        backdrop-filter: blur(24px);
        opacity: 0;
        visibility: hidden;
        transform: translateY(-10px) scale(0.98);
        transition: opacity 420ms var(--ease), transform 420ms var(--ease), visibility 420ms var(--ease);
      }

      .nav-links.open {
        opacity: 1;
        visibility: visible;
        transform: translateY(0) scale(1);
      }

      .nav-links a {
        min-height: 50px;
        justify-content: center;
      }

      .nav-toggle {
        display: inline-flex !important;
        flex: 0 0 44px;
        opacity: 1;
        visibility: visible;
      }
      .hero-layout,
      .atlas-grid,
      .experience-grid,
      .split-story,
      .luccio-layout,
      .news-shell,
      .community-grid,
      .contact-grid,
      .newsletter-grid,
      .footer-grid {
        grid-template-columns: 1fr;
      }

      .sticky-copy { position: static; }
      .hero-media { order: -1; min-height: 620px; }
      .hero-photo-main { inset: 0 0 88px 72px; }
    }

    @media (max-width: 760px) {
      :root { --container: min(100% - 28px, 1400px); }

      .site-header { top: 10px; }
      .nav-island { border-radius: 28px; }
      .brand-name span { display: none; }
      .brand-name strong { font-size: 0.92rem; }
      .nav-actions #nav-auth { display: none; }
      .nav-mobile-login { display: inline-flex !important; }
      .nav-toggle {
        position: absolute;
        right: clamp(10px, calc(100vw - 380px), 96px);
        top: 8px;
      }
      .nav-login { min-height: 40px; padding: 0 12px; }

      .hero {
        padding: 112px 0 70px;
      }

      .hero-title {
        max-width: 100%;
        font-size: clamp(2.85rem, 13.7vw, 4.25rem);
        line-height: 0.93;
        letter-spacing: -0.025em;
      }

      .hero-media {
        min-height: 560px;
      }

      .hero-photo-main {
        inset: 22px 0 118px 18px;
      }

      .hero-photo-side {
        width: 54%;
        bottom: 58px;
      }

      .hero-photo-tiny {
        width: 50%;
        right: 0;
      }

      .hero-note {
        left: 18px;
        top: 0;
        width: 150px;
      }

      .section-head,
      .stack-card-inner,
      .post-featured {
        grid-template-columns: 1fr;
      }

      .section {
        padding: 78px 0;
      }

      .fact-grid,
      .post-card-grid {
        grid-template-columns: 1fr;
      }

      .atlas-photo .photo-slot,
      .map-plate,
      .map-plate .photo-slot {
        min-height: 460px;
      }

      .stack-card,
      .stack-card-inner,
      .stack-card .photo-slot {
        min-height: auto;
      }

      .stack-card .photo-slot {
        min-height: 320px;
      }

      .carousel {
        min-height: 560px;
      }

      .carousel-viewport {
        min-height: 460px;
        padding: 34px 0 82px;
      }

      .carousel-slide {
        top: 34px;
        width: calc(100% - 24px);
        min-height: 360px;
        height: 380px;
      }

      .carousel-slide[data-state="prev"],
      .carousel-slide[data-state="next"] {
        opacity: 0;
        transform: translate3d(-50%, 26px, 0) scale(0.82);
      }

      .carousel-caption {
        display: grid;
        align-items: start;
      }

      .photo-rail {
        grid-auto-columns: minmax(236px, 80vw);
      }

      .footer-bottom {
        display: grid;
      }
    }

    @media (prefers-reduced-motion: reduce) {
      *,
      *::before,
      *::after {
        animation-duration: 0.001ms !important;
        animation-iteration-count: 1 !important;
        scroll-behavior: auto !important;
        transition-duration: 0.001ms !important;
      }

      [data-reveal] {
        opacity: 1;
        transform: none;
      }
    }
  </style>
</head>
<body>
  <a class="skip-link" href="#main">Salta al contenuto</a>

  <header class="site-header" id="navbar">
    <div class="nav-island">
      <a class="brand" href="#home" aria-label="Pro Loco Rivalta sul Mincio">
        <span class="brand-mark" aria-hidden="true"></span>
        <span class="brand-name">
          <strong>Pro Loco Rivalta</strong>
          <span>sul Mincio - Mantova</span>
        </span>
      </a>

      <nav class="nav-links" id="navList" aria-label="Navigazione principale">
        <a href="#home" class="active">Home</a>
        <a href="#esperienze">Esperienze</a>
        <a href="#galleria">Galleria</a>
        <a href="#valli">Valli</a>
        <a href="#luccio">Luccio</a>
        <a href="#news">News</a>
        <a href="#contatti">Contatti</a>
        <a href="login" class="nav-mobile-login">Accedi</a>
      </nav>

      <div class="nav-actions">
        <div id="nav-auth">
          <a href="login" class="nav-login">Accedi</a>
        </div>
        <button class="nav-toggle" id="navToggle" type="button" aria-label="Apri menu" aria-expanded="false" aria-controls="navList">
          <span></span>
          <span></span>
          <span></span>
        </button>
      </div>
    </div>
  </header>

  <main id="main" class="page-shell">
    <section class="hero" id="home">
      <div class="container hero-layout">
        <div class="hero-copy-block" data-reveal>
          <p class="eyebrow">Parco del Mincio - Mantova</p>
          <h1 class="hero-title">Rivalta sul Mincio, dove l&rsquo;acqua <em>diventa paese</em>.</h1>
          <p class="hero-copy">Un borgo di pescatori nel cuore delle Valli del Mincio: canneti, chiari, aironi, ciclabili lente e una cucina d&rsquo;acqua che tiene insieme memoria e futuro.</p>
          <div class="button-row">
            <a class="button magnetic" href="#galleria"><span>Guarda la galleria</span><span class="button-icon" aria-hidden="true">&rarr;</span></a>
            <a class="button secondary magnetic" href="#esperienze"><span>Scopri cosa fare</span><span class="button-icon" aria-hidden="true">&rarr;</span></a>
          </div>
        </div>

        <div class="hero-media" aria-label="Spazi fotografici principali">
          <div class="hero-note" data-reveal style="--delay: 140ms">Tante foto, un solo racconto.<span>Ogni riquadro ha uno slot stabile per i tuoi scatti.</span></div>
          <figure class="photo-frame hero-photo-main" data-parallax="0.12" data-reveal style="--delay: 80ms">
            <div class="photo-slot" data-photo-slot="hero-main" data-photo-label="Foto hero principale" style="--photo:url('https://picsum.photos/seed/rivalta-mincio-hero-main/1200/1500')" role="img" aria-label="Scorcio fotografico delle Valli del Mincio"></div>
          </figure>
          <figure class="photo-frame hero-photo-side" data-parallax="-0.06" data-reveal style="--delay: 180ms">
            <div class="photo-slot" data-photo-slot="hero-reeds" data-photo-label="Canneti e acqua" style="--photo:url('https://picsum.photos/seed/rivalta-reeds-water/900/1100')" role="img" aria-label="Canneti lungo il Mincio"></div>
          </figure>
          <figure class="photo-frame hero-photo-tiny" data-parallax="0.2" data-reveal style="--delay: 260ms">
            <div class="photo-slot" data-photo-slot="hero-community" data-photo-label="Comunit&agrave;" style="--photo:url('https://picsum.photos/seed/rivalta-local-life/850/850')" role="img" aria-label="Vita locale a Rivalta sul Mincio"></div>
          </figure>
        </div>
      </div>
    </section>

    <section class="marquee-band" aria-label="Segni del territorio">
      <div class="marquee-track" aria-hidden="true">
        <span>Valli del Mincio</span><span>Birdwatching</span><span>Canoa e barca</span><span>Sagra del Luccio</span><span>Ciclabile Mantova-Peschiera</span><span>Rete Natura 2000</span>
        <span>Valli del Mincio</span><span>Birdwatching</span><span>Canoa e barca</span><span>Sagra del Luccio</span><span>Ciclabile Mantova-Peschiera</span><span>Rete Natura 2000</span>
      </div>
    </section>

    <section class="section compact" id="introduzione">
      <div class="container">
        <div class="section-head">
          <div data-reveal>
            <p class="section-kicker">Atlante vivo</p>
            <h2 class="section-title">Il centro della provincia ha il ritmo lento delle <em>zone umide</em>.</h2>
          </div>
          <p class="section-lead" data-reveal style="--delay: 140ms">Rivalta sul Mincio &egrave; una base naturale per leggere il territorio: il fiume rallenta, si allarga in canali e canneti, e la Pro Loco &ldquo;Amici di Rivalta&rdquo; custodisce itinerari, tradizioni e accoglienza.</p>
        </div>

        <div class="atlas-grid">
          <article class="atlas-panel atlas-photo" data-reveal>
            <div class="atlas-panel-inner">
              <div class="photo-slot" data-photo-slot="atlas-wide-wetlands" data-photo-label="Ampio scatto delle valli" style="--photo:url('https://picsum.photos/seed/rivalta-atlas-wetlands/1500/1000')" role="img" aria-label="Vista ampia delle Valli del Mincio"></div>
            </div>
          </article>

          <article class="atlas-panel" data-reveal style="--delay: 120ms">
            <div class="atlas-panel-inner atlas-copy">
              <div>
                <h2>Acqua, canneti, paese.</h2>
                <p>Qui il paesaggio &egrave; fatto di margini sottili: barche, argini, praterie a carice, chiari e case basse. La homepage diventa una mappa emotiva dove sostituire ogni placeholder con le tue foto.</p>
              </div>
              <div class="fact-grid" aria-label="Dati del territorio">
                <div class="fact"><strong data-count="260">0</strong><span>specie di uccelli censite nel sistema palustre</span></div>
                <div class="fact"><strong data-count="1100">0</strong><span>ettari di riserva nel cuore delle Valli</span></div>
                <div class="fact"><strong data-count="43">0</strong><span>chilometri di Mincio dal Garda al Po</span></div>
                <div class="fact"><strong data-count="1967">0</strong><span>anno di fondazione della Pro Loco</span></div>
              </div>
            </div>
          </article>
        </div>
      </div>
    </section>

    <section class="section" id="esperienze">
      <div class="container experience-grid">
        <div class="sticky-copy" data-reveal>
          <p class="section-kicker">Esperienze</p>
          <h2 class="section-title">Quattro modi per entrare nel <em>Mincio</em>.</h2>
          <p class="section-lead">Una sezione pensata come stack fotografico: ogni card pu&ograve; diventare una storia, un evento o un pacchetto visita quando avrai le immagini finali.</p>
          <div class="button-row">
            <a class="button secondary magnetic" href="#contatti"><span>Organizza una visita</span><span class="button-icon" aria-hidden="true">&rarr;</span></a>
          </div>
        </div>

        <div class="stack-list">
          <article class="stack-card" data-stack-card data-reveal>
            <div class="stack-card-inner">
              <div class="photo-slot" data-photo-slot="experience-boat" data-photo-label="Barca nelle valli" style="--photo:url('https://picsum.photos/seed/rivalta-boat-experience/1100/900')" role="img" aria-label="Escursione in barca nelle Valli del Mincio"></div>
              <div class="stack-card-copy">
                <div>
                  <h3>Dal livello dell&rsquo;acqua.</h3>
                  <p>Partenze dagli imbarcaderi, canali stretti, silenzio dei canneti e un punto di vista che cambia il modo di leggere la valle.</p>
                </div>
                <div class="stack-meta"><span class="tag">barca</span><span class="tag">canoa</span><span class="tag">guide locali</span></div>
              </div>
            </div>
          </article>

          <article class="stack-card" data-stack-card data-reveal>
            <div class="stack-card-inner">
              <div class="photo-slot" data-photo-slot="experience-bike" data-photo-label="Ciclabile" style="--photo:url('https://picsum.photos/seed/rivalta-bike-path/1100/900')" role="img" aria-label="Ciclabile nei pressi di Rivalta"></div>
              <div class="stack-card-copy">
                <div>
                  <h3>La valle a pedali.</h3>
                  <p>Rivalta &egrave; una tappa ideale tra Mantova e Peschiera del Garda, con deviazioni lente verso argini, pontili e scorci d&rsquo;acqua.</p>
                </div>
                <div class="stack-meta"><span class="tag">cicloturismo</span><span class="tag">argini</span><span class="tag">soste</span></div>
              </div>
            </div>
          </article>

          <article class="stack-card" data-stack-card data-reveal>
            <div class="stack-card-inner">
              <div class="photo-slot" data-photo-slot="experience-birdwatching" data-photo-label="Birdwatching" style="--photo:url('https://picsum.photos/seed/rivalta-birdwatching/1100/900')" role="img" aria-label="Birdwatching nelle Valli del Mincio"></div>
              <div class="stack-card-copy">
                <div>
                  <h3>Aironi, chiari, canneti.</h3>
                  <p>La zona umida &egrave; un archivio naturale aperto: specie migratrici, nidificazioni, fioriture estive e tracce da osservare con rispetto.</p>
                </div>
                <div class="stack-meta"><span class="tag">fauna</span><span class="tag">Ramsar</span><span class="tag">stagioni</span></div>
              </div>
            </div>
          </article>
        </div>
      </div>
    </section>

    <section class="section" id="galleria">
      <div class="container">
        <div class="section-head">
          <div data-reveal>
            <p class="section-kicker">Galleria</p>
            <h2 class="section-title">Un carosello per le foto che arriveranno.</h2>
          </div>
          <p class="section-lead" data-reveal style="--delay: 120ms">Una galleria viva: foto morbide e fluttuanti, autoplay, frecce, punti, tastiera, swipe e scorrimento manuale orizzontale per sfogliare liberamente.</p>
        </div>

        <div class="gallery-stage" data-reveal>
          <div class="carousel" id="rsmCarousel" tabindex="0" role="region" aria-roledescription="carousel" aria-label="Galleria fotografica di Rivalta sul Mincio">
            <div class="carousel-viewport">
              <article class="carousel-slide" aria-label="Valli del Mincio">
                <div class="carousel-card">
                  <div class="photo-slot" data-photo-slot="carousel-valli" data-photo-label="Valli del Mincio" style="--photo:url('https://picsum.photos/seed/rivalta-carousel-valli/1400/900')" role="img" aria-label="Valli del Mincio"></div>
                </div>
                <div class="carousel-caption"><h3>Valli del Mincio</h3><p>Lo scatto ampio per aprire il racconto naturalistico.</p></div>
              </article>

              <article class="carousel-slide" aria-label="Rivalta dal porto">
                <div class="carousel-card">
                  <div class="photo-slot" data-photo-slot="carousel-porto" data-photo-label="Porto e borgo" style="--photo:url('https://picsum.photos/seed/rivalta-carousel-porto/1400/900')" role="img" aria-label="Porto di Rivalta sul Mincio"></div>
                </div>
                <div class="carousel-caption"><h3>Porto e borgo</h3><p>Case, barche, argini e arrivi lenti.</p></div>
              </article>

              <article class="carousel-slide" aria-label="Sagra del Luccio">
                <div class="carousel-card">
                  <div class="photo-slot" data-photo-slot="carousel-sagra-luccio" data-photo-label="Sagra del Luccio" style="--photo:url('https://picsum.photos/seed/rivalta-carousel-sagra/1400/900')" role="img" aria-label="Tavola della Sagra del Luccio"></div>
                </div>
                <div class="carousel-caption"><h3>Sagra del Luccio</h3><p>La tradizione gastronomica come immagine centrale.</p></div>
              </article>

              <article class="carousel-slide" aria-label="Ciclabile del Mincio">
                <div class="carousel-card">
                  <div class="photo-slot" data-photo-slot="carousel-ciclabile" data-photo-label="Ciclabile" style="--photo:url('https://picsum.photos/seed/rivalta-carousel-cycling/1400/900')" role="img" aria-label="Percorso ciclabile del Mincio"></div>
                </div>
                <div class="carousel-caption"><h3>Ciclabile</h3><p>Una foto orizzontale perfetta per raccontare il viaggio.</p></div>
              </article>

              <article class="carousel-slide" aria-label="Comunit&agrave; Pro Loco">
                <div class="carousel-card">
                  <div class="photo-slot" data-photo-slot="carousel-comunita" data-photo-label="Comunit&agrave;" style="--photo:url('https://picsum.photos/seed/rivalta-carousel-community/1400/900')" role="img" aria-label="Comunit&agrave; della Pro Loco"></div>
                </div>
                <div class="carousel-caption"><h3>Comunit&agrave;</h3><p>Volti, volontari, eventi, mani al lavoro.</p></div>
              </article>
            </div>

            <div class="carousel-controls" aria-label="Controlli galleria">
              <button class="carousel-btn" type="button" data-carousel-prev aria-label="Foto precedente">&larr;</button>
              <div class="carousel-dots" data-carousel-dots></div>
              <button class="carousel-btn" type="button" data-carousel-next aria-label="Foto successiva">&rarr;</button>
            </div>
          </div>
        </div>
      </div>
    </section>

    <section class="section" id="valli">
      <div class="container">
        <div class="section-head">
          <div data-reveal>
            <p class="section-kicker">Riserva naturale - Sito Ramsar</p>
            <h2 class="section-title">Le Valli del Mincio, oasi d&rsquo;acqua e <em>silenzio</em>.</h2>
          </div>
          <p class="section-lead" data-reveal style="--delay: 120ms">Tra Rivalta e Borgo Angeli il fiume rallenta e diventa un mosaico di canneti, chiari e praterie a carice. Una zona umida riconosciuta dalla Convenzione di Ramsar e dalla rete Natura 2000.</p>
        </div>

        <div class="photo-rail" data-horizontal-gallery data-reveal>
          <figure class="rail-item"><div class="photo-slot" data-photo-slot="rail-canneti" data-photo-label="Canneti" style="--photo:url('https://picsum.photos/seed/rivalta-rail-reeds/900/1200')" role="img" aria-label="Canneti delle Valli del Mincio"></div></figure>
          <figure class="rail-item"><div class="photo-slot" data-photo-slot="rail-chiari" data-photo-label="Chiari" style="--photo:url('https://picsum.photos/seed/rivalta-rail-water/900/1200')" role="img" aria-label="Specchi d'acqua nelle Valli"></div></figure>
          <figure class="rail-item"><div class="photo-slot" data-photo-slot="rail-ninfee" data-photo-label="Ninfee" style="--photo:url('https://picsum.photos/seed/rivalta-rail-waterlilies/900/1200')" role="img" aria-label="Fioritura delle ninfee"></div></figure>
          <figure class="rail-item"><div class="photo-slot" data-photo-slot="rail-aironi" data-photo-label="Aironi" style="--photo:url('https://picsum.photos/seed/rivalta-rail-herons/900/1200')" role="img" aria-label="Aironi nel Parco del Mincio"></div></figure>
        </div>
      </div>
    </section>

    <section class="section compact" id="luccio">
      <div class="container split-story">
        <div class="story-card" data-reveal>
          <p class="section-kicker">Il simbolo del borgo</p>
          <h3>Il luccio tiene insieme tavola, festa e memoria.</h3>
          <p>Il Luccio in Bianco e il Luccio in Salsa alla Rivaltese sono ricette PAT dal 2013. La homepage lascia spazio a immagini di cucina, volontari, piatti e tavolate della sagra.</p>
          <ul class="story-list">
            <li>Ricette tramandate, legate alla pesca e alla cucina d&rsquo;acqua dolce.</li>
            <li>Un racconto gastronomico da collegare a foto di dettaglio e reportage.</li>
            <li>CTA verso news, eventi e contatti senza interrompere l&rsquo;esperienza.</li>
            <li>Spazio dedicato alla Festa del Pesce annuale, con programma e men&ugrave; in evidenza.</li>
          </ul>
          <div class="button-row">
            <a class="button magnetic" href="#news"><span>Leggi le ultime</span><span class="button-icon" aria-hidden="true">&rarr;</span></a>
          </div>
        </div>

        <div class="luccio-layout" data-reveal style="--delay: 120ms">
          <article class="recipe-panel">
            <h3>Bianco alla Rivaltese</h3>
            <p>Limone, cedro di Gargnano, olio EVO, Grana Padano e aromi. Uno slot foto dedicato al piatto finito pu&ograve; vivere qui.</p>
            <div class="recipe-list"><span class="tag">PAT 2013</span><span class="tag">ricetta locale</span></div>
          </article>
          <article class="recipe-panel">
            <h3>Salsa alla Rivaltese</h3>
            <p>Cipolla, capperi, peperoni, acciughe, spezie e polenta. Il blocco &egrave; pronto per foto di cucina o servizio.</p>
            <div class="recipe-list"><span class="tag">sagra</span><span class="tag">tradizione</span></div>
          </article>
          <article class="recipe-panel">
            <h3>Festa del Pesce</h3>
            <p>Area speciale in stile men&ugrave;: antipasti di lago, luccio in bianco, luccio in salsa, grigliate e dolci locali. Perfetta per aggiornare date e programma dell&rsquo;evento annuale.</p>
            <div class="recipe-list"><span class="tag">evento annuale</span><span class="tag">men&ugrave;</span><span class="tag">azzurrino</span><span class="tag">famiglie</span><span class="tag">musica</span></div>
          </article>
          <figure class="photo-frame" style="grid-column: 1 / -1;">
            <div class="photo-slot" data-photo-slot="luccio-table" data-photo-label="Foto tavola e luccio" style="--photo:url('https://picsum.photos/seed/rivalta-luccio-table/1200/760')" role="img" aria-label="Tavola dedicata al luccio"></div>
          </figure>
        </div>
      </div>
    </section>

    <section class="section" id="news">
      <div class="container">
        <div class="section-head">
          <div data-reveal>
            <p class="section-kicker">News e articoli</p>
            <h2 class="section-title">Le ultime dal territorio.</h2>
          </div>
          <p class="section-lead" data-reveal style="--delay: 120ms">Eventi, escursioni, avvisi e racconti caricati dalla redazione tramite Supabase. Se non ci sono post, resta un vuoto curato e coerente con il nuovo design.</p>
        </div>

        <div class="news-shell">
          <main class="news-main" aria-label="Ultime news" data-reveal>
            <div class="news-inner">
              <div class="news-filter-row" id="news-filter-row" aria-label="Filtra news per categoria"></div>
              <div class="news-status" id="news-status">Caricamento articoli in corso.</div>
              <article class="post-featured skeleton" id="post-featured" style="display:none"></article>
              <div class="post-card-grid" id="posts-grid"></div>
              <div class="news-empty" id="news-empty" style="display:none">
                <div>
                  <strong>Nessun articolo pubblicato</strong>
                  <span>I contenuti verranno mostrati qui appena la redazione li pubblica.</span>
                </div>
              </div>
            </div>
          </main>

        </div>
      </div>
    </section>

    <section class="section dark" id="chi-siamo">
      <div class="container community-grid">
        <div class="community-panel" data-reveal>
          <p class="section-kicker">Chi siamo</p>
          <h2>Una comunit&agrave; che lavora per il territorio.</h2>
          <p>L&rsquo;Associazione Pro Loco &ldquo;Amici di Rivalta&rdquo; A.P.S. nasce il 26 aprile 1967. Promuove iniziative culturali, ambientali e gastronomiche, con una rete di volontari che tiene vivo il rapporto tra borgo, acqua e visitatori.</p>
          <div class="button-row">
            <a class="button secondary magnetic" href="#contatti"><span>Contatta la Pro Loco</span><span class="button-icon" aria-hidden="true">&rarr;</span></a>
          </div>
        </div>
        <div class="community-list">
          <article class="community-item" data-reveal style="--delay: 80ms"><strong>Dal 1967</strong><span>Promozione turistica, tutela ambientale, cultura locale e supporto agli eventi del borgo.</span></article>
          <article class="community-item" data-reveal style="--delay: 160ms"><strong>Amici del Mincio</strong><span>Sezione naturalistica dedicata a ricerca, monitoraggio e conoscenza delle Valli.</span></article>
          <article class="community-item" data-reveal style="--delay: 240ms"><strong>Canoa Club</strong><span>Sezione sportiva per vivere il paesaggio dall&rsquo;acqua, con escursioni e assistenza.</span></article>
        </div>
      </div>
    </section>

    <section class="section" id="dove-siamo">
      <div class="container">
        <div class="section-head">
          <div data-reveal>
            <p class="section-kicker">Dove siamo</p>
            <h2 class="section-title">Arrivare a Rivalta &egrave; gi&agrave; parte del viaggio.</h2>
          </div>
          <p class="section-lead" data-reveal style="--delay: 120ms">Rivalta sul Mincio &egrave; frazione del Comune di Rodigo, a circa 10 km da Mantova. L&rsquo;ufficio turistico Pro Loco si trova in Via Porto, 31.</p>
        </div>

        <div class="contact-grid">
          <figure class="map-plate" data-reveal>
            <iframe
              title="Google Maps - Pro Loco Rivalta sul Mincio"
              src="https://www.google.com/maps?q=Via+Porto+31,+46040+Rivalta+sul+Mincio+MN&output=embed"
              loading="lazy"
              referrerpolicy="no-referrer-when-downgrade"
              style="width:100%;min-height:420px;border:0;border-radius:30px;"
              allowfullscreen></iframe>
          </figure>
          <article class="contact-card" data-reveal style="--delay: 120ms">
            <div>
              <h3>Via Porto, 31</h3>
              <p class="section-lead">46040 Rivalta sul Mincio, frazione di Rodigo, provincia di Mantova.</p>
            </div>
            <div class="contact-lines">
              <div class="contact-line"><strong>Email</strong><a href="mailto:info@prolocorivalta.mn.it">info@prolocorivalta.mn.it</a></div>
              <div class="contact-line"><strong>Telefono</strong><a href="tel:+393398995680">+39 339 899 5680</a></div>
              <div class="contact-line"><strong>Accessi</strong><span>Auto dalla SP 18, bici lungo la ciclabile del Mincio, barca dagli imbarcaderi della valle.</span></div>
            </div>
            <div class="button-row">
              <a class="button secondary magnetic" href="https://www.google.com/maps/dir/?api=1&destination=Via+Porto+31,+46040+Rivalta+sul+Mincio+MN" target="_blank" rel="noopener">
                <span>Apri percorso su Google Maps</span><span class="button-icon" aria-hidden="true">&rarr;</span>
              </a>
            </div>
          </article>
        </div>
      </div>
    </section>

    <section class="newsletter" aria-labelledby="newsletter-title">
      <div class="container newsletter-grid">
        <div data-reveal>
          <p class="section-kicker">Resta in contatto</p>
          <h2 id="newsletter-title">Il Mincio nella tua casella di posta.</h2>
          <p>Una mail al mese con eventi, sagre, escursioni e racconti dalle Valli del Mincio. Niente rumore, solo cose utili.</p>
        </div>
        <form class="newsletter-form" id="newsletter-form" data-reveal style="--delay: 120ms" novalidate>
          <div class="newsletter-form-inner">
            <div class="field">
              <label for="newsletter-email">Email</label>
              <input id="newsletter-email" type="email" name="email" autocomplete="email" placeholder="nome@email.it" required />
            </div>
            <p class="form-status" id="newsletter-status" role="status"></p>
            <button class="button magnetic" type="submit"><span>Iscrivimi</span><span class="button-icon" aria-hidden="true">&rarr;</span></button>
            <p style="margin:16px 0 0;color:rgba(23,34,29,.58);font-size:.9rem;">Iscrivendoti accetti la <a href="privacy" style="text-decoration:underline;">privacy policy</a>. Puoi disiscriverti in qualsiasi momento.</p>
          </div>
        </form>
      </div>
    </section>
  </main>

  <footer class="footer" id="contatti">
    <div class="container">
      <div class="footer-grid">
        <div>
          <h3>Pro Loco Rivalta sul Mincio</h3>
          <p>Ecoturismo, cultura locale, eventi, Sagra del Luccio e promozione ambientale nel cuore del Parco Regionale del Mincio.</p>
        </div>
        <nav aria-label="Sezioni">
          <h4>Esplora</h4>
          <ul class="footer-links">
            <li><a href="#esperienze">Esperienze</a></li>
            <li><a href="#galleria">Galleria</a></li>
            <li><a href="#valli">Valli</a></li>
            <li><a href="#news">News</a></li>
          </ul>
        </nav>
        <nav aria-label="Area utente">
          <h4>Area</h4>
          <ul class="footer-links">
            <li><a href="login">Accedi</a></li>
            <li><a href="me">Dashboard</a></li>
            <li><a href="write">Scrivi</a></li>
            <li><a href="profile">Profili</a></li>
          </ul>
        </nav>
        <nav aria-label="Legale e social">
          <h4>Link</h4>
          <ul class="footer-links">
            <li><a href="privacy">Privacy</a></li>
            <li><a href="cookie">Cookie</a></li>
            <li><a href="note-legali">Note legali</a></li>
            <li><a href="https://www.facebook.com/prolocoamicidirivalta/" target="_blank" rel="noopener noreferrer">Facebook</a></li>
            <li><a href="https://www.instagram.com/prolocrivalta" target="_blank" rel="noopener noreferrer">Instagram</a></li>
          </ul>
        </nav>
      </div>
      <div class="footer-bottom">
        <span>&copy; <span id="year"></span> Pro Loco Rivalta sul Mincio A.P.S.</span>
        <span>Via Porto, 31 - 46040 Rivalta sul Mincio (MN)</span>
      </div>
    </div>
  </footer>

  <a class="back-to-top" href="#home" aria-label="Torna su">&uarr;</a>

  <script src="https://cdn.jsdelivr.net/npm/@supabase/supabase-js@2/dist/umd/supabase.min.js"></script>
  <script src="supabase.config.js"></script>
  <script src="https://cdn.jsdelivr.net/npm/gsap@3.12.5/dist/gsap.min.js"></script>
  <script src="https://cdn.jsdelivr.net/npm/gsap@3.12.5/dist/ScrollTrigger.min.js"></script>
  <script>
  (function () {
    'use strict';

    var reduceMotion = window.matchMedia && window.matchMedia('(prefers-reduced-motion: reduce)').matches;
    var CATEGORY_ORDER = ['Eventi', 'Territorio', 'Vita locale', 'Avvisi', 'Richieste', 'Storie'];
    var LEGACY_TO_CANONICAL = {
      'eventi': 'Eventi',
      'territorio': 'Territorio',
      'vita locale': 'Vita locale',
      'avvisi': 'Avvisi',
      'richieste': 'Richieste',
      'storie': 'Storie',
      'il luccio': 'Storie',
      'fauna': 'Territorio',
      'ambiente': 'Territorio',
      'ecoturismo': 'Territorio',
      'storia': 'Storie'
    };
    var BADGE_COLOR_BY_CATEGORY = {
      'Eventi': 'green',
      'Territorio': 'default',
      'Vita locale': 'default',
      'Avvisi': 'green',
      'Richieste': 'green',
      'Storie': 'default'
    };

    function $(selector, root) {
      return (root || document).querySelector(selector);
    }

    function $all(selector, root) {
      return Array.prototype.slice.call((root || document).querySelectorAll(selector));
    }

    function escapeHtml(value) {
      return String(value || '')
        .replace(/&/g, '&amp;')
        .replace(/</g, '&lt;')
        .replace(/>/g, '&gt;')
        .replace(/"/g, '&quot;')
        .replace(/'/g, '&#39;');
    }

    function canonicalCategory(input) {
      var raw = String(input || '').trim();
      if (!raw) return '';
      var normalized = raw.toLowerCase();
      if (LEGACY_TO_CANONICAL[normalized]) return LEGACY_TO_CANONICAL[normalized];
      for (var i = 0; i < CATEGORY_ORDER.length; i += 1) {
        if (CATEGORY_ORDER[i].toLowerCase() === normalized) return CATEGORY_ORDER[i];
      }
      return raw;
    }

    function badgeColor(input) {
      return BADGE_COLOR_BY_CATEGORY[canonicalCategory(input)] || 'default';
    }

    function categoryHref(input) {
      return 'category?name=' + encodeURIComponent(canonicalCategory(input) || '');
    }

    window.RSM_CATEGORIES = {
      all: function () { return CATEGORY_ORDER.slice(); },
      canonical: canonicalCategory,
      badgeColor: badgeColor,
      categoryHref: categoryHref
    };

    function initNav() {
      var toggle = $('#navToggle');
      var nav = $('#navList');
      if (!toggle || !nav) return;

      function closeMenu() {
        nav.classList.remove('open');
        toggle.classList.remove('open');
        toggle.setAttribute('aria-expanded', 'false');
        document.body.classList.remove('menu-open');
      }

      toggle.addEventListener('click', function () {
        var open = !nav.classList.contains('open');
        nav.classList.toggle('open', open);
        toggle.classList.toggle('open', open);
        toggle.setAttribute('aria-expanded', String(open));
        document.body.classList.toggle('menu-open', open);
      });

      $all('a[href^="#"]', nav).forEach(function (link) {
        link.addEventListener('click', closeMenu);
      });

      document.addEventListener('keydown', function (event) {
        if (event.key === 'Escape') closeMenu();
      });

      var sectionIds = ['home', 'esperienze', 'galleria', 'valli', 'luccio', 'news', 'chi-siamo', 'dove-siamo', 'contatti'];
      var links = $all('a[href^="#"]', nav);
      var observer = new IntersectionObserver(function (entries) {
        entries.forEach(function (entry) {
          if (!entry.isIntersecting) return;
          links.forEach(function (link) {
            link.classList.toggle('active', link.getAttribute('href') === '#' + entry.target.id);
          });
        });
      }, { rootMargin: '-42% 0px -52% 0px', threshold: 0.01 });

      sectionIds.forEach(function (id) {
        var section = document.getElementById(id);
        if (section) observer.observe(section);
      });
    }

    function initReveal() {
      var items = $all('[data-reveal]');
      if (!items.length) return;
      if (reduceMotion || !('IntersectionObserver' in window)) {
        items.forEach(function (item) { item.classList.add('is-visible'); });
        return;
      }

      var observer = new IntersectionObserver(function (entries) {
        entries.forEach(function (entry) {
          if (entry.isIntersecting) {
            entry.target.classList.add('is-visible');
            observer.unobserve(entry.target);
          }
        });
      }, { threshold: 0.14 });

      items.forEach(function (item) { observer.observe(item); });
    }

    function initCounters() {
      var counters = $all('[data-count]');
      if (!counters.length) return;
      if (reduceMotion || !('IntersectionObserver' in window)) {
        counters.forEach(function (el) { el.textContent = el.dataset.count || '0'; });
        return;
      }

      var observer = new IntersectionObserver(function (entries) {
        entries.forEach(function (entry) {
          if (!entry.isIntersecting) return;
          var el = entry.target;
          var target = parseInt(el.dataset.count, 10) || 0;
          var start = performance.now();
          var duration = 1300;
          function frame(now) {
            var t = Math.min((now - start) / duration, 1);
            var eased = 1 - Math.pow(1 - t, 3);
            el.textContent = String(Math.round(target * eased));
            if (t < 1) requestAnimationFrame(frame);
          }
          requestAnimationFrame(frame);
          observer.unobserve(el);
        });
      }, { threshold: 0.45 });

      counters.forEach(function (el) { observer.observe(el); });
    }

    function initMagneticButtons() {
      $all('.magnetic').forEach(function (button) {
        button.addEventListener('pointermove', function (event) {
          var rect = button.getBoundingClientRect();
          var x = ((event.clientX - rect.left) / rect.width) * 100;
          var y = ((event.clientY - rect.top) / rect.height) * 100;
          button.style.setProperty('--mx', x + '%');
          button.style.setProperty('--my', y + '%');
        });
      });
    }

    function initNewsletter() {
      var form = $('#newsletter-form');
      var input = $('#newsletter-email');
      var status = $('#newsletter-status');
      if (!form || !input || !status) return;

      form.addEventListener('submit', function (event) {
        event.preventDefault();
        var value = input.value.trim();
        if (!value || !/^[^\s@]+@[^\s@]+\.[^\s@]+$/.test(value)) {
          status.textContent = 'Inserisci un indirizzo email valido.';
          input.focus();
          return;
        }
        status.textContent = 'Grazie, iscrizione registrata in questa sessione.';
        form.reset();
      });
    }

    function initCarousel() {
      var root = $('#rsmCarousel');
      if (!root) return;
      var slides = $all('.carousel-slide', root);
      var prev = $('[data-carousel-prev]', root);
      var next = $('[data-carousel-next]', root);
      var dotsRoot = $('[data-carousel-dots]', root);
      var index = 0;
      var timer = null;
      var startX = 0;
      var pointerDown = false;

      function setSlide(nextIndex) {
        index = (nextIndex + slides.length) % slides.length;
        slides.forEach(function (slide, i) {
          var state = 'hidden';
          if (i === index) state = 'active';
          else if (i === (index - 1 + slides.length) % slides.length) state = 'prev';
          else if (i === (index + 1) % slides.length) state = 'next';
          slide.dataset.state = state;
          slide.setAttribute('aria-hidden', state === 'active' ? 'false' : 'true');
        });
        $all('.carousel-dot', dotsRoot).forEach(function (dot, i) {
          dot.classList.toggle('active', i === index);
          dot.setAttribute('aria-current', i === index ? 'true' : 'false');
        });
      }

      function stop() {
        if (timer) window.clearInterval(timer);
        timer = null;
      }

      function play() {
        if (reduceMotion) return;
        stop();
        timer = window.setInterval(function () { setSlide(index + 1); }, 4600);
      }

      slides.forEach(function (_slide, i) {
        var dot = document.createElement('button');
        dot.className = 'carousel-dot';
        dot.type = 'button';
        dot.setAttribute('aria-label', 'Vai alla foto ' + (i + 1));
        dot.addEventListener('click', function () {
          setSlide(i);
          play();
        });
        dotsRoot.appendChild(dot);
      });

      if (prev) prev.addEventListener('click', function () { setSlide(index - 1); play(); });
      if (next) next.addEventListener('click', function () { setSlide(index + 1); play(); });

      root.addEventListener('keydown', function (event) {
        if (event.key === 'ArrowLeft') {
          event.preventDefault();
          setSlide(index - 1);
          play();
        }
        if (event.key === 'ArrowRight') {
          event.preventDefault();
          setSlide(index + 1);
          play();
        }
      });

      root.addEventListener('pointerdown', function (event) {
        pointerDown = true;
        startX = event.clientX;
        stop();
        if (root.setPointerCapture) root.setPointerCapture(event.pointerId);
      });

      root.addEventListener('pointerup', function (event) {
        if (!pointerDown) return;
        pointerDown = false;
        var delta = event.clientX - startX;
        if (Math.abs(delta) > 42) setSlide(index + (delta < 0 ? 1 : -1));
        play();
      });

      root.addEventListener('pointercancel', function () {
        pointerDown = false;
        play();
      });
      root.addEventListener('wheel', function (event) {
        if (Math.abs(event.deltaX) < 8 && Math.abs(event.deltaY) < 8) return;
        event.preventDefault();
        if (Math.abs(event.deltaX) > Math.abs(event.deltaY)) {
          setSlide(index + (event.deltaX > 0 ? 1 : -1));
        } else {
          setSlide(index + (event.deltaY > 0 ? 1 : -1));
        }
        play();
      }, { passive: false });

      root.addEventListener('mouseenter', stop);
      root.addEventListener('mouseleave', play);
      root.addEventListener('focusin', stop);
      root.addEventListener('focusout', play);

      setSlide(0);
      play();
    }

    function initAuth() {
      var nav = $('#nav-auth');
      if (!nav) return;
      if (typeof supabase === 'undefined' || typeof SUPABASE_URL === 'undefined' || SUPABASE_URL.indexOf('your-project') >= 0) {
        nav.innerHTML = '<a href="login" class="nav-login">Accedi</a>';
        return;
      }

      var client = supabase.createClient(SUPABASE_URL, SUPABASE_ANON_KEY);
      var outsideBound = false;

      function initials(username) {
        return String(username || 'utente').trim().slice(0, 2).toUpperCase();
      }

      function closeDropdown() {
        var dropdown = $('#profileDropdown');
        var button = $('#profileBtn');
        if (dropdown) dropdown.classList.remove('open');
        if (button) button.setAttribute('aria-expanded', 'false');
      }

      function item(label, href, active, primary) {
        return '<a href="' + href + '" role="menuitem" class="' + (active ? 'active ' : '') + (primary ? 'item-primary' : '') + '"><span>' + label + '</span><span aria-hidden="true">&rarr;</span></a>';
      }

      function buildMenu(username, color) {
        var safe = escapeHtml(username);
        var current = window.location.pathname;
        var profileHref = 'profile?u=' + encodeURIComponent(username);
        var isProfile = current.indexOf('profile') >= 0;
        var isDashboard = current.indexOf('me') >= 0;
        var isWrite = current.indexOf('write') >= 0;
        var editItem = isProfile && new URLSearchParams(window.location.search).get('u') === username
          ? item('Modifica profilo', 'modifica-profilo', false, false)
          : '';

        return '<div class="profile-menu" id="profileMenu">' +
          '<button class="profile-btn" id="profileBtn" type="button" aria-haspopup="true" aria-expanded="false">' +
          '<span class="profile-avatar" style="background:' + escapeHtml(color || '#2f6b67') + '">' + escapeHtml(initials(username)) + '</span>' +
          '<span>@' + safe + '</span>' +
          '</button>' +
          '<div class="profile-dropdown" id="profileDropdown" role="menu" aria-label="Menu navigazione">' +
          '<div class="profile-dropdown__section-title">Navigazione</div>' +
          item('Profilo', profileHref, isProfile, false) +
          item('Dashboard', 'me', isDashboard, false) +
          editItem +
          item('Scrivi', 'write', isWrite, true) +
          '<div class="profile-dropdown__sep"></div>' +
          '<button type="button" data-action="logout" class="danger" role="menuitem"><span>Esci</span><span aria-hidden="true">&rarr;</span></button>' +
          '</div></div>';
      }

      function bindMenu() {
        var button = $('#profileBtn');
        var dropdown = $('#profileDropdown');
        if (!button || !dropdown) return;

        button.addEventListener('click', function (event) {
          event.stopPropagation();
          var open = !dropdown.classList.contains('open');
          dropdown.classList.toggle('open', open);
          button.setAttribute('aria-expanded', String(open));
        });

        $all('[data-action="logout"]', dropdown).forEach(function (logoutButton) {
          logoutButton.addEventListener('click', function () {
            client.auth.signOut().then(function () { window.location.reload(); });
          });
        });

        $all('a', dropdown).forEach(function (link) {
          link.addEventListener('click', closeDropdown);
        });

        if (!outsideBound) {
          document.addEventListener('click', function (event) {
            var menu = $('#profileMenu');
            if (menu && !menu.contains(event.target)) closeDropdown();
          }, { capture: true });
          document.addEventListener('keydown', function (event) {
            if (event.key === 'Escape') closeDropdown();
          });
          outsideBound = true;
        }
      }

      function render(session) {
        if (!session) {
          nav.innerHTML = '<a href="login" class="nav-login">Accedi</a>';
          return;
        }

        client.from('profiles')
          .select('username,avatar_color')
          .eq('id', session.user.id)
          .single()
          .then(function (result) {
            var profile = result && result.data ? result.data : {};
            var username = profile.username || (session.user.email || 'profilo').split('@')[0];
            nav.innerHTML = buildMenu(username, profile.avatar_color || '#2f6b67');
            bindMenu();
          });
      }

      client.auth.onAuthStateChange(function (_event, session) { render(session); });
      client.auth.getSession().then(function (result) {
        render(result && result.data ? result.data.session : null);
      });
    }

    function newsSeed(post) {
      var type = String(post.image_type || post.category || 'territorio').toLowerCase().replace(/[^a-z0-9]+/g, '-');
      return type || 'territorio';
    }

    function newsPhoto(slot, seed) {
      return '<div class="news-card__media photo-slot" data-photo-slot="' + escapeHtml(slot) + '" style="--photo:url(https://picsum.photos/seed/rivalta-news-' + escapeHtml(seed) + '/900/680)" aria-hidden="true"></div>';
    }

    function initPosts() {
      var status = $('#news-status');
      var filterRow = $('#news-filter-row');
      var featured = $('#post-featured');
      var grid = $('#posts-grid');
      var empty = $('#news-empty');
      if (!status || !filterRow || !featured || !grid || !empty) return;

      var categories = CATEGORY_ORDER.slice();
      var activeCategory = 'all';
      var posts = [];

      function setStatus(message) {
        status.textContent = message || '';
        status.style.display = message ? '' : 'none';
      }

      function fmt(iso) {
        if (!iso) return '';
        return new Date(iso).toLocaleDateString('it-IT', { day: 'numeric', month: 'short', year: 'numeric' });
      }

      function badgeClass(color) {
        return color === 'green' ? 'badge green' : 'badge';
      }

      function normalize(post) {
        var category = canonicalCategory(post.category) || 'Territorio';
        return Object.assign({}, post, {
          category: category,
          badge_color: post.badge_color || badgeColor(category)
        });
      }

      function filteredPosts() {
        if (activeCategory === 'all') return posts;
        return posts.filter(function (post) { return post.category === activeCategory; });
      }

      function renderFilters() {
        var chips = ['all'].concat(categories);
        filterRow.innerHTML = chips.map(function (cat) {
          var label = cat === 'all' ? 'Tutte' : cat;
          var on = activeCategory === cat ? ' on' : '';
          return '<button type="button" class="news-filter-chip' + on + '" data-cat="' + escapeHtml(cat) + '">' + escapeHtml(label) + '</button>';
        }).join('');

        $all('[data-cat]', filterRow).forEach(function (button) {
          button.addEventListener('click', function () {
            activeCategory = button.getAttribute('data-cat') || 'all';
            renderFilters();
            renderPosts();
          });
        });
      }

      function cardMarkup(post) {
        var seed = newsSeed(post);
        return '<article class="post-card">' +
          newsPhoto('news-card-' + escapeHtml(seed), seed) +
          '<div class="post-card__body">' +
          '<div class="post-meta"><time>' + escapeHtml(fmt(post.published_at)) + '</time><a href="' + categoryHref(post.category) + '">' + escapeHtml(post.category) + '</a></div>' +
          '<h3 class="post-card__title"><a href="post?id=' + encodeURIComponent(post.id) + '">' + escapeHtml(post.title || 'Articolo') + '</a></h3>' +
          '<p class="post-card__excerpt">' + escapeHtml(post.excerpt || '') + '</p>' +
          '<div class="post-card__footer"><a class="btn-read" href="post?id=' + encodeURIComponent(post.id) + '">Leggi <span aria-hidden="true">&rarr;</span></a><span class="' + badgeClass(post.badge_color) + '">' + escapeHtml(post.badge || post.category) + '</span></div>' +
          '</div></article>';
      }

      function renderPosts() {
        var list = filteredPosts();
        featured.style.display = 'none';
        grid.innerHTML = '';

        if (!list.length) {
          empty.style.display = '';
          setStatus(activeCategory === 'all' ? '' : 'Nessun articolo per la categoria selezionata.');
          return;
        }

        empty.style.display = 'none';
        setStatus('');
        var first = list[0];
        var seed = newsSeed(first);
        featured.classList.remove('skeleton');
        featured.innerHTML =
          newsPhoto('news-featured-' + escapeHtml(seed), seed) +
          '<div class="post-featured__body">' +
          '<div class="post-meta"><time>' + escapeHtml(fmt(first.published_at)) + '</time><a href="' + categoryHref(first.category) + '">' + escapeHtml(first.category) + '</a></div>' +
          '<h2 class="post-featured__title"><a href="post?id=' + encodeURIComponent(first.id) + '">' + escapeHtml(first.title || 'Articolo') + '</a></h2>' +
          '<p class="post-featured__excerpt">' + escapeHtml(first.excerpt || '') + '</p>' +
          '<div class="post-featured__footer"><a class="btn-read" href="post?id=' + encodeURIComponent(first.id) + '">Leggi <span aria-hidden="true">&rarr;</span></a><span class="' + badgeClass(first.badge_color) + '">' + escapeHtml(first.badge || first.category) + '</span></div>' +
          '</div>';
        featured.style.display = '';
        grid.innerHTML = list.slice(1, 7).map(cardMarkup).join('');
      }

      renderFilters();

      if (typeof supabase === 'undefined' || typeof SUPABASE_URL === 'undefined' || SUPABASE_URL.indexOf('your-project') >= 0) {
        posts = [];
        renderPosts();
        setStatus('Archivio non collegato. Quando Supabase risponde, le news appariranno qui.');
        return;
      }

      var client = supabase.createClient(SUPABASE_URL, SUPABASE_ANON_KEY);
      client
        .from('posts')
        .select('id, title, excerpt, category, badge, badge_color, image_type, published_at')
        .eq('published', true)
        .order('published_at', { ascending: false })
        .limit(60)
        .then(function (result) {
          if (result.error) {
            posts = [];
            renderPosts();
            setStatus('Non riesco a caricare le news in questo momento.');
            return;
          }
          posts = (result.data || []).map(normalize);
          renderPosts();
        });
    }

    function initGsap() {
      if (reduceMotion || typeof gsap === 'undefined' || typeof ScrollTrigger === 'undefined') return;
      gsap.registerPlugin(ScrollTrigger);

      gsap.utils.toArray('[data-parallax]').forEach(function (el) {
        var amount = parseFloat(el.getAttribute('data-parallax') || '0.1') * 240;
        gsap.to(el, {
          y: amount,
          ease: 'none',
          scrollTrigger: {
            trigger: el,
            start: 'top bottom',
            end: 'bottom top',
            scrub: 1.2
          }
        });
      });

      gsap.utils.toArray('[data-stack-card]').forEach(function (card, index) {
        gsap.fromTo(card,
          { y: 70, rotate: index % 2 ? -1.4 : 1.4, scale: 0.94 },
          {
            y: 0,
            rotate: 0,
            scale: 1,
            ease: 'power3.out',
            scrollTrigger: {
              trigger: card,
              start: 'top 88%',
              end: 'top 40%',
              scrub: 1.1
            }
          }
        );
      });

      gsap.utils.toArray('.photo-slot').forEach(function (slot) {
        gsap.fromTo(slot,
          { scale: 0.94 },
          {
            scale: 1.03,
            ease: 'none',
            scrollTrigger: {
              trigger: slot,
              start: 'top bottom',
              end: 'bottom top',
              scrub: 1.4
            }
          }
        );
      });

      ScrollTrigger.refresh();
    }

    document.addEventListener('DOMContentLoaded', function () {
      var year = $('#year');
      if (year) year.textContent = String(new Date().getFullYear());
      initNav();
      initReveal();
      initCounters();
      initMagneticButtons();
      initNewsletter();
      initCarousel();
      initAuth();
      initPosts();
      initGsap();
    });
  })();
  </script>
</body>
</html>
